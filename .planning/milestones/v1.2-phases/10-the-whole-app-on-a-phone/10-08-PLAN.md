---
phase: 10-the-whole-app-on-a-phone
plan: 08
type: execute
wave: 7
gap_closure: true
depends_on: ["10-05", "10-06", "10-07", "10-09", "10-10", "10-11"]
files_modified:
  - .planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md
autonomous: false
requirements: [PHON-07, PHON-08, PHON-09, PHON-10]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "This is the one part of the phase a machine cannot do, and it is the phase's own decisive test. PHON-10 asks for the whole trip proven in a shaper's hand on a real iPhone and a real Android phone. No automated test, on any emulator, at any viewport, satisfies it."
    - "The first sweep was not completed. Its own text says so: only the four named questions plus sign-in were reported, and 'treat any row of the ten-step table not mentioned below as still unwalked.' Steps 3, 5, 6, 8 and 10 — the account menu, the rack's rename, duplicate and delete, all five design screens by tapping the bar, saving, and the sideways return home — have never been walked on real hardware."
    - "Every row is PASS, FAIL or N/A, and nothing softer. A FAIL is written in the shaper's own words and never rounded up to 'mostly works' — that sentence is what becomes the next fix. An N/A carries one line of why."
    - "**This sweep restarts from step 1, on both phones.** One phone was walked on 2026-09-11, it found three things, and the walk was deliberately stopped. The fixes it produced change sideways behaviour on every screen, so nothing walked on that build still stands — not the rows that passed, and not the rows that failed. Every cell is answered again, on both phones, against the fixed build. The first walk's answers are kept on the sheet as a dated record of what was found, never as a filled-in row."
    - "The sheet must say plainly which answers are expected to be different this time, so the shaper compares against a written record rather than a memory — and two of the three predictions the sheet carried into the first walk are now wrong and must be replaced, not left standing. A phone held sideways now goes back to the desktop layout, controls beside the board, with the six screen tabs in the top row — the opposite of what the sheet last predicted, and the shaper's own decision (D-10). The Hide Toolbar tip no longer depends on the layout at all: on an iPhone it should appear upright AND sideways, and on an Android phone it should not appear at all, which is correct behaviour and not a fault. A board card is still about three-quarters of what a shaper can see on a tall enough screen, and on a short one it stops shrinking at a floor, so a board is always visible."
    - "The question that blocked scoring the tip on the first walk — which phone was it on? — no longer blocks anything, because the tip was moved off the layout switch. The sheet asks it as two separate rows, one per phone, each with its own right answer, so it is settled by the walk rather than by first identifying the device."
    - "Three rows the first walk never reached must survive into this one and be answered: saving a second board and checking the rack reads in the same order after leaving and coming back; the avatar's five-tap count; and the whole of the second phone, which was never walked at all."
    - "The avatar question is re-asked as a behaviour, never as a size. 'Unsure, looks the same to me' failed last time because a tap target cannot be judged by eye. The replacement is something a hand can actually do: tap just outside the visible rim of the round account picture, five times. Every tap opening the menu means the space is really there; some taps doing nothing means it is not."
    - "The sweep must run against code that actually contains the four fixes. On a phone that means either the deployed site after this work is pushed and Vercel has finished, or a development server reachable over the same Wi-Fi — and the sheet records which one, and the commit it was, so a failure found by hand is provably a new finding rather than a fix that had not shipped yet."
    - "The sheet is handed over against a fully green automated run — types, unit tests, build and the whole browser suite — recorded at the exact commit. That is what makes a hand-found failure a real, new finding instead of something the tests would have caught first."
    - "No credential, verification code or screenshot showing a filled-in sign-in form is ever written into the sheet. What is recorded is whether it worked, never what was typed to make it work."
    - "Nothing here claims a requirement is satisfied by automated tests. PHON-10 is satisfied by the walk or it is not satisfied."
    # --- edge-probe lift (spec-less fallback, --auto) ---
    - "PHON-10 / empty: the trip is walked once from a genuinely empty account — signed up fresh, no saved boards — so the home screen's no-rack state, the first save, and the rack appearing for the first time are all seen on real hardware rather than assumed from a populated account."
    - "PHON-10 / ordering: after saving a second board, the rack's order is recorded as observed and re-checked after leaving and returning to the home screen. It must be the same order both times; the order itself is decided by shipped, unit-tested code that this phase did not touch."
    - "PHON-10 / idempotency: the same board is opened from the rack twice, and the phone is turned sideways and back at least twice during the design screens. Both must land exactly where they landed the first time, with nothing left half-switched."
    - "PHON-10 / adjacency: the sideways checks are deliberately taken on both phones, because their sideways widths straddle nothing any more — about 844 dots on the iPhone and about 863 on the Pixel, both comfortably over the 820-dot switch. Both must now show the same thing sideways: the desktop layout, controls beside the board. If the two phones disagree, that is the finding."
    - "PHON-07: the account control's tap target is at least 44 dots in the compiled stylesheet, proven by an assertion against the real compiled CSS rather than by reading a class name out of a file. That test ships and runs in the suite this sheet is handed over against; the sheet's own question is the behaviour a machine cannot reach."
    - statement: "PHON-07: tapping a finger-width outside the visible rim of the round account picture opens the account menu, on a real phone, in a real signed-in session. No automated test in this project can reach this — under the suite's deliberately fake sign-in key the account control never settles, so nothing about it renders in any browser test. The count out of five is the only evidence there is."
      verification: backstop
    - "PHON-08 / idempotency: renaming, duplicating or deleting the same board twice leaves the rack in the state the second action implies — no orphaned card, no duplicate row, no board that reappears. Walked by hand on the rack, since the rack's own actions are what step 5 exercises."
    - statement: "PHON-08 / concurrency: a rack action interrupted by backgrounding the browser mid-way leaves no half-applied board — either the rename, duplicate or delete took, or it did not. Only a real phone that can genuinely be backgrounded can settle this."
      verification: backstop
    - statement: "PHON-10 / concurrency: the sweep may be interrupted. If the shaper stops part-way, the phone and the numbered step reached are written down in one sentence, and the next session resumes there rather than from step 1 — the same resume rule the first sheet carried."
      verification: backstop
    - "PHON-09: the summary screen and the on-screen order form are re-read on a real phone as step 9, including the printing that quick task 260910-2ny made work from the shaper's own iPhone. This requirement already has real-device evidence; the step exists so the whole trip is walked end to end rather than assembled from separate sittings."
  artifacts:
    - "10-SWEEP-2.md — the second sweep sheet, re-issued for a second walk: every answer cell empty again, the first walk preserved as a dated record beneath the tables, the changed expectations replaced with the ones the fixes actually deliver, the four named questions re-asked as behaviours, and a green-build header naming the commit it was handed over at"
    - "10-SWEEP-2.md, after the checkpoint — the shaper's own answers from both phones, verbatim, one per row, with no blank cell left"
  key_links:
    - "10-09, 10-10 and 10-11's merged fixes -> the build the phone actually loads. The sheet records which build and how it was reached, because a sweep against undeployed code proves nothing. All three must be on the branch the phone reaches before a single row is walked"
    - "10-05, 10-06 and 10-07's merged fixes -> the build the first walk was made against, two of which that walk found faults in. What survives of them is what plans 10-09 and 10-10 left standing, and the sheet describes the app as it is now, never as those three plans described it"
    - "10-RESEARCH.md's Real-Device Sweep section -> the ten steps, carried over unchanged in substance so this sweep is comparable with the first"
    - "10-07's avatar edge-tap behaviour -> question 1 on this sheet, lifted verbatim rather than reworded"
    - "the completed sheet -> PHON-07, PHON-08, PHON-09 and PHON-10. This walk is the evidence for all four, and the only evidence for PHON-10"
  prohibitions:
    - statement: "No row is filled from an emulator, a simulator, a screenshot or a memory of how it looked. If the actual phone was not in hand for that step, it is not a PASS."
      status: unverified
    - statement: "No FAIL is softened. It is recorded in the shaper's own words, exactly as said."
      status: unverified
    - statement: "No blank cell. Every one of the ten rows, on both phones, ends as PASS, FAIL or N/A with a reason. An incomplete sheet is the finding that failed this phase the first time and it is not repeated."
      status: unverified
    - statement: "Nothing typed into a sign-in form is ever written down — no email address, no password, no verification code, and no screenshot showing one filled in."
      status: unverified
    - statement: "No requirement is marked satisfied on this plan's own authority. The sheet records what happened; marking requirements is the orchestrator's job, after the verifier reads it."
      status: unverified
    - statement: "No code is changed in this plan. If the sweep finds something, it becomes the next fix with its own plan — it is not patched inside the sweep."
      status: unverified

security:
  asvs_level: 1
  block_on: high
---

<objective>
**Put the app back in a shaper's hands, and this time walk the whole thing.**

The first sweep found three real defects no emulator could have caught, and that was worth every
minute of it. But it stopped after the four questions it was pointed at. By its own admission the
ten-step table was never filled in — the account menu, renaming and duplicating and deleting a board
from the rack, working through all five design screens by tapping the bar, saving, and coming back to
the home screen sideways were all never walked at all.

Three of the fixes those failures produced have now landed: a phone held sideways stays a phone, a
board card is about three-quarters of whatever screen it is on, and the account avatar claims a
thumb's worth of space in a way the sign-in service's own styling cannot quietly override. So three
of the answers on this sheet are expected to be different from last time, and the sheet says exactly
which three, so nobody is comparing against a memory.

One question is re-asked differently on purpose. Last time the avatar's tap size came back "unsure,
looks the same to me", and that was correctly counted as a failure — a thumb-sized target is not
something anyone can judge by eye. This time it is a thing to do, not a thing to look at: tap the very
edge of the round picture, five times, and count how many open the menu.

Purpose: closes verification gap 3 in full, and the human half of gaps 1 and 2. This walk is the only
evidence PHON-10 can have.
Output: a second sweep sheet, handed over against a green build, and then filled in — every row, both
phones, no blanks.

## Re-issued after the first walk (2026-09-11)

**This plan has already been started once and is being re-issued, not started fresh.** Task 1 wrote
the sheet and it was handed over. One phone was walked. It found three things — the board pictures
collapsing to nothing sideways, the rail-band controls sitting under all three RAILS tabs, and the
Hide Toolbar tip still not appearing — and the walk was deliberately stopped there rather than
carrying on to a second phone that would have re-found the same three. Those findings, and the two
rulings taken from them, are recorded at the end of `10-SWEEP-2.md` under "First walk — what came
back".

Three fix plans have landed since (10-09, 10-10 and 10-11), and one of them is a founder decision
that changes what a phone does sideways on **every screen**. So this sweep restarts from step 1, on
both phones. Nothing walked on the old build still stands — not the rows that passed, and not the
rows that failed. Task 1 below is therefore a rewrite of the existing sheet, not the writing of a new
one, and the first walk stays on it as a dated record of what was found rather than as filled-in
answers.

Three rows the first walk never reached carry into this one and must be answered this time: saving a
second board and checking the rack reads the same order after leaving and coming back (step 8), the
avatar's five-tap count, and the entire second phone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP.md
@.planning/phases/10-the-whole-app-on-a-phone/10-VERIFICATION.md
@.planning/phases/10-the-whole-app-on-a-phone/10-05-SUMMARY.md
@.planning/phases/10-the-whole-app-on-a-phone/10-06-SUMMARY.md
@.planning/phases/10-the-whole-app-on-a-phone/10-07-SUMMARY.md
@CLAUDE.md
</context>

<gaps_this_plan_closes>
| Gap (10-VERIFICATION.md) | Closed by |
|---|---|
| 3 — the ten-step table was never filled row by row; steps 3, 5, 6, 8, 10 unwalked | Tasks 1, 2, 3 |
| 1, second `missing` item — walk sign-up and the account menu on real phones | Steps 1 and 3 of the sheet |
| 2, second `missing` item — walk the rack's rename, duplicate and delete on a real signed-in phone | Step 5 of the sheet |
| 1, first `missing` item's re-measurement — the avatar, in a genuine signed-in session | Named question 1, re-asked as a behaviour |

And, from the first walk of this very sheet (10-SWEEP-2.md, "First walk — what came back"):

| What the first walk left open | Closed by |
|---|---|
| Step 8 — save two boards, check the rack's order after leaving and coming back. Never reached. | Step 8, re-issued with a "never answered" marker |
| The avatar's five-tap count. Never answered. | Named question 1, re-issued with a "never answered" marker |
| The second phone. Never walked at all. | Both phone columns, walked from step 1 |
| Whether the three fixes from the first walk actually work in a shaper's hand | The whole sheet, re-walked against the fixed build |

Nothing in this gap set is deferred.
</gaps_this_plan_closes>

<executor_notes>
- **Your files, and only these:** `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` (new).
  You change no application code at all. If the sweep finds something, it becomes the next fix with
  its own plan — it is not patched inside this one.
- **Plans 10-09, 10-10 and 10-11 have all landed on `main` before you start**, on top of 10-05,
  10-06 and 10-07. Read the three newest SUMMARYs first — they are what the shaper is re-testing.
  Plan 10-07's SUMMARY carries the avatar edge-tap behaviour written out; lift it verbatim rather
  than rewording it. Plan 10-10's SUMMARY states in its own words which question the sweep has to
  ask about the Hide Toolbar tip on each phone; lift that verbatim too.
- **This plan has been started once already and is being re-issued.** Task 1 is a rewrite of an
  existing sheet, not the writing of a new one, and the dated first-walk record at the end of that
  sheet is preserved word for word. If you find yourself creating `10-SWEEP-2.md` from scratch, stop
  — you are about to delete the record of why this sweep is happening twice.
- **Do not run this plan in a worktree.** The green-build header must record a run on `main` at the
  commit the shaper's phone will actually load, and `npm run build` cannot resolve `next` in a
  worktree. If the orchestrator has placed you in one, say so and stop.
- **Task 2 is a blocking checkpoint.** `workflow.human_verify_mode` is `end-of-phase` and this is the
  end of the phase. At the checkpoint, do NOT write a SUMMARY — stop, and return the sheet and the
  handover instructions inline. The SUMMARY is written in task 3, after the shaper's answers come
  back.
- **`workflow.auto_advance` does not apply here.** A human walking a phone cannot be auto-approved.
- **Never write a credential into any file you produce**, including a SUMMARY, and never paste a
  screenshot that shows a sign-in form filled in.
- **Commit subjects are per task and in plain English about what changed for the shaper.**
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Re-issue the sheet — every step again, both phones, and the expectations that are actually true now</name>
  <files>.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md</files>
  <precondition>All three fix plans (10-09, 10-10, 10-11) must be merged and reachable from a phone before the sheet is handed over: either pushed to `main` with Vercel's deployment finished, or served by a development server on the same Wi-Fi network. The sheet records which, and the commit. A sweep against a build that does not contain the fixes proves nothing — and this sweep has already been run once against a build that contained two faults, which is the whole reason it is being re-issued.</precondition>
  <read_first>
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` in full, including the dated
      "First walk — what came back" section at the end — the sheet you are rewriting, what the first
      walk answered, and the two rulings it produced. That section is PRESERVED, not deleted: it is
      the record of why this sweep is happening twice.
    - The SUMMARYs from plans 10-09, 10-10 and 10-11 — what actually changed under the shaper's
      thumb since the first walk, in each one's own words, and the measured figures each recorded.
      The "What changed under your thumb" section of this sheet is written from those, not from the
      plans. Plan 10-10's SUMMARY states in its own words which question the sweep now has to ask
      about the Hide Toolbar tip on each phone; lift it rather than re-deriving it.
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP.md` in full — the first sheet. Its ten
      steps, its recording rules, its four named questions, its reachability instructions, its resume
      note, and its "Results" section. The second sheet keeps the same ten steps in substance so the
      two are comparable; what changes is that this one says which answers are expected to be
      different and why, and re-asks the avatar question as a behaviour.
    - `.planning/phases/10-the-whole-app-on-a-phone/10-VERIFICATION.md`, the gaps block — which rows
      were never walked, stated as a list. Those rows get a marker on this sheet so the shaper can see
      at a glance which ones have never been answered at all.
    - `.planning/phases/10-the-whole-app-on-a-phone/10-REVIEW.md` — for the mid-phase nav-row overflow
      fix, which is the subject of step 7's first question and now has a test behind it.
  </read_first>
  <action>
    Rewrite `10-SWEEP-2.md` so it can be walked again from step 1, on both phones, against the fixed
    build. It keeps the shape it already has, so it still reads as one series with the first sheet.
    Three things govern the rewrite:

    **The first walk is preserved, not overwritten.** The dated "First walk — what came back" section
    at the end of the sheet stays exactly where it is, word for word, including the shaper's verbatim
    answers, the code findings, the two rulings (D-10 and D-11) and the open question. It is the
    record of why this sweep is happening twice. Add one line at the top of it saying that the sheet
    above was re-issued on the date of this rewrite and that none of these answers was carried up
    into the tables.

    **Every answer cell is empty again.** Both phone columns, every one of the twelve rows, and every
    one of the named questions. Nothing is pre-filled and nothing is carried over — not even a row
    the first walk marked good, because the fixes change sideways behaviour on every screen and the
    build those answers were given against no longer exists.

    **Every expectation the sheet states must be one the fixed build actually delivers.** Two of the
    three it carried into the first walk are now wrong and must be replaced rather than left standing.
    Write the replacements from the three SUMMARYs, not from memory of what was predicted last time.

    Open with a green-build table recording the commit and the results of `npx tsc --noEmit`,
    `npx vitest run`, `npm run build` and `npm run test:e2e`, each run on `main` at that commit. Run
    them; do not carry numbers over from a SUMMARY. State the pass and fail counts as the tools
    report them.

    Then "What changed under your thumb **since your last walk**", in plain English about the board
    and the screen, written from the three SUMMARYs:

    - **A phone on its side is a normal browser again.** Turning it sideways now gives the controls
      beside the board and the six screen tabs in the top row, the way a computer shows it — the
      shaper's own decision from the last walk, in their own words, quoted on the sheet.
    - **A board's picture can never disappear.** It still takes about three-quarters of what you can
      see on a tall enough screen, and on a short screen it now stops shrinking at a floor instead of
      going to nothing. On a very short screen that means a card can be taller than the screen and
      take a small scroll to see whole — a board you can recognise, rather than no board at all.
      Say plainly that this trade is the thing being asked about.
    - **The rails instructions page no longer carries the sliders.** Reading it on a phone now ends
      where the reading ends. The sliders are still there, one tap away, on the VIEWER and DATA tabs.
    - **The Hide Toolbar tip no longer depends on the layout.** On an iPhone it should now appear
      both upright and sideways. On an Android phone it should not appear at all — its advice is
      Safari's own menu wording, so that is correct, not a fault.

    Carry over from the first sheet anything still true and not yet confirmed by hand — in
    particular that the account picture claims a thumb's worth of space around it, which has still
    never been confirmed by a real thumb.

    Then a short section, "What should be different this time, and what the last walk said". Name
    each changed expectation, quote what the shaper actually reported last time beside it, and — for
    the two predictions the last sheet got wrong — say plainly that the sheet predicted the opposite
    and why the prediction changed, so a shaper reading it can see the app's own reasoning being
    corrected rather than a claim quietly swapped. Include a line for the rails instructions page,
    which was not predicted at all last time because nobody knew about it until the shaper found it.

    Then the recording rules, carried over verbatim from the first sheet — PASS, FAIL or N/A and
    nothing softer; a FAIL in the shaper's own words; never write down anything typed into a sign-in
    form.

    Then how to reach the app from a phone, written for whichever route is actually true when you hand
    it over: the deployed site if the work is pushed and Vercel has finished, or the development
    server's network address otherwise. Name the commit either way.

    Then the ten steps, per phone, as a table with an iPhone column and an Android column. Keep the
    first sheet's steps and their "watch for" notes, and make these specific changes:

    - Mark steps 3, 5, 6, 8 and 10 with a visible "never walked" flag and one line each saying so.
      Steps 8 and the avatar question keep that flag even though the first walk reached the steps
      around them, because neither was ever answered.
    - Step 5 is expanded so all three rack actions are individually named — rename it, duplicate it,
      delete one — each with its own expected outcome, since "the rack's actions" as one row is how
      they went unwalked the first time. Add to each: do it a second time on the same board, and say
      whether the rack ends up in the state the second action implies — no leftover card, no
      duplicate row, no board that comes back.
    - Step 6 gains a RAILS instructions check by name: on the INSTRUCTIONS tab, does the page end
      where the reading ends, with no sliders underneath it? And are the sliders still there on
      VIEWER and DATA? This is the fix for what the shaper reported last time in their own words.
    - Step 6 keeps its instruction to turn the phone sideways and back at least twice, landing
      exactly where it started each time — same tab, same scroll position, nothing half-switched.
      Add: do one of those turns in the middle of dragging a control, and say whether the control
      ends up somewhere sensible rather than half-applied.
    - Step 7's expectation is rewritten to the opposite of what this sheet carried last time, and
      says so in as many words: a phone turned sideways on a design screen should now show the
      desktop look — controls beside the board, the six screen tabs in the top row — which is the
      shaper's own decision from the last walk. Keep its first question, does the screen scroll
      sideways at all, which it should not.
    - Step 7's toolbar-tip sub-question becomes two rows with two different right answers, one per
      phone: on the iPhone the tip should appear both upright and sideways; on the Android phone it
      should not appear at all, and that is correct rather than a fault. Say which is which on the
      sheet so neither is scored against the wrong expectation.
    - Step 8 keeps the second board and the rack-order check — save a second, different board, note
      the order the two appear in, leave the home screen and come back, and confirm the order reads
      the same both times. Flag it as never answered.
    - Step 10's expectation is rewritten: sideways, the home screen now shows the desktop look too,
      the two-up card grid stays, and — the thing that failed last time — every board picture must
      be visibly there, not a sliver and not nothing. Ask directly whether the four boards are still
      tellable apart at that size, and whether needing a small scroll to see a whole card feels
      wrong. That is a real question about a real trade, not a formality.
    - Add to step 1 that this walk starts from a genuinely fresh, empty account, so the empty rack, the
      first save and the rack appearing for the first time are all seen. Add one line asking whether
      a signed-in account with no boards saved yet shows the plain preset grid with no error and no
      empty-state message, which is what it is meant to do.

    Then the named questions, re-asked, each with what was said last time quoted beside it so the
    shaper can see exactly what is being re-tested.

    - Question 1 is the avatar, and it is a behaviour, not an impression: lift plan 10-07's edge-tap
      instruction verbatim — tap just outside the visible rim of the round account picture, five
      times, and write down how many of the five opened the menu. Say why the question changed shape:
      the first time it was answered "unsure, looks the same to me", and a tap target cannot be judged
      by eye. Mark it as still never answered, because the walk stopped before it.
    - Question 2, the iOS keyboard over a rename or name-a-board field, carries over unchanged.
    - Question 3 becomes the board pictures sideways on the home screen, with the shaper's own
      "horizontal the boards on the landing page cards have a 0 height" quoted beside it, and the new
      expectation: every board visibly there, the four still tellable apart, and a direct question
      about whether a small scroll to see a whole card feels wrong.
    - Question 4 becomes the sideways design screen, with "horizontal is useless, as there are no
      controls other than the drag" quoted beside it, and the new expectation: the desktop look,
      controls beside the board. Ask whether that is actually better in the hand, since it is the
      shaper's own call being implemented and this is the first chance to check it.
    - Add a question for the rails instructions page, with "rails keeps the controls under all 3
      tabs" quoted beside it and the new expectation stated.
    - Add a question for the Hide Toolbar tip, split by phone, with its two different right answers
      as above.

    Close with the overall verdict prompt and the resume note — which phone and which numbered step,
    in one sentence, if the walk has to stop part-way. Keep the resume note prominent: the last walk
    stopped part-way and that is a normal, expected outcome, not a failure.

    Every cell in the ten-step table and every named-question table cell is left empty for the shaper
    to fill, on BOTH phones. Do not pre-fill a single one, and do not carry a single answer up from
    the first walk's record below.
  </action>
  <acceptance_criteria>
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` exists and contains a green-build
      table naming a commit and the actual output counts from all four commands, run on `main`.
    - The ten-step table has an iPhone column and an Android column, both entirely empty. Report the
      `grep -c` you used to prove no cell is pre-filled, and the row count it must equal.
    - The dated "First walk — what came back" section is present and byte-identical to what it was
      before this rewrite, apart from the one added line saying the sheet above was re-issued.
      Report `git diff` for that section and confirm nothing else in it moved.
    - No answer from the first walk appears in any table cell. Report how you checked.
    - Steps 3, 5, 6, 8 and 10 each carry a visible "never walked before" marker, and step 8 and the
      avatar question carry it despite the first walk having reached the steps around them.
    - Step 5 names rename, duplicate and delete as three separately answerable items, each asking for
      the action to be done twice.
    - Step 6 names the RAILS INSTRUCTIONS tab check and the turn-while-dragging check.
    - Step 7 states that both phones should now show the desktop look sideways, says explicitly that
      this is the opposite of what this sheet predicted last time, and splits the toolbar-tip
      question into two rows with two different right answers.
    - Step 10 states the two-up grid stays and that every board picture must be visibly there, and
      asks the trade question about scrolling to see a whole card.
    - Question 1 is phrased as a countable action with a numeric answer, not as an impression, and
      matches plan 10-07's SUMMARY word for word.
    - Every expectation the sheet states was written from plan 10-09's, 10-10's or 10-11's SUMMARY,
      not from the predictions this sheet carried into the first walk. Name, for each changed
      expectation, which SUMMARY it came from.
    - The recording rules include the instruction never to write down anything typed into a sign-in
      form.
    - The sheet names how the shaper reaches the app and at which commit.
    - `grep -c` of the table's answer cells confirms none is pre-filled — report the check you used.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build && npm run test:e2e</automated>
  </verify>
  <done>There is a sheet a shaper can walk through again from the beginning with a phone in one hand, covering every step of the trip — including the ones that have still never been checked on a real phone — and it says plainly what should look different this time, including the two places the last sheet predicted the opposite.</done>
  <reversibility rating="reversible">A document. Nothing executable changes.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The shaper walks the whole trip on a real iPhone and a real Android</name>
  <files>.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md</files>
  <read_first>
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` — the sheet you wrote in task 1.
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP.md`'s "Results" section — what the first
      sweep found, so the handover can say honestly which answers are expected to be different.
    - `$HOME/.claude/gsd-core/templates/continue-here.md` — the pause pattern, in case the walk runs
      across two sittings.
  </read_first>
  <action>
    Hand the shaper the sheet and stop. This is a human-only action: it needs two real phones, a real
    account, a real thumb and a real keyboard, and no CLI, API or browser automation stands in for any
    of them. Do not attempt a single step on an emulator or a simulator, and do not fill a single cell
    on the shaper's behalf.

    Say plainly that this is a second walk of the same sheet and that it starts again from step 1 on
    both phones, because the fixes their last walk produced change what a phone does sideways on
    every screen — so nothing they answered last time still stands, including the rows that passed.

    Say plainly what is new under their thumb since their last walk, so they know what they are
    re-testing: a phone on its side is a normal browser again, controls beside the board and the six
    screen tabs in the top row, which is their own decision being implemented; a board's picture can
    no longer disappear on a short screen, though on a very short one a card may take a small scroll
    to see whole, and that trade is a real question for them; the rails instructions page no longer
    carries the sliders underneath it; and the tip about hiding the browser's own toolbar no longer
    depends on the layout, so on an iPhone it should now appear both ways up and on an Android phone
    it should not appear at all. The tip itself has not changed a word.

    Say too that the account picture's thumb-sized tap area has still never been confirmed by a real
    thumb, and that the count out of five is the only way it ever will be.

    Say too that the whole automated suite is green at the commit recorded at the top of the sheet, so
    anything the phones find is a real, new finding rather than something a test would have caught
    first.
  </action>
  <instructions>
    Open `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` and walk it.

    1. Reach the app from the phone by whichever route the sheet names — the deployed site, or the
       development server's network address on the same Wi-Fi.
    2. Walk all ten steps once on the iPhone, then once on the Android phone — **from step 1 both
       times, whatever was walked before.** Either order, and it can be done in separate sittings.
       The last walk covered one phone and stopped at step 10; none of those answers carries over,
       because the fixes it produced change sideways behaviour on every screen.
    3. Fill in every cell: PASS, FAIL or N/A, and nothing softer. A FAIL gets what broke, in the
       shaper's own words. An N/A gets one line of why.
    4. Give particular attention to the five steps marked "never walked before" — the account menu; the
       rack's rename, duplicate and delete; all five design screens by tapping the bottom bar; saving;
       and returning to the home screen sideways. None of those has ever been checked on a real phone.
    5. Answer the four named questions at the bottom. Question 1 is a count, not an impression: tap just
       outside the rim of the round account picture, five times, and write down how many of the five
       opened the menu.
    6. Never write down anything typed into a sign-in form — no email address, no password, no
       verification code, and no screenshot showing one filled in.

    If the walk stops part-way, write one sentence saying which phone and which numbered step was
    reached, so the next session resumes there rather than from step 1.
  </instructions>
  <verification>
    The sweep is done when `10-SWEEP-2.md` has no blank cell: every one of the ten steps carries a PASS,
    a FAIL with what broke, or an N/A with a reason, on both phones, and the four named questions are
    answered — question 1 as a count out of five. The executor then returns the completed table inline,
    restating every FAIL in enough detail to plan a fix from, and writes no SUMMARY until the shaper's
    answers come back.
  </verification>
  <acceptance_criteria>
    - Every one of the ten steps carries a PASS, a FAIL with what broke, or an N/A with a reason, on
      both phones — no blank cells.
    - The four named questions are answered, with question 1 answered as a number out of five.
    - The changed expectations are each explicitly confirmed or failed: the desktop look sideways on
      a design screen, the board pictures visibly there sideways on the home screen, the rails
      instructions page with no sliders under it, and the Hide Toolbar tip — present on the iPhone
      both ways up, absent on the Android phone.
    - The three rows the last walk never reached are answered: step 8's rack order after leaving and
      returning, the avatar's count out of five, and every row on the second phone.
    - The executor returns the completed table inline in its response and does NOT write a SUMMARY at
      this checkpoint.
    - If the sweep is paused, a continue-here note exists naming the phone and the step number to
      resume at.
    - No credential, verification code or image of a filled-in sign-in form appears anywhere in what is
      returned.
  </acceptance_criteria>
  <verify>
    <human-check>The shaper walks all ten steps on a real iPhone and a real Android phone and returns the sheet with no blank cell.</human-check>
  </verify>
  <done>Two real phones have been through the entire trip — signing up, signing in, the account menu, picking a board, renaming and duplicating and deleting one, all five design screens, saving, the summary, and back home sideways — and what they found is written down.</done>
  <resume-signal>Paste the filled-in sheet, or say "sweep complete" and hand back the file. If something failed, say so plainly — a FAIL here is the point of the exercise, not a setback.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write down exactly what the phones said</name>
  <files>.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md</files>
  <read_first>
    - The shaper's answers as returned at the checkpoint — the primary source, and the only source.
    - `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP.md`'s own "Results — sweep run
      2026-09-11" section — the house shape for recording a sweep's outcome: what was reported, what
      was inferred (nothing), and a plain statement that any row not mentioned is still unwalked.
  </read_first>
  <action>
    Transcribe the shaper's answers into `10-SWEEP-2.md`, into the cells they belong in, using the
    shaper's own words for anything that failed. Add a results section beneath the tables recording the
    date walked, which phones, and any finding in the shaper's own phrasing.

    Three rules, and they are the ones the first sweep's honesty depended on:

    - If a row came back blank, it is recorded as still unwalked. It is never inferred from a
      neighbouring answer, and never filled from what the automated tests say.
    - A FAIL is quoted, not paraphrased. The sentence the shaper wrote is what becomes the next fix.
    - Nothing is softened. If the sheet is incomplete, the results section says so at the top, in the
      same plain terms the first sheet used.

    Then write the plan's SUMMARY, and be explicit in it about what the sweep does and does not
    establish. Name each of PHON-07, PHON-08, PHON-09 and PHON-10 and say, for each, exactly which
    rows of the sheet evidence it and whether that evidence is complete. **Mark no requirement as
    satisfied** — that is the orchestrator's job after the verifier reads this — and do not claim
    PHON-10 is met by any automated test.

    If the sweep found anything, list each finding as its own item with the shaper's words, so it can
    be planned as a fix rather than buried in prose.
  </action>
  <acceptance_criteria>
    - Every cell in `10-SWEEP-2.md`'s ten-step and named-question tables is either an answer or an
      explicit "still unwalked" marker — no cell is silently empty.
    - Every FAIL is a verbatim quotation of what the shaper wrote.
    - The results section states the date, both phones, and whether the sheet is complete.
    - No credential, verification code or image of a filled-in sign-in form appears anywhere in the
      file or in the SUMMARY.
    - The SUMMARY names all four requirement IDs with the specific rows evidencing each, marks none as
      satisfied, and makes no claim that PHON-10 is met by an automated test.
    - `grep -n` over the finished file returns no remaining pre-fill placeholder text from task 1 —
      report the check you used.
  </acceptance_criteria>
  <verify>
    <human-check>The shaper confirms the recorded answers are what they actually said, and that no FAIL has been softened or rounded up.</human-check>
    <automated>test -s .planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md</automated>
  </verify>
  <done>What the two phones actually said is written down, in the shaper's own words, with nothing rounded up and nothing left blank — so whatever comes next is planned against what happened rather than what was hoped.</done>
  <reversibility rating="reversible">A document.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| New document | `10-SWEEP-2.md` — the second real-device sweep sheet | `.planning/phases/10-the-whole-app-on-a-phone/` |
| Green-build record | the commit, and the four tool results it was handed over against | `10-SWEEP-2.md` header |
| Filled results | the shaper's own answers, one per row, both phones | `10-SWEEP-2.md` results section |
| Requirement evidence map | which rows evidence PHON-07, PHON-08, PHON-09 and PHON-10, and how completely | `10-08-SUMMARY.md` |
</artifacts_this_phase_produces>

<flagged_assumptions>
Edge-probe rows, re-resolved for the re-issued sweep:

- **PHON-09** — the summary screen and the on-screen order form render with no sideways page scroll
  at every phone width this suite tests, and step 9 of this sheet re-reads them in the hand inside
  the whole trip rather than in isolation. PHON-09 already has real-device evidence from four quick
  tasks on 2026-09-10, including printing from the shaper's own iPhone, and the first walk answered
  step 9 "sumamry screen looks pretty good both V and H" — which is re-asked anyway, because the
  build it was said about no longer exists.
- **PHON-07, PHON-08 and PHON-10's remaining rows** are lifted into this plan's truths above, each
  marked explicit or backstop. The two backstops — tapping outside the avatar's rim, and a rack
  action interrupted by backgrounding the browser — cannot be reached by any test in this project,
  and this walk is their only evidence.

Planner assumptions the executor must check rather than inherit:

- That the gap-closure work will be reachable from a phone at sweep time. The task carries this as an
  explicit precondition and the sheet must record which route and which commit — a sweep against a
  build without the fixes would repeat the whole exercise for nothing.
- That the shaper has both an iPhone and an Android phone available in the session. If only one is,
  the other phone's column is N/A with that reason, and PHON-10 remains incomplete rather than being
  quietly declared met on one device.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shaper's real signed-in session → a written record | The one place in this phase where real credentials and a real session are exercised, and where a careless note could capture one |
| deployed site or LAN development server → a phone on the network | The sweep is walked against a real build over a real network, not against a test harness |
| a human's spoken answer → a planning document | Transcription is where a FAIL can be softened into a PASS by accident |

No code changes. No dependency changes. The risk surface here is a document and a real session.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12 | Information Disclosure | `10-SWEEP-2.md` and the plan SUMMARY | high | mitigate | The sweep is the only part of this phase exercising a real signed-in session, so it is the only place a credential, verification code or a screenshot of a filled-in sign-in form could be written down. The rule is carried verbatim from the first sheet into the recording rules, repeated in the checkpoint's own instructions, and asserted as an acceptance criterion on task 3. |
| T-10-13 | Repudiation | the transcription of the shaper's answers | medium | mitigate | An answer softened or inferred during transcription destroys the evidence PHON-10 rests on. Task 3 requires verbatim quotation of every FAIL, an explicit "still unwalked" marker for any blank, and a human check that the record matches what was said. |
| T-10-14 | Spoofing | a development server reachable over Wi-Fi | low | accept | A LAN development server is unauthenticated by nature and this is a shaper's own home or shop network, walked for minutes. The alternative — the deployed site — is preferred where available, and the sheet records which was used. |
| T-10-15 | Tampering | walking against a build without the fixes | medium | mitigate | A sweep against undeployed code would produce false failures and waste the walk. Task 1 carries an explicit precondition and the sheet records the commit and the route by which the phone reached it. |
| T-10-SC | Tampering (supply chain) | npm installs | low | accept | This plan installs no package and changes no application code; `package.json` and both lockfiles are outside its file list. |

ASVS level 1, block on `high`. T-10-12 is the one `high` finding and it is mitigated in three places —
the sheet's own rules, the checkpoint's instructions, and a task acceptance criterion.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npx vitest run`, `npm run build` and `npm run test:e2e` all run on `main` at
  the handover commit, with their real results recorded in the sheet's header.
- The sheet is handed over with every answer cell empty.
- After the checkpoint, no cell is silently empty — each is an answer or a marked "still unwalked".
- Every FAIL is a verbatim quotation.
- No credential appears anywhere in the sheet or the SUMMARY.
- The SUMMARY maps all four requirement IDs to the rows evidencing them and marks none satisfied.
</verification>

<success_criteria>
- The whole trip has been walked on a real iPhone and a real Android phone: sign up, sign in, the
  account menu, picking a preset, the rack's rename and duplicate and delete, all five design screens
  by tapping the bottom bar, saving, the summary, and the sideways return home.
- Every row on both phones is PASS, FAIL or N/A with a reason. No blank cell — the condition the first
  sweep failed.
- The avatar question is answered as a count out of five, not as an impression.
- The three changed expectations are each confirmed or failed on real hardware.
- Whatever the phones found is written down in the shaper's own words, ready to be planned as a fix.
</success_criteria>

<output>
At the checkpoint in task 2, write no SUMMARY — return the sheet and the handover instructions inline
and stop. After the checkpoint resolves, create
`.planning/phases/10-the-whole-app-on-a-phone/10-08-SUMMARY.md`, including the filled sheet's results,
the requirement evidence map for PHON-07, PHON-08, PHON-09 and PHON-10 with none marked satisfied, and
each finding listed separately in the shaper's own words so it can be planned as its own fix.
</output>
