---
phase: 08-the-rails-screen-finished
plan: 06
type: execute
wave: 5
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05]
files_modified: []
autonomous: false
requirements: [PRNT-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With the box unticked, the Overview Sheet, the Full Sized Template and the Paper Saver are byte-identical to their pre-phase output, in both Imperial and Metric, proven by rebuilding every page from the pre-phase commit and comparing content streams (PRNT-06, D-11)."
    - "With the box unticked, the order form's printed output is unchanged in both systems and on both paper sizes — proven by a scripted print-to-PDF diff if that proves reliable, and by the Phase 7 D-10 human print-preview audit if it does not (PRNT-06, D-11)."
    - "components/template/ is byte-identical to its pre-phase state, so the three jsPDF outputs are unchanged by construction as well as by measurement (D-11)."
    - "The new preference column reaches production only after this phase's code is pushed to main and Vercel has finished deploying it (CLAUDE.md database rule, D-07)."
  artifacts:
    - "PRNT-06 evidence recorded in 08-06-SUMMARY.md: the pre-phase SHA, the per-surface page counts, the number of differing content-stream operators per surface, and the order-form method actually used"
  key_links:
    - "pre-phase commit 3210103522a3a5e59148401f57904a23fae93026 -> detached scratch worktree -> rebuilt PDFs -> pypdf content-stream compare"
    - "push to main -> Vercel deploy completes -> npm run db:migrate:prod"
  prohibitions:
    - "components/template/* is not modified by this plan or any plan in this phase (D-11)."
    - "npm run db:migrate:prod is never run before the code is pushed and deployed (CLAUDE.md database rule)."
    - "No throwaway probe file is left behind in the repository after the diff."
    - "The scratch worktree is removed and no .env file of any kind is created."
---

<objective>
Prove the promise the whole milestone rests on: a shaper who never ticks the box prints exactly what
they print today. Rebuild every Overview Sheet, Full Sized Template and Paper Saver page from the
commit before this phase started and compare them against the ones the current code produces; do the
same for the order form as far as the tooling reliably allows, and fall back to the print-preview
audit Phase 7 used where it does not. Then, and only then, let the shaper put the new preference
column on the production database.

Purpose: PRNT-06, plus the one step of this phase that must happen in the right order or the live
site reads a database it was not built for.

Output: measured evidence in the SUMMARY, and a migrated production database.
</objective>

## Artifacts this phase produces

This plan creates **no source files**. Its output is evidence recorded in
`08-06-SUMMARY.md` plus one applied production migration. Every symbol this phase produces is listed
in the "Artifacts this phase produces" sections of plans 08-01 through 08-05; nothing new is added
here.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-the-rails-screen-finished/08-CONTEXT.md
@.planning/phases/08-the-rails-screen-finished/08-RESEARCH.md
@.planning/phases/08-the-rails-screen-finished/08-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>

1. **This plan is not autonomous.** Task 3 is a blocking human action. Tasks 1 and 2 run without a
   stop; Task 3 waits for the shaper.
2. **Worktree limits.** `npm run build` does not work in a worktree; the orchestrator runs the real
   build on `main` after the merge. `worktree.cleanup-wave` refuses file deletions — the throwaway
   probe below is created and deleted inside a single command so no deletion survives the wave.
3. **`.env*` files are hard-blocked** for every agent. `npm run db:migrate:prod` pulls and removes its
   own transient production env file through the npm script's own trap; you never write one yourself,
   and Task 3 is the shaper's action, not yours.
4. **Environment facts** (verified 2026-09-07): Google Chrome 152.0.7977.77 is installed; Python
   `pypdf` 6.16.1 is importable; `qlmanage` is at `/usr/bin/qlmanage`; poppler, Puppeteer and
   Playwright are **absent** and none may be installed — Playwright belongs to Phase 9.

</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Prove the three template PDFs are unchanged, page for page</name>
  <files>(no repository files are modified — a throwaway probe is created and deleted in the same command)</files>
  <read_first>
    - `.planning/milestones/v1.1-phases/07-metric-on-paper/07-VERIFICATION.md` — the "Human
      Verification Outcome" table, rows 2–7: the exact method (rebuild every PDF from the pre-phase
      commit, compare content streams) and the result that is the bar to repeat, 68 pages byte-identical
      across the three surfaces.
    - `.planning/phases/08-the-rails-screen-finished/08-RESEARCH.md` — Environment Availability and
      "Don't Hand-Roll" (why a content/visual diff rather than a byte diff of the whole file: a
      Chrome- or jsPDF-produced PDF is not guaranteed byte-identical run to run because of producer
      metadata and font subsetting).
    - `components/template/build-template-pdf.ts`, `build-strip-pdf.ts`, `build-overview-pdf.ts` —
      the three builders' exported entry points and their arguments, so the probe can call them
      directly. Read only; never modified.
    - `components/template/build-strip-pdf.test.ts` — how an existing test constructs a board and
      calls a builder, the shape the probe copies.
  </read_first>
  <action>
    The pre-phase baseline is commit `3210103522a3a5e59148401f57904a23fae93026` — the last commit
    before any Phase 8 code landed. Confirm that by walking `git log --oneline` and checking the
    commit after it is the first Phase 8 code commit; if the history says otherwise, use the actual
    commit before the first Phase 8 code commit and record which one you used.

    Add a detached scratch worktree at that commit somewhere outside the repository (a path under
    `/private/tmp`), and symlink the current checkout's `node_modules` into it so the old tree can run
    without a second install. Do not run `npm install` in the scratch tree.

    In each tree — the scratch one and the current one — write a throwaway probe test file under
    `components/` named so it sorts last and is obviously disposable, which builds every PDF the three
    surfaces produce, in both Imperial and Metric, for the same fixed board inputs on both paper
    sizes, and writes each to a per-tree output directory. Run it with vitest, then delete the probe
    file in the same shell command that ran it, so no probe survives even if a later step fails.

    Compare the two output directories with `pypdf`: for each PDF, assert the page count matches and
    then extract and compare each page's content stream — text operators and drawing operators alike.
    Report, per surface, the total number of pages compared and the number of differing operators.
    The expected result is zero differences on every page of all three surfaces; anything else is a
    regression to investigate, not to explain away. Where a page-level difference needs eyes, render
    just that page to PNG with `qlmanage -t` and look — there is no poppler on this machine.

    Remove the scratch worktree when finished. Record in the SUMMARY: the baseline SHA, the page
    count per surface per system, the differing-operator count per surface, and the total.
  </action>
  <verify>
    <automated>git diff --quiet 3210103522a3a5e59148401f57904a23fae93026 -- components/template/ &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 3210103522a3a5e59148401f57904a23fae93026 -- components/template/` prints nothing.
    - `git diff --stat 3210103522a3a5e59148401f57904a23fae93026 -- lib/geometry/rail-bands.ts lib/models/design-snapshot.ts components/summary/use-print-fit.ts` prints nothing.
    - The pypdf comparison reports the same page count in both trees for every one of the three surfaces, in both systems.
    - The pypdf comparison reports **zero** differing content-stream operators across all compared pages; the SUMMARY records the exact totals.
    - `git status --porcelain` shows no leftover probe file, and `git worktree list` shows no scratch worktree.
    - `npm test` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A read-only measurement; nothing in the repository changes.</reversibility>
  <done>Every page of the Overview Sheet, the Full Sized Template and the Paper Saver is measured identical to its pre-phase output in both systems, and the evidence is in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the unticked order form is unchanged</name>
  <files>(no repository files are modified)</files>
  <read_first>
    - `.planning/milestones/v1.1-phases/07-metric-on-paper/07-04-SUMMARY.md` — how Phase 7 proved the
      order form unchanged: the units ledger plus a human print-preview audit, not a PDF diff.
    - `.planning/milestones/v1.1-phases/07-metric-on-paper/07-VERIFICATION.md` — row 1 of the Human
      Verification Outcome table: firing the real `beforeprint` handler and measuring zoom and clipped
      elements across every node, and why one measured box covers both papers.
    - `.planning/phases/08-the-rails-screen-finished/08-RESEARCH.md` — Pitfall 4 (headless Chrome can
      print before this app's own `beforeprint` handler has attached) and Open Question 2 (the wait is
      not empirically established in this repo).
    - `components/summary/use-print-fit.ts` — `useOrderFormPrintFit`'s `beforeprint` listener and its
      per-sheet walk, so you know what a correctly captured print looks like.
  </read_first>
  <action>
    The order form is a browser-printed page, not a jsPDF build, so it needs its own proof. Attempt
    the scripted route first, and stop attempting it the moment it proves unreliable — this is a
    best-effort path with a named fallback, not a requirement.

    **Scripted attempt.** Start the app, then drive Google Chrome's own headless print-to-PDF against
    `/design/summary` with the box **unticked**, in Imperial and in Metric and for both paper sizes.
    Chrome's `--print-to-pdf` runs the browser's real print pipeline — the same `@media print` CSS and
    the same `beforeprint` event `window.print()` fires — so it is a legitimate capture. The one risk
    is timing: this app's print fit attaches its `beforeprint` listener inside an effect after React
    hydrates, and a bare URL load can print before that has happened, producing a PDF that never had
    the sheets pinned. Give the page a deliberate wait before the print step (a virtual-time budget,
    or navigate-wait-then-print), and validate the capture before trusting it: the captured PDF must
    have exactly two pages and its sheet boxes must match what a human sees in the browser's own print
    preview. Then capture the same four combinations from the pre-phase scratch worktree of Task 1 and
    compare content streams with `pypdf`, exactly as Task 1 does.

    **Fallback, and it is a real one.** If the capture cannot be made to reflect the hydrated,
    print-fitted page reliably — wrong page count, unpinned sheets, or results that vary run to run —
    stop, record precisely what failed and why, and fall back to the Phase 7 D-10 method: a human
    print-preview audit of the unticked order form in both systems on both paper sizes, listed as a
    `<human-check>` below and harvested into end-of-phase UAT. Do not spend further effort forcing the
    automated path, and do not install Puppeteer or Playwright to get around it — Playwright belongs
    to Phase 9 by the roadmap's own sequencing.

    Either way, record in the SUMMARY which method was used, the exact commands, and the result for
    each of the four combinations.
  </action>
  <verify>
    <automated>npx vitest run lib/units-isolation.test.ts &amp;&amp; npm test</automated>
    <human-check>With the box unticked, open the summary's print preview in Imperial on Letter and on A4, and in Metric on Letter and on A4, and confirm pages 1 and 2 are exactly what they were before this phase — same layout, same values, same page marks reading of 2, same page-count note, nothing clipped.</human-check>
    <human-check>Tick the box and confirm the preview gains a third page and the marks read of 3, with pages 1 and 2 otherwise unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY names the method actually used for the order form (scripted diff or human audit) and, if scripted, the exact commands and the differing-operator counts for all four combinations.
    - If the scripted route was abandoned, the SUMMARY records the specific failure mode observed, not a generic "it was unreliable".
    - `npx vitest run lib/units-isolation.test.ts` exits 0 — the ledger's own proof that no print surface is silently imperial by omission still holds.
    - `git diff --stat 3210103522a3a5e59148401f57904a23fae93026 -- components/summary/use-print-fit.ts app/globals.css` prints nothing.
    - No new npm dependency appears in `package.json` (`git diff --quiet 3210103522a3a5e59148401f57904a23fae93026 -- package.json package-lock.json` exits 0).
    - `npm test` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A read-only measurement; nothing in the repository changes.</reversibility>
  <done>The unticked order form is shown unchanged in both systems and on both paper sizes, by measurement where measurement was reliable and by the Phase 7 audit method where it was not — and which one was used is on the record.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Push, let it deploy, then migrate production</name>
  <files>(no repository files are modified — this step pushes existing commits and applies an already-generated migration to the production database)</files>
  <action>
    Stop and hand this to the shaper. Do not push, and do not run the production migration, on your
    own initiative. Present the steps under `<instructions>` below, in that order, and wait for the
    resume signal. If the shaper asks the orchestrator to run the commands on their behalf — as
    happened in Phase 5 (05-07) — the same ordering still applies exactly and the shaper's own resume
    signal still gates it.
  </action>
  <instructions>
    See `<how-to-verify>` below for the six ordered steps and the CLAUDE.md rule they enforce.
  </instructions>
  <verification>
    The shaper confirms step 6: on the live site, signed in, the tick-box survives a reload.
  </verification>
  <what-built>
    The rails screen's INSTRUCTIONS tab, its plan and side reference figure, the actual-size view and
    its print path, the "Include Rail Band Instructions in Print" preference and the third order-form
    sheet it adds — all merged and green, with the new `print_rail_instructions` column applied to the
    Neon **development** branch only. Production has not been migrated.
  </what-built>
  <how-to-verify>
    CLAUDE.md's database rule is absolute and is quoted here in full:

    > **Always push code before migrating production.** When a change touches both the code and the
    > shape of the database, push to `main`, let Vercel finish deploying it, and only then run
    > `npm run db:migrate:prod` — the deployed site has to already understand a new column before that
    > column exists, or the live site ends up reading a database it wasn't built for. Never migrate
    > production ahead of the code.

    In this order, and only in this order:

    1. Confirm the phase's work is merged to `main` and `npm test` and `npm run build` are green from
       the main checkout (not a worktree).
    2. `git push origin main`.
    3. Watch the Vercel deployment for that commit through to **Ready**. Do not proceed while it is
       still building.
    4. Open https://shaper-coral.vercel.app, go to the rails screen, and confirm the site works
       normally — the tick-box is present and the site does not error even though its column does not
       exist in production yet. This is the deliberate degrade-to-cookie path; if the site is broken
       here, stop and say so rather than migrating.
    5. Run `npm run db:migrate:prod` from the main checkout. It pulls a transient production env file
       and removes it on exit through its own trap — do not create or edit any `.env` file by hand.
    6. Reload the live site, tick the box while signed in, reload again, and confirm it stayed ticked.

    If you would rather the orchestrator ran steps 2 and 5 for you, say so — that is what happened in
    Phase 5 (05-07), with this same ordering followed exactly and gated on your own resume signal.
  </how-to-verify>
  <resume-signal>Type "deployed and migrated" once step 6 passes, or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-phase commit -> scratch worktree | historical repository content, re-executed locally to produce comparison output |
| local checkout -> production database | the only step in this phase that writes to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-20 | Tampering | the production schema migration | high | mitigate | Ordering is enforced as a blocking human action that quotes CLAUDE.md's rule in full and requires the deployment to be Ready and the live site to be confirmed working before the migration runs. The migration itself only adds one nullable column and drops nothing, verified by reading the generated SQL in 08-02. |
| T-08-21 | Denial of Service | the live site between push and migration | medium | mitigate | The account read is wrapped in try/catch and degrades to the cookie, and the write is fire-and-forget through a bounded queue, so the site works normally in the window where the column does not exist yet. Step 4 of the checkpoint verifies this on the real site before the migration. |
| T-08-22 | Information Disclosure | the transient production env file | high | mitigate | `npm run db:migrate:prod` pulls it and removes it through its own shell trap on EXIT/INT/TERM; no agent creates, copies or edits any `.env` file, and every agent is hard-blocked from doing so. |
| T-08-23 | Tampering | the throwaway probe test files | low | mitigate | Created and deleted inside a single shell command so no probe survives a failure, and confirmed gone by `git status --porcelain` before the plan closes. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero new packages. This plan explicitly forbids installing Puppeteer or Playwright to rescue the scripted diff, and asserts it with a `git diff --quiet` on `package.json` and `package-lock.json` against the pre-phase commit. |
</threat_model>

<verification>
- `git diff --stat <pre-phase-sha> -- components/template/` prints nothing.
- Zero differing content-stream operators across every page of the three jsPDF surfaces, both systems,
  both paper sizes.
- The unticked order form shown unchanged in both systems on both papers, by the scripted diff or by
  the Phase 7 human audit — with the method named in the SUMMARY.
- `package.json` and `package-lock.json` unchanged against the pre-phase commit.
- No probe file and no scratch worktree left behind.
- Production migrated only after the push and a Ready deployment, confirmed by the shaper's own
  resume signal.
</verification>

<success_criteria>
- With the box unticked, every printed output — order form, Overview Sheet, Full Sized Template,
  Paper Saver — is provably what it was before this milestone.
- The evidence is measured and recorded, not asserted.
- The new preference column exists in production, applied after the code was deployed, and the
  preference survives a reload on the live site.
</success_criteria>

<output>
Create `.planning/phases/08-the-rails-screen-finished/08-06-SUMMARY.md` when done.
</output>
