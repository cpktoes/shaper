---
phase: 01-foundation-port-deploy-the-design-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/geometry/presets.ts
  - lib/geometry/presets.test.ts
  - components/design/design-store.tsx
  - app/layout.tsx
  - app/design/layout.tsx
  - app/page.tsx
  - components/setup/setup-screen.tsx
autonomous: false
requirements: [SETUP-01, OUTL-01, UNIT-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- End-to-end tracer behaviour (SETUP-01 / OUTL-01, D-01/D-02/D-04/D-05) ---
    - "Visiting `/` renders the setup screen directly — no forward to `/design/outline` — with exactly one entry per `BOARD_PRESETS` id (shortboard, fish, midlength, longboard)."
    - "Clicking a preset entry on `/` writes that preset's complete `OutlineSpec` into the shared board store and navigates to `/design/outline`, where the outline viewer immediately renders that preset's curve."
    - "Exactly one board-store provider instance mounts for the whole app: a preset applied on `/` is visible on `/design/outline` after client-side navigation, with no provider remount and no state reset."
    - "Both `/` and `/design/outline` fill the viewport height after the provider promotion — the design screens' `flex-1` panels still expand rather than collapsing to content height."
    # --- Edge-coverage lift: SETUP-01 (empty, encoding) ---
    - "Starting a design never begins from empty, partial, or null dimensions: `applyPreset` is the only whole-outline writer and every `BOARD_PRESETS` entry is a complete `OutlineSpec`, so `buildOutline()` never receives a partial spec."
    - "Preset identity comparison introduces no string-length or normalization rule: `id`, `name`, and `descriptor` are compile-time ASCII literals and presets are selected by `===` on `id`, so byte / code-point / grapheme-cluster definitions are all equivalent here."
    # --- Edge-coverage lift: OUTL-01 (empty, encoding) ---
    - "`buildOutline()` returns an `OutlineGeometry` with a non-empty `points` array and does not throw for every one of the four `BOARD_PRESETS` entries."
    - "Outline dimensions are branded numeric types (`Mm` / `Degrees`), never strings — every `BOARD_PRESETS` length/width/offset is authored via `inchesToMm()` and every angle via `degrees()`, so no text encoding or normalization rule applies to outline data."
    # --- Edge-coverage lift: UNIT-01 (boundary, precision) ---
    - "Every `BOARD_PRESETS` field lies inside the `OutlineControls` slider range for that field (length 60-120in, widePointWidth 16-25in, widePointOffset -12..12in, railLength 0-100, noseAngle 35-90, noseFullness 0-100, tailAngle 30-90, tailFullness 0-100, tail endWidth 0-16in, swallow crotchDepth 1-8in, diamond depth 1-5in) — a preset can never strand the editor on a value its own slider cannot reach."
    - "Every `BOARD_PRESETS` inch value round-trips `inchesToMm()` then `mmToInches()` to within 1e-9 inches, and display rounding is `formatInchesFraction`'s documented contract (nearest 1/denominator, default 16ths, half-away-from-zero with a 1e-9 nudge, fraction reduced by halving) — no preset value is authored as a raw millimetre literal."
    # --- UI-SPEC `## UI Considerations` lift: E5 Site nav (promoted to root layout) ---
    - "E5/loading: the promoted site nav renders its static links with the root layout — no loading, skeleton, or reserved-space state."
    - "E5/error: the promoted site nav is plain navigation links with no error state."
    - "E5/long-text: site nav wordmark and tab labels are fixed copy with no variable-length text."
    - statement: "The SHAPER wordmark and all five nav tabs neither wrap nor clip at a 375px viewport width on `/`."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT display a raw internal millimetre value under an inch or litre label, and MUST NOT perform an inch/mm conversion outside `lib/geometry/units.ts` — a shaper cuts foam to these numbers, so a mislabelled unit destroys a blank."
      status: resolved
      verification: judgment
  artifacts:
    - lib/geometry/presets.ts
    - lib/geometry/presets.test.ts
    - components/setup/setup-screen.tsx
    - app/page.tsx
  key_links:
    - "`app/layout.tsx` mounts exactly ONE board-store provider above both `/` and `/design/*` — leaving a second instance in `app/design/layout.tsx` silently desyncs preset application with no error"
    - "`applyPreset` -> `state.outline` -> `outlineGeometry` (useMemo) -> `OutlineViewer` — the single path that makes a clicked preset visible in the editor"
    - "`inchesToMm()` / `degrees()` at preset-authoring time is the only unit boundary preset data crosses"
    - "`app/design/layout.tsx`'s `flex min-h-0 flex-1 flex-col` wrapper is layout-critical, not decoration — it passes full-height sizing from the root `body` down to the editors"
---

<objective>
**As a** shaper, **I want to** land on Shaper and pick a board type, **so that** I start shaping a
recognisable board immediately instead of a blank 6'0" default.

This is the phase's **tracer**: the thinnest production-quality path that touches every layer Phase 1
modifies — pure geometry data (`lib/geometry/presets.ts`), shared client state
(`design-store.tsx`), the app shell (`app/layout.tsx`, `app/design/layout.tsx`), and a real route
(`app/page.tsx` + `components/setup/setup-screen.tsx`) — wired end-to-end and verified.

Purpose: prove the preset -> store -> geometry -> render path works before any UI chrome is built on
top of it. The single highest-risk assumption in this phase is that one provider instance can span
`/` and `/design/*`; this plan settles it on the first commit.

Output: a working `/` that starts a real design, four board-type presets as tested pure data, and one
board-store provider serving the whole app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-CONTEXT.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-PATTERNS.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/SKELETON.md

Next.js 16 conventions differ from most training data. Before editing any file under `app/`, read
`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md`. This codebase
already uses the current conventions (`LayoutProps<"/">` typed helpers) — extend them, do not
substitute remembered Next 13/14 idioms.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "pick a board type and shape it" — one path, every layer</name>
  <files>lib/geometry/presets.ts, components/design/design-store.tsx, app/layout.tsx, app/design/layout.tsx, app/page.tsx, components/setup/setup-screen.tsx</files>
  <read_first>
    - `lib/geometry/board.ts` — the real `OutlineSpec` and `TailShape` shapes presets must satisfy, plus `DEFAULT_BOARD_SPEC` and `TAIL_PRESETS` as the data-authoring convention to copy
    - `lib/geometry/units.ts` — the units boundary; read its head comment before authoring any numeric value
    - `components/design/design-store.tsx` — the existing `updateOutline` / `updateFins` / `setBoardName` action shape the new action must match, and the "no effects that write state" head comment
    - `app/layout.tsx` — current root layout (fonts, metadata, `min-h-full flex flex-col` body)
    - `app/design/layout.tsx` — the provider + nav + flex-wrapper block being lifted, and its head comment explaining why the wrapper div is load-bearing
    - `app/page.tsx` — the file being replaced
    - `app/design/outline/page.tsx` — the thin-Server-Component-renders-client-component convention `app/page.tsx` must follow
    - `components/outline/outline-editor.tsx` — the `"use client"` + `useDesign()` client-screen pattern `setup-screen.tsx` must follow
    - `components/outline/outline-controls.tsx` — the slider min/max for every outline field; preset values must land inside these
    - `.planning/phases/01-foundation-port-deploy-the-design-tool/01-RESEARCH.md` — Pattern 1 (provider promotion), Pattern 2 (preset as a patch), Pitfall 1 (height chain), Anti-Patterns
  </read_first>
  <behavior>
    - `BOARD_PRESETS` has exactly 4 entries with ids `shortboard`, `fish`, `midlength`, `longboard`.
    - For each entry, `buildOutline(preset.outline)` returns geometry with `points.length > 0` and does not throw.
    - Each entry's `length` is between `inchesToMm(60)` and `inchesToMm(120)` inclusive.
    - Each entry's `widePointWidth` is between `inchesToMm(16)` and `inchesToMm(25)` inclusive.
    - Each entry's `widePointOffset` is between `inchesToMm(-12)` and `inchesToMm(12)` inclusive.
    - Each entry's `noseAngle` is 35..90, `tailAngle` is 30..90, and `noseFullness`/`tailFullness`/`railLength` are 0..100.
    - A `squash`/`diamond`/`swallow` tail's `endWidth` is between `inchesToMm(0)` and `inchesToMm(16)`; a `swallow` `crotchDepth` is `inchesToMm(1)`..`inchesToMm(8)`; a `diamond` `depth` is `inchesToMm(1)`..`inchesToMm(5)`.
    - Every entry's `length`, `widePointWidth`, and `widePointOffset` round-trip `inchesToMm` then `mmToInches` to within 1e-9 inches.
    - The four ids are unique.
  </behavior>
  <action>
Build the thinnest complete path from a preset click on `/` to a preset curve rendered in the outline
editor. Six files, one per layer — this is a tracer, so breadth is deliberately minimal and depth is
production-quality. Ship no UI chrome here (no cards, thumbnails, hero, dialog, or continue affordance);
plan 02 expands the surface on top of this proven path.

**1. `lib/geometry/presets.ts` (new)** — implements D-01 (the core four roster) and D-02 (dims *and*
outline character, so a Fish starts looking like a fish). Pure data + types, no React, no I/O — same
tier as `board.ts`. Export `interface BoardPreset { id: "shortboard" | "fish" | "midlength" |
"longboard"; name: string; descriptor: string; outline: OutlineSpec }` and
`export const BOARD_PRESETS: readonly BoardPreset[]`. Import `OutlineSpec` as a type from `./board`
and `degrees`, `inchesToMm` from `./units`. Author every length/width/offset as `inchesToMm(<inches>)`
and every angle as `degrees(<deg>)` — never a bare number, never `mm()`.

Use these Claude-drafted starting values. Per D-03 and RESEARCH.md assumption A1 these are the
*starting point* the user tunes live in the editor during plan 04 — they are not final numbers, and
plan 04 replaces them wholesale. Every value below is inside the `OutlineControls` slider range for
its field:

  - `shortboard` / name "Shortboard" / descriptor "Fast and responsive, for performance surfing":
    length 74in, widePointWidth 18.75in, widePointOffset -1in, railLength 50, noseAngle 50deg,
    noseFullness 20, tailAngle 55deg, tailFullness 45, tail `{ kind: "squash", endWidth: inchesToMm(4) }`.
  - `fish` / "Fish" / "Wide and flat, for small-to-mid days":
    length 66in, widePointWidth 20.5in, widePointOffset 0in, railLength 60, noseAngle 70deg,
    noseFullness 55, tailAngle 35deg, tailFullness 15,
    tail `{ kind: "swallow", endWidth: inchesToMm(9), crotchDepth: inchesToMm(2.5) }`.
  - `midlength` / "Mid-length" / "Easy paddling with room to maneuver":
    length 84in, widePointWidth 21in, widePointOffset 0.5in, railLength 55, noseAngle 65deg,
    noseFullness 45, tailAngle 60deg, tailFullness 55, tail `{ kind: "round" }`.
  - `longboard` / "Longboard" / "Smooth glide, nose-to-tail control":
    length 108in, widePointWidth 23in, widePointOffset 1in, railLength 45, noseAngle 80deg,
    noseFullness 75, tailAngle 75deg, tailFullness 70, tail `{ kind: "squash", endWidth: inchesToMm(10) }`.

The descriptor strings are fixed copy from the UI-SPEC Copywriting Contract — use them verbatim.
Add a head comment stating that the numeric values are drafts pending the D-03 live-tuning pass.

**2. `components/design/design-store.tsx`** — add one action, `applyPreset: (preset: BoardPreset) =>
void`, implementing D-04's "clicking a preset drops the user into the editor with the preset applied".
Model it exactly on the sibling `updateOutline` action: a single `setState` call that replaces
`outline` wholesale with `preset.outline` (a preset is a complete spec, not a patch). Add it to the
`DesignContextValue` interface next to `updateOutline` and to the returned `value` object. Import
`BoardPreset` as a type from `@/lib/geometry/presets`. It must be a plain function called from an
event handler — the file's head comment forbids effects that write state, and that constraint holds.
Leave every other action, derived value, and memo untouched.

**3. `app/layout.tsx`** — promote the shared chrome so `/` and `/design/*` share one state instance
(RESEARCH.md Pattern 1). Import `SiteNav` from `@/components/site-nav` and the provider from
`@/components/design/design-store`. Inside `<body className="min-h-full flex flex-col">`, wrap
children as: provider > `<div className="flex min-h-0 flex-1 flex-col">` > `<SiteNav />` +
`{children}`. Carry that wrapper div class list across verbatim — per RESEARCH.md Pitfall 1 it is what
passes full-height sizing from the body down to the editors' `flex-1` panels, and dropping it
collapses them to content height. Keep `LayoutProps<"/">`, the Geist font variables, and the `html`
class list exactly as they are. While here, replace the scaffold metadata with
`title: "Shaper — Surfboard Design"` and a one-line description of the app.
<!-- planner-discipline-allow: DesignProvider -->

**4. `app/design/layout.tsx`** — reduce to `export default function DesignLayout(props:
LayoutProps<"/design">) { return <div className="flex min-h-0 flex-1 flex-col">{props.children}</div>; }`
with zero imports. Both the provider and the nav now live one level up; leaving either behind creates
a second context instance and `/design/*` silently stops reflecting presets applied from `/`. Rewrite
the head comment to explain the wrapper div's height-passthrough role without referring to state or nav.

**5. `app/page.tsx`** — implements D-05: `/` *is* the setup screen. Replace the whole file with a thin
Server Component exporting `metadata` (title "Shaper — Start a New Board") and returning
`<SetupScreen />`, matching how `app/design/outline/page.tsx` renders `OutlineEditor`. No forwarding
call and no import from `next/navigation` remain in this file.

**6. `components/setup/setup-screen.tsx` (new)** — the tracer's surface. `"use client"`, following the
`outline-editor.tsx` pattern: call `useDesign()` for `applyPreset`, and `useRouter()` from
`next/navigation` for navigation. Render an unadorned heading plus one `<button>` per `BOARD_PRESETS`
entry showing `name` and `descriptor`; `onClick` calls `applyPreset(preset)` then
`router.push("/design/outline")` — both inside the handler, no effect. Give the root a
`flex min-h-0 flex-1 flex-col` container so it inherits the height chain. Render all text as plain JSX
children; never inject raw HTML strings. Keep the markup deliberately plain — plan 02 replaces this
body with the UI-SPEC card layout, and styling it now would be thrown away.
  </action>
  <verify>
    <automated>npm run test && npm run lint && npm run build && test "$(grep -c 'DesignProvider' app/layout.tsx)" = "1" && test "$(grep -c 'DesignProvider' app/design/layout.tsx)" = "0" && test "$(grep -c 'redirect' app/page.tsx)" = "0" && test "$(grep -c 'flex min-h-0 flex-1 flex-col' app/layout.tsx)" = "1" && test "$(grep -c 'flex min-h-0 flex-1 flex-col' app/design/layout.tsx)" = "1" && test "$(grep -cE 'inchesToMm|degrees' lib/geometry/presets.ts)" -ge "4" && test "$(grep -c 'mm(' lib/geometry/presets.ts)" = "0" && test "$(grep -crE 'inchesToMm|mmToInches|MM_PER_INCH' components/setup/ | grep -v ':0$' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` passes with the pre-existing 562 tests still green (no regression from the store change).
    - `npm run build` completes with `/` listed as a prerendered route in the build output.
    - `grep -c 'DesignProvider' app/layout.tsx` returns exactly `1`; the same grep against `app/design/layout.tsx` returns `0`.
    - `app/design/layout.tsx` contains no `import` statement (`grep -c '^import' app/design/layout.tsx` returns `0`).
    - `grep -c 'redirect' app/page.tsx` returns `0`.
    - The literal `flex min-h-0 flex-1 flex-col` appears exactly once in each of `app/layout.tsx` and `app/design/layout.tsx`.
    - `lib/geometry/presets.ts` exports `BOARD_PRESETS` with 4 entries whose ids are exactly `shortboard`, `fish`, `midlength`, `longboard`.
    - No file under `components/setup/` performs a unit conversion — the final grep in `<verify>` (searching for `inchesToMm`, `mmToInches`, or `MM_PER_INCH` under `components/setup/`) matches zero files.
    - `grep -c 'mm(' lib/geometry/presets.ts` returns `0` — every value is authored through `inchesToMm()`/`degrees()`.
  </acceptance_criteria>
  <done>Visiting `/` in `npm run dev` shows four board-type buttons; clicking any one navigates to `/design/outline` with that preset's curve already drawn in the Template Viewer, and the sidebar sliders sit at that preset's values.</done>
  <reversibility rating="reversible">Promoting the store provider to the root layout is a two-file move with no data migration; `/` replacing the forward is a one-file revert (D-05 is itself recorded as reversible in CONTEXT.md).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Pin the preset roster with Vitest bounds and round-trip coverage</name>
  <files>lib/geometry/presets.test.ts</files>
  <read_first>
    - `lib/geometry/presets.ts` — the module under test as Task 1 wrote it
    - `lib/geometry/outline.test.ts` — the existing test-file convention (imports, `describe`/`it` shape, how `buildOutline` is exercised); presets need no golden fixture, so copy the structure and drop the fixture import
    - `lib/geometry/outline.ts` — the `buildOutline` signature and `OutlineGeometry` shape
    - `lib/geometry/units.ts` — `inchesToMm`, `mmToInches`, `MM_PER_INCH`
    - `components/outline/outline-controls.tsx` — the authoritative slider min/max per field; the bounds asserted here must be exactly these numbers
    - `vitest.config.ts` — confirms only `lib/**/*.test.ts` is collected
  </read_first>
  <action>
Add `lib/geometry/presets.test.ts` closing the Wave 0 gap named in RESEARCH.md's Validation
Architecture. This is the automated proof behind the SETUP-01/OUTL-01/UNIT-01 edge criteria in this
plan's `must_haves`, so each assertion below must exist as a real assertion, not a smoke test.

Write one `describe("BOARD_PRESETS")` block containing:

  - a roster test asserting `BOARD_PRESETS.length === 4` and that the set of `id` values equals
    `{"shortboard","fish","midlength","longboard"}` with no duplicates;
  - a `it.each`-style loop over every preset asserting `buildOutline(preset.outline)` does not throw
    and returns geometry whose `points` array is non-empty;
  - a bounds test per preset converting each stored value back with `mmToInches` and asserting the
    exact `OutlineControls` ranges: length 60..120, widePointWidth 16..25, widePointOffset -12..12,
    noseAngle 35..90, tailAngle 30..90, and railLength / noseFullness / tailFullness 0..100 (the last
    three are unitless, so assert them directly);
  - a tail-variant test that narrows on `preset.outline.tail.kind` and asserts `endWidth` 0..16in for
    `squash`/`diamond`/`swallow`, `crotchDepth` 1..8in for `swallow`, and `depth` 1..5in for `diamond`;
  - a units round-trip test asserting `mmToInches(inchesToMm(mmToInches(value)))` is within 1e-9 of
    `mmToInches(value)` for each preset's length, widePointWidth, and widePointOffset;
  - a non-empty-copy test asserting each preset's `name` and `descriptor` are non-empty strings.

Use `toBeGreaterThanOrEqual` / `toBeLessThanOrEqual` rather than snapshotting the drafted numbers —
plan 04 replaces every numeric value with the user's tuned values, and a snapshot would turn that
expected change into a false failure.
  </action>
  <verify>
    <automated>npx vitest run lib/geometry/presets.test.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/geometry/presets.test.ts` exits 0 with at least 6 passing tests.
    - `npm run test` reports a total test count strictly greater than the pre-existing 562.
    - The test file contains no `toMatchSnapshot` and no assertion pinning a specific drafted dimension to an exact value (`grep -c 'toMatchSnapshot' lib/geometry/presets.test.ts` returns `0`).
    - Temporarily widening any single preset value past its slider bound makes this file fail — the bounds assertions have teeth rather than passing vacuously.
  </acceptance_criteria>
  <done>`npm run test` is green and the preset module is covered by bounds, geometry-validity, and units round-trip assertions that fail if a preset is authored out of range.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the preset-to-editor path and the promoted layout</name>
  <what-built>
    `/` is now the setup screen (D-05) listing the four board-type presets (D-01). Clicking one applies
    its full outline character (D-02) and drops you straight into the outline editor (D-04). The board
    store and site nav were promoted to the root layout so `/` and `/design/*` share one live board.
    The screen is deliberately unstyled — plan 02 builds the real card UI on top of this path.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:3000

    1. **`/` loads the setup screen.** You land on a list of four board types (Shortboard, Fish,
       Mid-length, Longboard), NOT the outline editor. The dark SHAPER nav bar is visible above it,
       with no tab highlighted.
    2. **Presets are recognisably different.** Click "Fish" → the Template Viewer draws a short, wide,
       swallow-tail outline. Go back to `/` (browser back), click "Longboard" → a long, narrow-nosed
       outline with a squash tail. If a preset looks wrong for its board type, say so — the numbers
       are Claude-drafted placeholders you will tune yourself in a later plan, so "the shape is off"
       is expected feedback, not a bug.
    3. **State survives navigation (the critical check).** From `/design/outline`, drag the Board
       Length slider to a distinctly different value. Click the SHAPER nav tabs to RAILS, then
       VOLUME, then back to TEMPLATE. Your changed length must still be there. Then navigate to `/`
       (edit the URL to http://localhost:3000/) and back to TEMPLATE — the board must still be your
       edited one, not reset to a default.
    4. **Full-height layout is intact.** On `/design/outline`, the dark sidebar and the parchment
       viewer panel must both stretch to the bottom of the browser window. If either collapses to a
       short box around its content, the height chain broke — report it.
    5. **Nav at narrow width.** Squeeze the browser to roughly 375px wide (or use device toolbar,
       iPhone SE) while on `/`. Confirm the SHAPER wordmark and all five tabs stay on one line
       without wrapping or being cut off. Report exactly what you see — this is a known open item.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what you saw (which step, what was wrong)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> client state | Slider and preset input crosses into `design-store`; no server, database, or network boundary exists in this phase |
| repo -> build output | Source compiled by `next build`; anything not tree-shaken is publicly served |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `applyPreset` in `components/design/design-store.tsx` | low | mitigate | `applyPreset` accepts only a `BoardPreset` from the compile-time `BOARD_PRESETS` constant — no user-supplied or URL-derived value reaches it. Task 2's bounds tests prove no preset can write a value outside the editor's own clamp range, so `buildOutline` can never receive NaN/Infinity via this path. |
| T-01-02 | Tampering (XSS) | preset `name` / `descriptor` rendering in `components/setup/setup-screen.tsx` | low | mitigate | Preset copy is rendered as plain JSX children only; React escapes it. `components/setup/*` must contain no raw-HTML injection prop and no `document.write`/`window.open` — the same constraint `outline-viewer.tsx` records as threat T-QO-01. |
| T-01-03 | Information Disclosure | `app/layout.tsx` metadata | low | accept | Replacing the `create-next-app` scaffold title with a real product title exposes only the product name, which is already public in the GitHub repo. No further control needed at ASVS L1. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` records **zero new packages this phase** (empty table, no `[SLOP]`, no `[SUS]`). This plan runs no package-manager install command. If any task here would add a dependency, stop and re-run the legitimacy gate before installing. |
</threat_model>

<verification>
- `npm run test` green, total count above the pre-existing 562.
- `npm run lint` clean.
- `npm run build` succeeds and lists `/` as a prerendered route.
- One provider instance: a value edited on `/design/outline` survives a round trip through `/`.
- Both `/` and `/design/outline` fill the viewport height.
</verification>

<success_criteria>
A shaper opening the app picks one of four board types and is immediately shaping that board in the
outline editor, with the board persisting in-session across every design screen. The preset roster is
pure, tested data under `lib/geometry/`, and exactly one board-store provider serves the whole app.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and paths **created** by this plan (new — not pre-existing; exclude from drift verification):

| Artifact | Kind | Path |
|---|---|---|
| `BoardPreset` | exported TypeScript interface | `lib/geometry/presets.ts` |
| `BOARD_PRESETS` | exported `readonly BoardPreset[]` constant (4 entries) | `lib/geometry/presets.ts` |
| `"shortboard" \| "fish" \| "midlength" \| "longboard"` | `BoardPreset["id"]` string-literal union | `lib/geometry/presets.ts` |
| `applyPreset` | new action on `DesignContextValue` | `components/design/design-store.tsx` |
| `SetupScreen` | exported client React component | `components/setup/setup-screen.tsx` |
| `Home` | Server Component route export (replaces the forwarding version) | `app/page.tsx` |
| `lib/geometry/presets.ts` | new file | — |
| `lib/geometry/presets.test.ts` | new file | — |
| `components/setup/` | new directory | — |

Pre-existing symbols this plan only *modifies*: `RootLayout`, `DesignLayout`, `DesignProvider`,
`DesignContextValue`, `useDesign`, `SiteNav`.
</artifacts_this_phase_produces>

<planner_assumptions>
Surfaced, not silently dropped. Each is an unresolved edge-coverage row or a research open question
carried into execution.

1. **FIN-01 / FIN-02 / FIN-03 / VIZ-01 edge coverage is `unclassified` and therefore `unresolved`.**
   The deterministic edge probe could not classify these four requirements into a shape category, so
   no acceptance criterion was auto-derived for them. They are **not** covered by this plan's
   `must_haves`; plan 04 carries them as an explicit human-verified acceptance walkthrough against the
   existing `lib/geometry/fins.ts` golden tests and the fins screen. Flagged for review.
2. **All four presets' numeric values are Claude-drafted placeholders** (RESEARCH.md assumption A1,
   CONTEXT.md D-03). They are bounds-correct but not shaper-correct. Phase 1 is not complete until
   plan 04's live-tuning pass replaces them.
3. **No e2e framework exists.** The end-to-end click path is proven by the Task 3 human checkpoint,
   not by an automated test. Installing Playwright is explicitly out of scope this phase.
</planner_assumptions>

<output>
Create `.planning/phases/01-foundation-port-deploy-the-design-tool/01-01-SUMMARY.md` when done
</output>
