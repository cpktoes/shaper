---
phase: 02-accounts-saved-designs
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - app/design/actions.ts
  - lib/db/ownership.test.ts
  - components/setup/rack-card-menu.tsx
  - components/setup/rename-dialog.tsx
  - components/setup/delete-confirm-dialog.tsx
  - components/setup/board-rack-card.tsx
  - components/setup/board-rack.tsx
autonomous: true
requirements: [MODL-01, MODL-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every saved board's card carries a Rename, Duplicate and Delete menu (D-13)."
    - "Rename opens a dialog pre-filled with the current name; saving it changes the name on that board and nothing else."
    - "Duplicate is instant with no dialog: a new board named \"Copy of {name}\" appears in the rack, sorted to the top by its fresh last-touched stamp, and the original is untouched (D-13)."
    - "Delete is only reachable behind an are-you-sure that names the board being deleted; there is no trash and no undo — the confirm IS the safety (D-13)."
    - "A shaper can only rename, duplicate or delete their own boards: every one of these actions re-derives who is asking from the session and refuses to touch a row that is not theirs."
    - "The in-progress card carries no management menu — there is no row to rename, duplicate or delete."
    # --- Edge coverage lifted from the fallback report (MODL-01/MODL-03, resolved/explicit) ---
    - "An empty or whitespace-only board name is rejected inline ('Board needs a name.') before any Server Action runs; no row is created for a nameless save."
    - "Board names are stored verbatim as Unicode text with no normalization or length cap; the name is a label, not an identity key — two boards may share a name and identity is the row id."
    - "Two boards with identical names or identical last-touched timestamps remain distinct rack cards — row id is the identity; nothing merges or collides."
    # --- UI-SPEC ## UI Considerations lifted (resolved, explicit) ---
    - "name-rename-dialog / empty: submitting an empty or whitespace-only name is rejected inline with \"Board needs a name.\" before any Server Action runs."
    - "name-rename-dialog / loading: the primary button disables while the rename Server Action is in flight — no double-submit."
    - "name-rename-dialog / error: a failed rename keeps the dialog open with \"Couldn't save — check your connection and try again.\"; validation errors render inline under the field."
    - "name-rename-dialog / long-text: there is no character cap in the UI (the name column is unbounded text); long names are handled by rack-card truncation, not by truncating the input itself."
    - "delete-confirm-dialog / loading: the \"Delete Board\" button disables while the delete Server Action runs."
    - "delete-confirm-dialog / error: a failed delete keeps the dialog open with the inline text \"Couldn't delete — try again.\" rather than silently closing."
    - "delete-confirm-dialog / long-text: a long board name in the title wraps within the dialog's fixed width rather than widening or overflowing it."
    # --- UI-SPEC ## UI Considerations lifted (resolved, backstop — needs execution-time evidence) ---
    - statement: "rack-card-menu / error: with the duplicate action forced to fail, the rack is left unchanged and a visible, retryable error is surfaced — never a silent no-op."
      verification: backstop
  artifacts:
    - "app/design/actions.ts — renameModel, duplicateModel and deleteModel, each ownership-scoped"
    - "components/setup/rack-card-menu.tsx — the three-item menu, built on Base UI like the nav's settings menu"
    - "components/setup/rename-dialog.tsx and components/setup/delete-confirm-dialog.tsx"
  key_links:
    - "Every one of the three actions must re-derive the caller from the session and constrain its WHERE on both the row id and the owning-user column — this is the same seam saveModel already guards, and lib/db/ownership.test.ts must be extended to cover all four"
    - "Each action calls revalidatePath('/') after a successful write, or the rack shows the old name / the deleted board until the next hard reload"
    - "duplicateModel reads the source row inside the same ownership-scoped query it copies from — never from a snapshot the client hands back"
  prohibitions:
    - statement: "Deleting a board must never happen from a single click. There is no trash and no undo in this phase by explicit decision, which makes the confirm the only thing standing between a shaper and losing a shape they may have spent hours on."
      status: unverified
    - statement: "An action that fails must never look like one that worked. A duplicate that silently does nothing leaves a shaper believing they have a copy they do not have."
      status: unverified
---

<objective>
Board management on the rack: rename a board, branch a copy to riff on a shape, and delete one
behind a confirm that names it.

Purpose: D-09 made Save write over the board that was opened — which is only livable if there
is a deliberate way to branch a copy. D-13 gives each card the three things a shaper actually
needs, with the delete confirm standing in for a trash system this phase does not build.

Output: three more ownership-scoped Server Actions, the rack-card menu, and the rename and
delete dialogs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accounts-saved-designs/02-CONTEXT.md
@.planning/phases/02-accounts-saved-designs/02-UI-SPEC.md
@.planning/phases/02-accounts-saved-designs/02-PATTERNS.md
@.planning/phases/02-accounts-saved-designs/02-01-SUMMARY.md
@.planning/phases/02-accounts-saved-designs/02-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 2 (not pre-existing — do not flag these as drift):

**New in this plan:** `renameModel`, `duplicateModel`, `deleteModel`,
`components/setup/rack-card-menu.tsx`, `RackCardMenu`,
`components/setup/rename-dialog.tsx`, `RenameDialog`,
`components/setup/delete-confirm-dialog.tsx`, `DeleteConfirmDialog`

**Already created by earlier plans in this phase:** `proxy.ts`, `lib/db/client.ts`,
`lib/db/schema.ts`, `lib/db/queries.ts`, `listModels`, `lib/db/ownership.test.ts`,
`lib/geometry/design.ts`, `summarizeDesign`, `lib/models/design-snapshot.ts`, `parseSnapshot`,
`buildSnapshot`, `lib/models/autosave.ts`, `decideAutosave`, `lib/models/rack-order.ts`,
`sortRackEntries`, `app/design/actions.ts`, `saveModel`, `SignInDialog`, `NavAuthControl`,
`SaveButton`, `BoardRack`, `BoardRackCard`, `BoardNamePrompt`, `DesignState.modelId`,
`applyModel`, `components/ui/dialog.tsx`, `components/ui/input.tsx`

**Created by other plans in this phase:** `lib/models/banner-dismissal.ts`,
`components/auth/sign-in-banner.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renameModel, duplicateModel and deleteModel — each scoped to its owner</name>
  <files>app/design/actions.ts, lib/db/ownership.test.ts</files>
  <read_first>
    - app/design/actions.ts — `saveModel` in full; the three new actions copy its shape exactly
    - lib/db/schema.ts — the models table columns and the owning-user column's name
    - lib/db/ownership.test.ts — the source-contract assertions as 02-01 wrote them; this task widens their scope from one action to four
    - lib/models/design-snapshot.ts — parseSnapshot, for validating a duplicated snapshot on the way back in
    - .planning/phases/02-accounts-saved-designs/02-RESEARCH.md — Pattern 2 (the verbatim `renameModel` template) and Pitfall 3
    - .planning/phases/02-accounts-saved-designs/02-UI-SPEC.md — the Duplicate action copy row ("Copy of {name}")
    - node_modules/next/dist/docs/01-app/02-guides/server-actions.md — the ownership-check example pair
  </read_first>
  <behavior>
    Widen `lib/db/ownership.test.ts` so its existing source-contract assertions cover all four
    exported actions rather than just the save path. Written before the new actions exist, so
    they fail first:
    - Every exported async function in `app/design/actions.ts` awaits the session helper before
      any database call in its body.
    - No exported function signature in that file accepts a caller-supplied owner parameter.
    - Every Drizzle statement in that file that reads or writes the models table constrains on
      the owning-user column.
    - The file exports exactly the four expected action names and no others, so a fifth action
      added later cannot skip these checks unnoticed.
  </behavior>
  <action>
    Add three actions to `app/design/actions.ts`, each following `saveModel`'s established body
    shape without deviation:

    `renameModel(modelId, name)` — session first, refuse with "Sign in to rename a board." when
    absent; trim the name and refuse with "Board needs a name." when it is empty; then update
    that one row's name and updatedAt with a WHERE constraining on BOTH the row id AND the
    owning-user column. The name column is unbounded text and the value is stored verbatim: no
    normalization, no case folding, no length cap. A name is a label, not an identity — two of a
    shaper's boards are allowed to share one, and the row id remains the identity.

    `duplicateModel(modelId)` — session first; read the source row with an ownership-scoped
    select, so a shaper who supplies a row id that is not theirs reads nothing and the action
    refuses rather than copying someone else's board. Validate the source snapshot through
    `parseSnapshot` before writing it back. Insert a new row owned by the same session user,
    named "Copy of " plus the source name, carrying the same snapshot, with fresh created and
    updated stamps — which is what floats it to the top of the last-touched-first rack. The
    source row is not modified. Return the new row's id. The snapshot must come from the
    database read, never from anything the client passed in.

    `deleteModel(modelId)` — session first, then a single delete constraining on both the row id
    and the owning-user column. There is no soft-delete column and no trash table: D-13 decided
    the confirm is the safety, so this really does remove the row.

    Every one of the three calls `revalidatePath('/')` after a successful write. Without it the
    rack keeps showing the old name, or the board a shaper just deleted, until a hard reload.

    None of the three takes a user or owner parameter. The client sends a row reference and
    nothing else; who is asking is re-derived server-side, every time.
  </action>
  <verify>
    <automated>npm test -- lib/db/ownership.test.ts && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes with the widened `lib/db/ownership.test.ts` in the run
    - `npx tsc --noEmit` exits 0
    - `grep -nE 'export (async )?function [A-Za-z]+\([^)]*(userId|ownerId|clerkUserId)' app/design/actions.ts` returns no matches (exit status 1)
    - `grep -c "await auth()" app/design/actions.ts` returns at least 4
    - `grep -c "revalidatePath" app/design/actions.ts` returns at least 4
    - `grep -c "Copy of " app/design/actions.ts` returns at least 1
    - Signed in as one shaper and passing another shaper's row id to each of the three actions changes nothing in the database, verified by comparing row contents before and after
  </acceptance_criteria>
  <reversibility rating="reversible">Three additive Server Actions over an existing table; removing them leaves save, list and load exactly as they were.</reversibility>
  <done>A shaper can rename, copy and delete only their own boards, and the source-contract test fails loudly if any future action forgets to check who is asking.</done>
</task>

<task type="auto">
  <name>Task 2: The rack card's three-item menu</name>
  <files>components/setup/rack-card-menu.tsx, components/setup/board-rack-card.tsx</files>
  <read_first>
    - components/settings-menu.tsx — the whole file; this menu is built the same way, on the same primitives, with the same popup and row classes
    - components/setup/board-rack-card.tsx — as 02-03 left it, including the whole-card button and the in-progress variant
    - .planning/phases/02-accounts-saved-designs/02-UI-SPEC.md — the "Do NOT add a shadcn dropdown-menu" instruction, the rack-card-menu UI Considerations rows, the Visual Focal Points row naming Delete as the one item that draws the eye differently, and the Color section's note that menu-row hover is not an accent moment
    - .planning/phases/02-accounts-saved-designs/02-PATTERNS.md — the menu shell and row-hover class strings to copy
  </read_first>
  <action>
    Create `components/setup/rack-card-menu.tsx` exporting `RackCardMenu` with props for the
    board's id and name plus callbacks for the three actions. Build it directly on Base UI's
    `Menu` primitives, exactly as `components/settings-menu.tsx` builds the nav's gear menu —
    the same `Menu.Root` / `Menu.Trigger` / `Menu.Portal` / `Menu.Positioner` / `Menu.Popup`
    shell, the same `side="bottom" align="end" sideOffset={10}` positioning, the same popup
    classes (`min-w-64 origin-(--transform-origin) rounded-lg border border-surf-line-faint
    bg-surf-panel p-1.5 shadow-lg` with the open/closed animation classes), and the same row
    treatment `data-highlighted:bg-surf-well`. Do NOT generate a shadcn dropdown-menu: the app
    has one menu pattern and it stays the only one. Use `Menu.Item` rather than
    `Menu.RadioItem` — these are three commands, not a toggle group.

    Three items, in this order: Rename, Duplicate, Delete. Rename and Duplicate use the standard
    row treatment. Delete takes the destructive colour (`text-surf-warning-ink`) — it is the one
    item that should read differently, and the only place that colour appears on the rack. The
    trigger is an icon-only button (lucide `MoreVertical` or similar at `size-4`) and therefore
    needs an accessible name naming the board, so a screen-reader user knows which board's menu
    they opened.

    Mount the menu on `BoardRackCard`'s saved variant only. The in-progress card gets no menu —
    there is no row behind it to rename, copy or delete. Because the card itself is a whole-card
    `<button>`, the menu trigger cannot be nested inside it: nested interactive elements are
    invalid and the trigger's clicks would also fire the card's navigation. Restructure the card
    so the trigger sits as a sibling positioned over the card rather than inside the button, and
    confirm that clicking the trigger opens the menu without navigating, and that keyboard
    tabbing reaches both the card and the trigger separately.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run lint` is clean
    - `grep -c "@base-ui/react/menu" components/setup/rack-card-menu.tsx` returns at least 1
    - `test -f components/ui/dropdown-menu.tsx` fails — no shadcn dropdown-menu was generated
    - `grep -c "data-highlighted:bg-surf-well" components/setup/rack-card-menu.tsx` returns at least 1
    - `grep -c "surf-warning-ink" components/setup/rack-card-menu.tsx` returns at least 1
    - `grep -c "aria-label" components/setup/rack-card-menu.tsx` returns at least 1
    - In a browser: clicking the menu trigger on a rack card opens the menu and does NOT navigate to the editor; Tab reaches the card and the trigger as two separate stops
    - In a browser: the in-progress card shows no menu trigger
  </acceptance_criteria>
  <done>Each saved board's card carries a Rename / Duplicate / Delete menu that looks and behaves like the nav's settings menu, with Delete as the only item drawn in the warning colour.</done>
</task>

<task type="auto">
  <name>Task 3: Rename, Duplicate and Delete wired up, including when they fail</name>
  <files>components/setup/rename-dialog.tsx, components/setup/delete-confirm-dialog.tsx, components/setup/board-rack-card.tsx, components/setup/board-rack.tsx</files>
  <read_first>
    - components/setup/replace-board-dialog.tsx — the dialog shell, props shape (open / onOpenChange / onConfirm) and destructive-action treatment to mirror
    - components/setup/board-name-prompt.tsx — as 02-01 built it; the rename dialog is its sibling and should share its validation and in-flight behaviour rather than reinventing them
    - components/setup/board-rack.tsx and board-rack-card.tsx — as this plan's Task 2 left them
    - app/design/actions.ts — the three action signatures from Task 1
    - .planning/phases/02-accounts-saved-designs/02-UI-SPEC.md — the Rename dialog, Delete Board destructive-confirmation, Delete failure and Duplicate action copy rows, verbatim; and the name-rename-dialog / delete-confirm-dialog UI Considerations rows
  </read_first>
  <action>
    Create `components/setup/rename-dialog.tsx` exporting `RenameDialog` on the shadcn `Dialog`
    and `Input` primitives, styled identically to `replace-board-dialog.tsx`'s surfaces. Copy
    verbatim from the UI-SPEC: title "Rename board", the field pre-filled with the board's
    current name and auto-focused with the text selected, primary button "Save", and the
    validation message "Board needs a name." shown inline under the field when the value is
    empty or whitespace — blocking the submit before any server round trip. The primary button
    disables while the action is in flight so the dialog cannot double-submit. A failed rename
    keeps the dialog open with "Couldn't save — check your connection and try again." There is
    no character cap on what a shaper may type; long names are handled by the card's truncation,
    not by truncating the input.

    Create `components/setup/delete-confirm-dialog.tsx` exporting `DeleteConfirmDialog` on the
    existing `alert-dialog` primitives — this is a destructive confirm and
    `replace-board-dialog.tsx` is its exact structural sibling. Copy verbatim: the title asks to
    delete the board by name and says it cannot be undone, the cancel is "Cancel" and the
    destructive action is "Delete Board", styled with the destructive variant. The destructive
    button is the focal point; the cancel stays neutral. The "Delete Board" button disables
    while the action runs. A failed delete keeps the dialog open with the inline text
    "Couldn't delete — try again." rather than closing as if it had worked. A long board name in
    the title wraps inside the dialog's fixed width instead of widening it.

    Wire all three from `BoardRackCard`'s menu. Rename and Delete open their dialogs. Duplicate
    is instant with no dialog: it calls the action, and on success the new "Copy of …" card
    appears at the top of the rack on the revalidated render. Duplicate's failure needs its own
    affordance, because it has no dialog to keep open — surface a visible, retryable error on or
    beside the card (an inline error row on the card, or the menu re-opened with an error, both
    in the app's existing muted/warning idiom), never a silent no-op that leaves a shaper
    believing they have a copy they do not have. Pick the affordance during execution and verify
    it by forcing the action to fail.

    Keep the dialog open/close state local to the card or the rack as plain component state,
    following the convention `setup-screen.tsx` already uses for the replace confirm — this is a
    view concern and does not belong in the shared design store.

    One case needs care: deleting or renaming the board that is currently open in the editor.
    Renaming it should leave the shaper's open board and its `modelId` alone — only the row's
    label changed. Deleting it leaves the shaper holding a board whose row is gone; clear
    `modelId` so the next Save creates a fresh row rather than trying to write over a deleted
    one, and let the board itself stay in their hands. Handle both explicitly rather than
    leaving them to chance.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run lint` is clean
    - `grep -c "Delete Board" components/setup/delete-confirm-dialog.tsx` returns at least 1
    - `grep -c "Couldn&apos;t delete\|Couldn't delete" components/setup/delete-confirm-dialog.tsx` returns at least 1
    - `grep -c "Rename board" components/setup/rename-dialog.tsx` returns at least 1
    - `grep -c "Board needs a name" components/setup/rename-dialog.tsx` returns at least 1
    - `grep -nE '\.slice\(|\.substring\(|maxLength' components/setup/rename-dialog.tsx` returns no matches — no cap or truncation on what a shaper types
    - In a browser: renaming a board updates its card without a reload; submitting an empty name is refused inline and sends no request
    - In a browser: Duplicate produces a "Copy of …" card at the top of the rack with the original untouched, and forcing the action to fail leaves the rack unchanged with a visible, retryable error
    - In a browser: Delete is unreachable without the confirm, the confirm names the board, and a forced failure leaves the dialog open with the inline error
    - In a browser: deleting the board currently open in the editor leaves the design on screen and the next Save creates a new row rather than failing
  </acceptance_criteria>
  <reversibility rating="reversible">All view-layer wiring over actions that already exist; nothing here is written to the database that a later change would need to migrate.</reversibility>
  <done>A shaper can rename a board, branch a copy to riff on, and delete one behind a confirm that names it — and every one of those three tells them plainly when it did not work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Server Action | Three more mutations now accept a row id from the client, tripling the surface where a wrong WHERE clause would expose another shaper's board |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | rename / duplicate / delete input shapes | critical | mitigate | None of the three takes a user or owner parameter; identity is re-derived from the session inside each action, enforced by the widened signature grep and by `lib/db/ownership.test.ts` |
| T-02-03 | Elevation of Privilege | rename / delete WHERE clauses (IDOR) | critical | mitigate | Both constrain on the row id AND the owning-user column, so another shaper's id matches zero rows; verified by the cross-account manual check in Task 1 |
| T-02-12 | Information Disclosure | `duplicateModel`'s source read | critical | mitigate | The source row is read through an ownership-scoped select, so a foreign row id yields nothing to copy; the snapshot comes from that read and never from client input |
| T-02-13 | Repudiation | Irrecoverable delete with no audit trail | low | accept | D-13 decided there is no trash and no undo — the naming confirm is the deliberate safety. Accepted knowingly, recorded here so a later phase can revisit it rather than rediscover it |
| T-02-05 | Tampering | Duplicated snapshot | medium | mitigate | `parseSnapshot` validates the source snapshot before it is written into the new row |
</threat_model>

<verification>
- `npm test` green including the widened `lib/db/ownership.test.ts`
- `npx tsc --noEmit` exits 0, `npm run lint` clean
- `npm run build` from the main checkout
- Manual cross-account check: another shaper's row id changes nothing under any of the three actions
- Manual failure checks: forced failures on duplicate, rename and delete all surface visibly
</verification>

<success_criteria>
1. Every saved rack card has a Rename / Duplicate / Delete menu built on the app's one menu pattern
2. Rename changes only the label; an empty name is refused before any request is sent
3. Duplicate instantly produces a "Copy of …" board at the top of the rack, original untouched
4. Delete is unreachable without a confirm that names the board
5. All three refuse to touch a board that is not the signed-in shaper's
6. A failure in any of the three is visible and retryable — never a silent no-op
</success_criteria>

<output>
Create `.planning/phases/02-accounts-saved-designs/02-04-SUMMARY.md` when done
</output>
