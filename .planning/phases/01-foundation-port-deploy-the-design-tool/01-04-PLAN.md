---
phase: 01-foundation-port-deploy-the-design-tool
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - components/outline/outline-editor.tsx
  - lib/geometry/presets.ts
autonomous: false
requirements: [SETUP-01, OUTL-01, RAIL-01, FIN-01, FIN-02, FIN-03, VIZ-01, UNIT-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- D-03: the tuning loop closes ---
    - "The outline editor offers a development-only affordance that captures the live `OutlineSpec` as a ready-to-paste preset definition, so a preset can be tuned in the real editor and read back out without a persistence layer."
    - "The capture affordance is gated on `process.env.NODE_ENV === \"development\"` and is absent from the production build output — its label appears in no file under `.next/` after `npm run build`."
    - "Every value in `BOARD_PRESETS` is the shaper's own tuned value captured from the live outline editor, not a Claude-drafted placeholder."
    - "The tuned presets still satisfy every bound asserted by `lib/geometry/presets.test.ts` — tuning cannot land a preset outside its own slider's reach."
    # --- Phase acceptance: the five ROADMAP success criteria ---
    - "A user can set overall board dimensions (length, width, thickness) in inches and start a new design, entering via a board-type preset and adjusting with the editor's sliders."
    - "A user can shape an outline curve constrained to those dimensions and see it rendered in a 2D view."
    - "A user can select a fin configuration (single, thruster, quad, twin/2+1) and see the calculated fin placement (position, angle, toe) overlaid on the outline."
    - "A user can view calculated rail band dimensions (thickness/apex/tuck) at stations along the board."
    - "The app is live at a public Vercel URL with all measurements displayed in inches and litres."
    # --- Edge-coverage lift: RAIL-01 (adjacency, empty, ordering) ---
    - "RAIL-01/adjacency: rail bands are computed per named section (`nose`, `center`, `tail`) from a fixed-key `RailBandSpec`, never from a variable list of stations, so two stations cannot coincide, merge, or collide; sections holding equal values simply produce equal outputs, pinned by the `lib/geometry/rail-bands.test.ts` golden values extracted from the prototype."
    - "RAIL-01/empty: `computeRailBands()` is never called with an absent or partial spec — `DEFAULT_RAIL_BAND_SPEC` seeds the store's `rails` slice at mount and `applyPreset` writes only the `outline` slice, so no code path can empty the rail spec."
    - "RAIL-01/ordering: rail band output is a fixed-key record (`nose`/`center`/`tail`), not a sorted collection, so output order is structural and stable regardless of equal values — asserted by `npm run test -- rail-bands`."
  prohibitions:
    - statement: "MUST NOT present the Claude-drafted placeholder preset values to a shaper as though they were shaper-tuned. The product's core value is numbers a shaper trusts enough to cut foam to; shipping a roster of guessed curves under the same badge as the calculators spends that trust on something that has not earned it. Phase 1 is not complete until every preset carries a value the user tuned and approved."
      status: resolved
      verification: judgment
    - statement: "MUST NOT display a fallback, default, or approximated number in a calculated rail-band, fin-placement, or volume field without it being visibly distinguishable from a genuine calculated result. A silently substituted default in a field a shaper cuts to is worse than a visible error, because it looks exactly like an answer."
      status: resolved
      verification: judgment
  artifacts:
    - lib/geometry/presets.ts
    - components/outline/outline-editor.tsx
  key_links:
    - "`process.env.NODE_ENV` gating is the only thing keeping the capture affordance out of the public deployment — if the gate is written so the bundler cannot statically eliminate it, the button ships"
    - "`lib/geometry/presets.test.ts` bounds assertions are the guard rail on hand-pasted tuned values; they must run after the paste, not before"
    - "Pushing the tuned presets to `main` is also the first real confirmation that Vercel auto-deploy-on-push is wired"
---

<objective>
**As a** shaper, **I want to** tune each board-type preset's curves myself in the real editor,
**so that** a Fish actually starts looking like a fish and I trust the starting point as much as I
trust the calculators.

This plan closes CONTEXT.md D-03 — Claude drafts, the user tunes live, the tuned values are captured
back into `presets.ts` — and then walks the five ROADMAP success criteria against the live deployment
to seal Phase 1.

Purpose: preset quality is a trust matter, the same ethos as the rail-band and fin-placement
calculators. A guessed curve shipped under the product's badge spends credibility it has not earned.
Output: `BOARD_PRESETS` holding the shaper's own values, and a verified, deployed Phase 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-CONTEXT.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/SKELETON.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-01-SUMMARY.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-02-SUMMARY.md
@.planning/phases/01-foundation-port-deploy-the-design-tool/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Development-only "capture this outline as a preset" affordance</name>
  <files>components/outline/outline-editor.tsx</files>
  <read_first>
    - `components/outline/outline-editor.tsx` — the file being modified: its `useDesign()` destructuring, the `<aside>` sidebar that holds `OutlineControls`, and its head comment
    - `lib/geometry/units.ts` — `mmToInches` and the head comment forbidding conversions outside this module; the captured output must be expressed in the inch domain via this boundary
    - `lib/geometry/presets.ts` — the exact `BoardPreset.outline` literal shape the captured text must be pasteable into, including how tail variants are written
    - `lib/geometry/board.ts` — the `TailShape` discriminated union, so the captured text emits the right fields per tail kind
    - `components/ui/button.tsx` — the button primitive and its size variants, so the affordance matches the sidebar's existing visual language
  </read_first>
  <action>
Give the user a way to read the live outline back out of the editor as preset source, closing the D-03
loop without adding any persistence. CONTEXT.md explicitly accepts a development-only affordance here.

Add to `components/outline/outline-editor.tsx`, rendered inside the existing `<aside>` sidebar below
`<OutlineControls>`, a small button labelled `Copy preset values`. Render it only when
`process.env.NODE_ENV === "development"`. Write the guard as a direct, statically-analysable
comparison in the JSX condition — not via an intermediate variable, a helper function, or a runtime
config lookup — so the bundler substitutes the literal and dead-code-eliminates the whole block from
the production build. This gate is the only thing keeping the affordance off the public deployment.
<!-- planner-discipline-allow: Copy preset values -->

On click, build a text block from the live `outline` value already available from `useDesign()`,
shaped so it can be pasted directly into a `BOARD_PRESETS` entry's `outline` field:

  - express `length`, `widePointWidth`, and `widePointOffset` as `inchesToMm(<n>)` calls whose argument
    is `mmToInches(value)` rounded to 3 decimal places — route every conversion through
    `@/lib/geometry/units`, never a bare `/ 25.4`;
  - express `noseAngle` and `tailAngle` as `degrees(<n>)`;
  - emit `railLength`, `noseFullness`, and `tailFullness` as plain numbers, since they are unitless;
  - emit the `tail` object narrowed on `tail.kind`, with `endWidth`/`crotchDepth`/`depth` written as
    `inchesToMm(<n>)` where the variant carries them and omitted where it does not.

Write that text to the clipboard via `navigator.clipboard.writeText` inside the click handler, and also
`console.log` it as a fallback for when the clipboard API is unavailable or permission is denied —
wrap the clipboard call so a rejection does not throw past the handler. Give brief visual confirmation
that the copy happened (a transient label change is enough) using plain `useState`, following the
existing `showConstruction` convention in this file; add no effect that writes state.

Update the file's head comment to note the affordance exists, that it is development-only, and why
(it is how `lib/geometry/presets.ts` values get authored). Change nothing else in this file — the
editor's layout, the `OutlineControls` props, and the `OutlineViewer` call stay exactly as they are.
  </action>
  <verify>
    <automated>npm run test && npm run lint && npm run build && test "$(grep -c 'process.env.NODE_ENV' components/outline/outline-editor.tsx)" -ge "1" && test "$(grep -c 'mmToInches' components/outline/outline-editor.tsx)" -ge "1" && test "$(grep -cE '25\.4|/ *25' components/outline/outline-editor.tsx)" = "0" && test "$(grep -c 'useEffect' components/outline/outline-editor.tsx)" = "0" && test "$(grep -rl 'Copy preset values' .next/ 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and the affordance's label string appears in **no** file under `.next/` — `grep -rl 'Copy preset values' .next/` matches zero files, proving the production bundle drops it.
    - In `npm run dev`, the button is visible in the outline editor sidebar; clicking it places a pasteable text block on the clipboard and logs the same block to the browser console.
    - The pasted block drops into a `BOARD_PRESETS` entry's `outline` field and type-checks without edits — every length is an `inchesToMm(...)` call, every angle a `degrees(...)` call, and the tail object matches the `TailShape` variant for its `kind`.
    - `grep -c 'process.env.NODE_ENV' components/outline/outline-editor.tsx` returns at least `1`, and the comparison is written inline in the render condition (no indirection the bundler cannot fold).
    - `grep -cE '25\.4|/ *25' components/outline/outline-editor.tsx` returns `0` — no hand-rolled unit conversion; `mmToInches` is used instead.
    - `grep -c 'useEffect' components/outline/outline-editor.tsx` returns `0`.
    - `npm run test` still green — this task touches no `lib/` file.
  </acceptance_criteria>
  <done>A shaper can tune the outline sliders and click one button to get the exact source text for that curve, and that button does not exist in the deployed app.</done>
  <reversibility rating="reversible">A single self-contained, environment-gated block in one component; deleting it leaves the editor byte-identical to before.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Tune the four presets live, then walk the phase acceptance criteria</name>
  <precondition>The production URL from plan 03 is live and reachable, and `npm run dev` runs locally — this session uses both: the local dev server for the tuning work (the capture button exists only there) and the deployed URL for the acceptance walkthrough.</precondition>
  <what-built>
    The outline editor now has a development-only **Copy preset values** button in the sidebar. It
    captures whatever curve you have on screen as ready-to-paste preset source. This is the step
    where the four board-type presets stop being Claude's guesses and become your numbers.
  </what-built>
  <how-to-verify>
    This is a working session, not a checklist — budget some real time for part A.

    **Part A — tune the four presets** (local, `npm run dev`, http://localhost:3000)

    For each of Shortboard, Fish, Mid-length, and Longboard, in that order:

    1. Go to `/`, click that preset card. You land in the outline editor with the draft curve.
    2. Shape it until it looks like that board type should look — length, widepoint width and offset,
       rail length, nose angle and fullness, tail angle and fullness, and the tail shape itself. Use
       the Template Viewer and its dimension callouts; this is exactly the tool you would use for a
       real board.
    3. When you are happy with it, click **Copy preset values** at the bottom of the left sidebar.
    4. Paste the copied block back into this conversation, labelled with which preset it is —
       e.g. "FISH:" followed by the block.

    Four blocks, one per board type. If a preset's tail shape should change (say the longboard wants
    a round tail rather than a squash), just change it in the editor before copying — the captured
    block carries whichever variant you chose.

    **Part B — acceptance walkthrough** (on the live URL from plan 03, not localhost)

    Confirm each of the five things Phase 1 promised. Report any that fail.

    5. **Start a design with real dimensions.** Open the production URL. Pick a preset, then in the
       editor confirm you can set overall length and width in inches with the sliders and see the
       values update in feet-and-inches / fractional inches.
    6. **Shape and see an outline.** Drag nose angle and tail fullness; the 2D outline in the
       Template Viewer must redraw live and stay inside the length and width you set.
    7. **Fins.** Go to FINS. Switch the configuration between single, thruster, quad, and twin/2+1.
       For each, confirm you get calculated placement numbers (position from tail, angle, toe) and
       that the fin marks are drawn on the board outline.
    8. **Rail bands.** Go to RAILS. Confirm you see calculated rail band dimensions — thickness, apex,
       tuck — at stations along the board, and that they change when you change the rail inputs.
    9. **Units, everywhere.** Across TEMPLATE, RAILS, VOLUME, FINS, and SUMMARY, confirm every
       dimension reads in inches (or feet-and-inches) and volume reads in litres. Flag anything
       showing a raw millimetre number, an unlabelled number, or a unit that looks wrong.
    10. **Honest numbers.** If any calculated field ever shows a placeholder, a dash, a zero, or an
        obviously default value that looks like a real answer, flag it — a number a shaper might cut
        to must never be a silent fallback.

    Also confirm the **Copy preset values** button is NOT present on the production URL's editor
    sidebar (it should exist only on localhost).
  </how-to-verify>
  <resume-signal>Paste the four captured preset blocks (labelled SHORTBOARD / FISH / MIDLENGTH / LONGBOARD), then type "approved" for part B or describe which numbered step failed and how</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the tuned presets, re-verify bounds, and ship</name>
  <files>lib/geometry/presets.ts</files>
  <read_first>
    - `lib/geometry/presets.ts` — the current file with the drafted values, and the head comment noting they are drafts pending tuning
    - `lib/geometry/presets.test.ts` — the bounds, geometry-validity, and round-trip assertions the tuned values must still satisfy
    - `components/outline/outline-controls.tsx` — the authoritative slider ranges, in case a captured value sits at a bound and needs checking rather than assuming
    - `.planning/phases/01-foundation-port-deploy-the-design-tool/01-03-SUMMARY.md` — the production URL, for the post-deploy check
  </read_first>
  <action>
Replace the drafted preset values with the shaper's captured ones and confirm nothing regressed.

**1. Paste in the tuned values.** For each of the four entries in `BOARD_PRESETS`, replace the
`outline` object wholesale with the block the user supplied for that preset in Task 2. Keep each
entry's `id`, `name`, and `descriptor` exactly as they are — only `outline` changes. If a captured
block carries a different `tail.kind` than the draft did, take the captured variant including its
variant-specific fields, and drop any field the new variant does not define.

**2. Update the head comment.** It currently states the values are drafts pending the D-03 tuning
pass. Rewrite it to record that these are the shaper's tuned values, captured from the live outline
editor on the date of the tuning session, and that any future change should go through the same
capture loop rather than being hand-edited.

**3. Re-run the guard rails.** `npx vitest run lib/geometry/presets.test.ts` must pass with the new
values. If a bounds assertion fails, the captured value sits outside the slider range that produced it
— treat that as a real signal worth reporting rather than widening the assertion, and check the
capture output against `outline-controls.tsx` before changing anything. Then `npm run test`,
`npm run lint`, and `npm run build`.

**4. Ship it.** Commit and push to `main`. This push is also the first real confirmation that plan
03's Vercel Git integration auto-deploys: after the push, wait for the deployment and re-fetch the
production URL, asserting it returns HTTP 200 and still serves all four preset names. Record in the
summary that auto-deploy-on-push was confirmed (or that it was not, if it needed a manual trigger).

**5. Confirm the deployed build is clean.** Fetch the production HTML for `/design/outline` and
confirm the development-only capture affordance's label does not appear in it.
  </action>
  <verify>
    <automated>npx vitest run lib/geometry/presets.test.ts && npm run test && npm run lint && npm run build && test "$(grep -c 'mm(' lib/geometry/presets.ts)" = "0" && test "$(grep -cE 'inchesToMm|degrees' lib/geometry/presets.ts)" -ge "4" && test -n "$PROD_URL" && curl -fsS -o /tmp/shaper-prod.html -w '%{http_code}\n' "$PROD_URL" | grep -q '^200$' && grep -q 'Shortboard' /tmp/shaper-prod.html && grep -q 'Longboard' /tmp/shaper-prod.html && curl -fsS "$PROD_URL/design/outline" | grep -qv 'Copy preset values'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/geometry/presets.test.ts` passes with the tuned values — every bound, geometry-validity, and round-trip assertion still holds.
    - `npm run test`, `npm run lint`, and `npm run build` all exit 0.
    - `grep -c 'mm(' lib/geometry/presets.ts` returns `0` — tuned values are still authored through `inchesToMm()`/`degrees()`, not pasted as raw millimetres.
    - The four `id` / `name` / `descriptor` triples are unchanged from plan 01; only `outline` objects differ.
    - The file's head comment records that the values are the shaper's tuned values and names the capture loop as the way to change them.
    - After the push, the production URL returns HTTP 200 and its body contains `Shortboard` and `Longboard`.
    - The production `/design/outline` HTML does not contain the development-only capture affordance's label.
    - The SUMMARY records whether the push auto-deployed without manual intervention.
  </acceptance_criteria>
  <done>`BOARD_PRESETS` carries the shaper's own numbers, the bounds tests still pass, and the tuned presets are live on the production URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| development build → production build | The capture affordance must exist on one side of this line and not the other; the boundary is enforced by bundler dead-code elimination, not by a runtime check |
| clipboard | The affordance writes application state into the OS clipboard, outside the browser's sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | development-only capture affordance shipped in the production Vercel build | medium | mitigate | Gate on a direct, inline `process.env.NODE_ENV === "development"` comparison so the bundler statically folds it and eliminates the block. Proven, not assumed: Task 1 greps the entire `.next/` output for the affordance's label and requires zero matches, and Task 3 re-checks the deployed HTML. An indirection the bundler cannot fold (a variable, a helper, a config lookup) would ship the code even though the button never renders — which is why the guard's *form* is specified, not just its presence. |
| T-01-12 | Information Disclosure | `navigator.clipboard.writeText` writing board geometry to the OS clipboard | low | accept | The captured content is the user's own board dimensions on their own machine, written only in response to their explicit click, in a development build. No third-party data and no secret material is involved. |
| T-01-13 | Tampering | hand-pasted tuned values entering `BOARD_PRESETS` | medium | mitigate | Pasted source is the one path where a value bypasses the editor's `clampFinite` guards. `lib/geometry/presets.test.ts` is the compensating control and must run *after* the paste; Task 3 forbids widening a failing bounds assertion to make a paste fit, since that failure is the signal the control exists to raise. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` records zero new packages this phase (empty table, no `[SLOP]`, no `[SUS]`). This plan runs no install command. If any task here would add a dependency, stop and re-run the legitimacy gate before installing. |
</threat_model>

<verification>
- `npx vitest run lib/geometry/presets.test.ts` green against the tuned values.
- `npm run test`, `npm run lint`, `npm run build` green.
- The capture affordance's label appears in no file under `.next/` and not in the deployed HTML.
- Production URL returns 200 and serves the tuned preset roster.
- All five ROADMAP success criteria confirmed by the Task 2 walkthrough on the live URL.
</verification>

<success_criteria>
Every board-type preset carries a curve the shaper tuned and approved, captured through a
development-only loop that does not exist in the deployed app; the five Phase 1 success criteria are
confirmed against the live Vercel URL; and the tuned presets are shipped.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and paths **created** by this plan (new — not pre-existing; exclude from drift verification):

| Artifact | Kind | Path |
|---|---|---|
| development-only preset-capture affordance | environment-gated JSX block + click handler + local `useState` | `components/outline/outline-editor.tsx` |

This plan creates no new files and no new exported symbols. `BOARD_PRESETS` and its four entries are
pre-existing from plan 01 — this plan replaces their `outline` values only.

Pre-existing symbols this plan only *modifies*: `OutlineEditor`, `BOARD_PRESETS`.
</artifacts_this_phase_produces>

<planner_assumptions>
Surfaced, not silently dropped.

1. **FIN-01, FIN-02, FIN-03, and VIZ-01 edge coverage is `unclassified` and therefore `unresolved`.**
   The deterministic edge probe could not classify these four requirements into a shape category, so
   no acceptance criterion was auto-derived for them. They are covered here by the Task 2 acceptance
   walkthrough (steps 7 and 6/9) against the existing `lib/geometry/fins.ts` golden tests and the
   shipped viewers — a human check, not a derived predicate. Flagged for review; a verifier that
   cannot confirm them from explicit evidence should route to human review rather than pass.
2. **`process.env.NODE_ENV` gating has no precedent in this codebase** (RESEARCH.md assumption A3).
   It is a standard Next.js idiom and Task 1 proves the elimination empirically by grepping `.next/`,
   rather than trusting the idiom. If the user would rather the affordance be removed entirely before
   the phase seals, that is a one-block deletion.
3. **The tuning session's output quality is the user's call, not a derivable criterion.** "A Fish
   starts looking like a fish" cannot be asserted by a test; the bounds tests only prove the values
   are reachable, not that they are good. This is why the prohibition against shipping untuned
   presets is `judgment`-tier.
4. **Task 2 combines a creative session (tuning) with an acceptance checklist.** They are grouped
   because both need the app open in a browser, but part A may take considerably longer than part B.
</planner_assumptions>

<output>
Create `.planning/phases/01-foundation-port-deploy-the-design-tool/01-04-SUMMARY.md` when done
</output>
