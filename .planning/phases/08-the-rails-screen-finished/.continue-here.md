---
phase: 08-the-rails-screen-finished
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-09-08T08:29:08Z
---

<current_state>
Plans 08-01 through 08-05 are merged to main, built and tested (41 files, 2208 tests green at b9f8d3d).
The phase code review (08-REVIEW.md) found 2 warnings; both are fixed (88adf18, a34b026) and recorded in 08-REVIEW-FIX.md.
Plan 08-06 ran Tasks 1 and 2 (the PRNT-06 proofs, below) and stopped at Task 3, a blocking checkpoint:human-action:
the shaper must push main, wait for the Vercel deployment to be Ready, confirm the live site, THEN run npm run db:migrate:prod, then confirm the tick-box survives a reload.
08-06-SUMMARY.md has NOT been written yet (deliberately: the continuation agent writes it after the resume signal).
The 08-06 executor worktree had zero commits and was auto-removed by the harness; nothing was merged for wave 5.
Main is exactly what the shaper should push.
</current_state>

<completed_work>
- Waves 1-4: plans 08-01..08-05 executed in worktrees, merged, build+test gates green after every wave, tracking committed.
- REQUIREMENTS.md: RAIL-02..RAIL-06 and PRNT-05 marked complete; PRNT-06 left pending for 08-06.
- Code review + fixes committed (095d63f, 88adf18, a34b026, b9f8d3d).
- 08-06 Task 1 (three jsPDF surfaces byte-identical to pre-phase): done, evidence below.
- 08-06 Task 2 (unticked order form unchanged): scripted route blocked, Phase 7 human audit fallback recorded, evidence below.
</completed_work>

<remaining_work>
- 08-06 Task 3: the shaper's push -> Vercel Ready -> live-site check -> npm run db:migrate:prod -> reload-and-tick check. Resume signal: "deployed and migrated".
- After the resume signal: spawn a gsd-executor continuation agent SEQUENTIALLY on main (no worktree) to write and commit 08-06-SUMMARY.md from the evidence below (plus the shaper's own report of steps 2-6), then roadmap.update-plan-progress 08 08-06 complete, commit tracking.
- Then the execute-phase tail: aggregate_results (no SECURITY.md yet; security_enforcement is on, so /gsd-secure-phase 8 is due before advancing), code_review_gate (REVIEW.md already exists: status has findings but both fixed), verify_phase_goal (gsd-verifier, sonnet) -> expected human_needed -> 08-UAT.md -> /gsd-verify-work 8.
- Delete this file once resumed.
</remaining_work>

<decisions_made>
- Code review ran between the last code wave (08-05) and 08-06, per this repo's practice, so the push carries the fixes. WR-01: actual-size.css print rules are now guarded by :has([data-view-full-sized-dialog]) so a direct print of the rails screen prints the screen again. WR-02: the printed sheet's caption now calls exampleRailThickness(false) instead of a duplicated 3.5 literal. IN-01 skipped (pre-existing).
- 08-06 Task 2 fell back to the Phase 7 D-10 human audit: the pre-phase scratch worktree cannot serve any route because Clerk throws "Missing publishableKey" without .env.local, and .env* files are hard-blocked for agents. The two audit items are human-checks for end-of-phase UAT.
- The 08-06 executor was told not to write SUMMARY.md at the checkpoint; its evidence is preserved here instead.
</decisions_made>

<blockers>
- Task 3 is a human action (push to main triggers a production deploy; the production migration is irreversible in ordering). Waiting on the shaper. If the shaper asks the orchestrator to run steps 2 and 5, keep exactly the same order and gate each on their say-so (Phase 5 precedent, 05-07).
</blockers>

<context>
## Evidence for 08-06-SUMMARY.md (from the 08-06 executor, 2026-09-08)

### Task 1 — Overview Sheet, Full Sized Template, Paper Saver byte-identity
- Baseline SHA: 3210103522a3a5e59148401f57904a23fae93026. Confirmed: the three commits between it and the first Phase 8 code commit 9610ee8 (50188dd, 7082fdd, 71f17db) touch only .planning/ (git diff --stat 3210103 9610ee8^ -- . ':!.planning' is empty).
- Read-only guarantees: git diff --quiet 3210103 -- components/template/ (exit 0); git diff --stat 3210103 -- lib/geometry/rail-bands.ts lib/models/design-snapshot.ts components/summary/use-print-fit.ts app/globals.css (empty); git diff --quiet 3210103 -- package.json package-lock.json (exit 0); npm test 41 files / 2208 passed / 2 skipped / 0 failed.
- Method: detached scratch worktree at the baseline under /private/tmp/gsd-08-06-prnt/scratch with node_modules symlinked to the main checkout's install (the agent worktree's own node_modules is only a .vite cache; Node resolves the main install by walking up). No npm install anywhere. Throwaway probe components/zz-probe-pdf.test.ts built BOARD_PRESETS[0] through buildTemplatePdf, buildStripPdf and buildOverviewPdf for imperial/metric x letter/a4, run with vitest and deleted in the same shell command in both trees; git status --porcelain empty in both afterwards. In the scratch tree node_modules/.bin/vitest was used because npx resolved a stale global cache (minor quirk, not a failure).
- Page counts, identical in both trees per combination: Full Sized Template 16 pages x 4 combos = 64; Paper Saver 11 x 4 = 44; Overview Sheet 1 x 4 = 4; total 112 pages.
- Differing operators: 0. Verified two ways with pypdf 6.16.1: (1) tokenized operator-sequence comparison, 0 differing tokens across all 112 pages; (2) raw decompressed content-stream byte comparison (page.get_contents().get_data()), 0 pages differ by a single byte across all three surfaces, both systems, both papers (stronger than the plan's operator-level bar).
- Cleanup: git worktree remove --force + git worktree prune; git worktree list shows only main and the untouched determined-tereshkova-590f1b; git status --porcelain empty; npm test re-run clean. The comparison scripts and output PDFs remain under /private/tmp/gsd-08-06-prnt (outside the repo) if anyone wants to inspect them.

### Task 2 — unticked order form unchanged
- Method actually used: the Phase 7 D-10 human print-preview audit fallback. The scripted print-to-PDF diff was abandoned before the print-capture step.
- Specific failure mode: (1) next dev under Turbopack in the scratch worktree failed with TurbopackInternalError "Symlink [project]/node_modules is invalid, it points out of the filesystem root"; worked around with next dev --webpack on port 3010. (2) With webpack every route, including /design/summary, returned HTTP 500 because the scratch tree has no .env.local and the Clerk SDK throws "@clerk/nextjs: Missing publishableKey" before any application code runs. Agents are hard-blocked from reading, copying or creating any .env* file, and the main checkout (which has .env.local) is on the post-phase commit, so there is no way to render the pre-phase baseline. This is a structural block, not the Pitfall 4 hydration-timing race, and was hit well inside the 30-minute budget without needing headless Chrome.
- Mechanically verified instead: npx vitest run lib/units-isolation.test.ts 25/25; npm test 41/2208/2/0; git diff --stat 3210103 -- components/summary/use-print-fit.ts app/globals.css empty; package.json and package-lock.json unchanged (no Puppeteer/Playwright).
- Human checks deferred to end-of-phase UAT: (a) with the box unticked, open the summary print preview in Imperial on Letter and A4 and in Metric on Letter and A4 and confirm pages 1 and 2 are exactly as before this phase (same layout, values, "of 2" page marks, page-count note, nothing clipped); (b) tick the box and confirm the preview gains a third page and the marks read "of 3" with pages 1 and 2 otherwise unchanged.
- Servers: a pre-existing next dev on port 3005 (PID 59680, another session, main checkout at b9f8d3d) was only curled, never touched; the executor's own webpack server on 3010 was killed and confirmed gone. No .env* file was read, copied or created.
</context>

<next_action>
Start with: ask the shaper for the Task 3 resume signal ("deployed and migrated", or what went wrong). Then spawn the 08-06 continuation agent sequentially on main to write 08-06-SUMMARY.md from the evidence above, delete this file, and run the execute-phase tail (aggregate, secure-phase reminder, verify, UAT).
</next_action>
