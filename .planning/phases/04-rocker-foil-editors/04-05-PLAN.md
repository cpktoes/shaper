---
phase: 04-rocker-foil-editors
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-04"]
files_modified:
  - lib/geometry/presets.ts
  - lib/geometry/presets.test.ts
  - components/rocker/rocker-editor.tsx
  - components/rocker/rocker-viewer.tsx
  - components/summary/order-form.tsx
autonomous: true
requirements: [ROCK-01, FOIL-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Starting a board from any of the four presets gives it a side profile with that board type's own character: a Fish is flatter and thicker, a Longboard carries more nose lift, a Shortboard more rocker overall (D-12)"
    - "A shaper can tune a preset's rocker and foil in the live editor and copy the values back out, the same development-only capture loop the outline presets were tuned through in Phase 1 (D-12)"
    - "The Summary order form's rocker box draws the board's real rocker curve with its real nose and tail lift values, replacing the placeholder ticks it was built with (D-04)"
    - "No screen other than ROCKER and the Summary's rocker box gains a side profile — the prototype's Rails-page side profile is not restored (D-04)"
    - "The Summary's rocker box always has a complete rocker to draw, because a reopened pre-phase board backfills its defaults before the Summary renders (UI: summary-rocker-box empty)"
    - "The rocker box renders synchronously from the loaded design — no async state (UI: summary-rocker-box loading)"
    - "The drawn curve is monotone by construction, so no invalid curve can reach the order form (UI: summary-rocker-box error)"
    - "The rocker box always draws from a complete five-station spec — a partially defined rocker cannot exist at render time (UI: summary-rocker-box partial)"
    - "The rocker box adds no new text beyond the drawn curve and its two lift values; the surrounding form labels are pre-existing fixed strings (UI: summary-rocker-box long-text)"
    - statement: "The real curve fits the order form's fixed-size rocker box at any extreme station combination — a very high nose lift with a very low tail lift on the shortest and the longest board — using the same fit-to-viewBox technique the side-profile viewer uses, never a fixed-scale drawing that could clip"
      verification: backstop
  artifacts:
    - path: "lib/geometry/presets.ts"
      provides: "Each of the four board presets carries its own rocker and foil"
      exports: ["BoardPreset", "BOARD_PRESETS"]
    - path: "components/summary/order-form.tsx"
      provides: "The reserved rocker box drawing the board's real curve"
  key_links:
    - from: "lib/geometry/presets.ts"
      to: "lib/geometry/rocker.ts"
      via: "each preset's rocker is a RockerSpec authored through inchesToMm"
      pattern: "RockerSpec"
    - from: "components/summary/order-form.tsx"
      to: "components/rocker/rocker-viewer.tsx"
      via: "the rocker box renders the same side-profile drawing in its compact, calloutless mode"
      pattern: "RockerViewer"
    - from: "components/rocker/rocker-editor.tsx"
      to: "lib/geometry/presets.ts"
      via: "the development-only capture affordance emits pasteable BoardPreset rocker/foil source"
      pattern: "inchesToMm"
---

<objective>
Give every board type its own side profile, and put the real rocker curve into the space the Summary
order form has been holding open for it.

Purpose: a shaper who picks "Fish" should get a board that already looks like a fish from the side,
not a generic curve — the same thing Phase 1 delivered for outlines. And the order form's rocker box
was built as a placeholder with a comment saying it waits for the rocker screen; the rocker screen
now exists, so the placeholder goes.

Output: rocker and foil on all four `BOARD_PRESETS` with invariant tests; the development-only
capture affordance on the ROCKER screen so the founder can tune them live; and the Summary order
form's rocker box drawing the board's own curve.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rocker-foil-editors/04-CONTEXT.md
@.planning/phases/04-rocker-foil-editors/04-UI-SPEC.md
@.planning/sketches/MANIFEST.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Every board type gets its own side profile</name>
  <files>lib/geometry/presets.ts, lib/geometry/presets.test.ts, components/rocker/rocker-editor.tsx</files>
  <read_first>
    - `lib/geometry/presets.ts` (whole file) — the header's D-03 tuning-status convention (which presets carry the shaper's own captured values and which are Claude-drafted and reviewed), the `BoardPreset` interface, and the rule that every length is authored through `inchesToMm()` and every angle through `degrees()`, never a bare number.
    - `lib/geometry/presets.test.ts` (whole file) — the existing invariant style: bounds-correctness against the controls' own ranges rather than pinned magic numbers.
    - `components/outline/outline-editor.tsx` lines 26-100 and the capture handler around lines 128-140 — `roundedInches`, `buildPresetSource` and `handleCopyPreset`, the exact development-only capture affordance to mirror, including the `process.env.NODE_ENV === "development"` gate that lets the bundler remove it from production.
    - `components/rocker/rocker-editor.tsx` (as extended in plan 04-02) — the sidebar and where a flex-none footer holding the capture button belongs, per quick task 260823-ux2 which pinned that footer across all three editors.
    - `lib/geometry/rocker.ts` and `lib/geometry/foil.ts` — `ROCKER_LIFT_RANGE_IN` and `FOIL_THICKNESS_RANGE_IN`, which every preset value must sit inside.
    - `components/design/design-store.tsx` — `applyPreset`, which already reads `preset.rocker` and `preset.foil` and replaces the board wholesale.
    - `.planning/phases/04-rocker-foil-editors/04-CONTEXT.md` D-12.
  </read_first>
  <behavior>
    `lib/geometry/presets.test.ts` new cases, one per preset unless stated:
    - Every preset carries a `rocker` with all four lift keys and a `foil` with all five thickness keys, each a finite number.
    - Every rocker lift sits inside `ROCKER_LIFT_RANGE_IN` and every foil thickness inside `FOIL_THICKNESS_RANGE_IN`, so a preset can never load a value its own slider cannot reach.
    - Every preset's nose-tip lift exceeds its tail-tip lift — every surfboard carries more nose rocker than tail rocker.
    - Every preset's nose-12in lift exceeds its tail-12in lift, and both are below their own tip's lift.
    - On every preset the centre is the thickest station, and each tip is thinner than the 12in station beside it — the board tapers toward both ends.
    - The four presets differ from one another: no two presets share an identical rocker object and no two share an identical foil object. This is D-12's actual requirement, stated mechanically.
    - The Fish's centre thickness divided by its length exceeds the Shortboard's — a fish is proportionally thicker.
    - The Longboard's nose-tip lift exceeds the Shortboard's — a longboard carries more nose lift.
    - `sampleRocker` and `sampleFoil` run over every preset at its own length without producing a fold-back or a negative thickness.
  </behavior>
  <action>
    Extend `BoardPreset` in `lib/geometry/presets.ts` with `rocker: RockerSpec` and `foil: FoilSpec`, and
    give each of the four presets its own values. Author every number through `inchesToMm()`, never a bare
    millimetre. Use these drafted values, in inches:

    | Preset | rocker noseTip | rocker nose12 | rocker tail12 | rocker tailTip | foil noseTip | foil nose12 | foil center | foil tail12 | foil tailTip |
    |---|---|---|---|---|---|---|---|---|---|
    | shortboard | 4.75 | 1.4 | 0.45 | 2.1 | 0.3125 | 1.2 | 2.4 | 1.45 | 0.25 |
    | fish | 3.5 | 1.0 | 0.3 | 1.4 | 0.375 | 1.5 | 2.75 | 1.75 | 0.3125 |
    | midlength | 4.5 | 1.25 | 0.4 | 1.75 | 0.375 | 1.7 | 2.9 | 1.85 | 0.3125 |
    | longboard | 5.5 | 1.5 | 0.35 | 1.6 | 0.5 | 2.1 | 3.25 | 2.0 | 0.4375 |

    Update the file header's tuning-status paragraph to record honestly what these are: Claude-drafted
    starting values carrying each board type's recognised side-profile character — the Fish flatter and
    proportionally thicker, the Longboard with the most nose lift, the Shortboard with the most rocker
    overall — awaiting the founder's review in the live editor, exactly the state the outline presets were
    in before Phase 1's capture session tuned two of them. Say plainly that any future change should come
    back through the capture loop rather than being hand-edited, the same instruction the file already
    carries for outlines, rails and fins.

    Add the development-only capture affordance to `components/rocker/rocker-editor.tsx`, mirroring the
    Template screen's: a `buildRockerPresetSource(rocker, foil)` helper producing a pasteable
    `rocker: { ... }, foil: { ... }` block with every value emitted as `inchesToMm(<3-decimal inches>)`
    through the same `roundedInches` rounding, a `handleCopyPreset` that logs it and writes it to the
    clipboard with the clipboard rejection swallowed as a silent no-op (the log already carries the text),
    and a "Copy preset values" button pinned in the sidebar's flex-none footer. Gate the whole thing on
    `process.env.NODE_ENV === "development"` so the bundler removes it from production, and style it for a
    dark sidebar the way quick task 260821-dmg restyled the Template screen's.
  </action>
  <verify>
    <automated>npm test -- lib/geometry/presets.test.ts</automated>
    <automated>npm test &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
    <human-check>From the home screen start a new board from each of the four presets in turn and open /design/rocker. Confirm each one reads as its own board from the side: the Fish flat and thick, the Longboard with a lifted nose, the Shortboard with the most curve overall. Note anything that looks wrong so the values can be tuned with the capture button.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/geometry/presets.test.ts` exits 0 with every case under Behavior green.
    - `npm test`, `npm run lint` and `npm run build` all exit 0.
    - `lib/geometry/presets.ts` contains `rocker:` and `foil:` on all four presets: `grep -c 'rocker: {' lib/geometry/presets.ts` reports 4 and `grep -c 'foil: {' lib/geometry/presets.ts` reports 4.
    - Every preset value crosses the units boundary: `grep -c 'inchesToMm(' lib/geometry/presets.ts` reports at least 36 more than before this task (nine stations across four presets).
    - `components/rocker/rocker-editor.tsx` contains `process.env.NODE_ENV === "development"` and `buildRockerPresetSource`.
    - The capture affordance cannot reach production: `grep -c 'Copy preset values' components/rocker/rocker-editor.tsx` reports at least 1 and that string appears only inside the development-gated block.
    - Behavior: applying each of the four presets loads a distinct side profile, and the development-only button copies a pasteable source block for the values currently on screen.
  </acceptance_criteria>
  <done>Each board type starts with its own rocker and foil, the differences are pinned by invariant tests rather than by eye, and the founder can tune any of them live and paste the result back.</done>
</task>

<task type="auto">
  <name>Task 2: The Summary's rocker box gets the real curve</name>
  <files>components/summary/order-form.tsx, components/rocker/rocker-viewer.tsx</files>
  <read_first>
    - `components/summary/order-form.tsx` lines 15-30 (the header note saying the rocker box stays a placeholder until the rocker screen exists), lines 125-150 (`OrderFormTick` and `RockerTicks`, the placeholder to replace), and lines 300-360 (the right-hand column, the `caption="Rocker"` panel with the `order-form-rocker` class, and the two `RockerTicks` call sites inside it).
    - `components/summary/order-form-primitives.tsx` — the panel and tick primitives the box is built from.
    - `components/rocker/rocker-viewer.tsx` (as written and extended in plans 04-01 and 04-02) — its props and its fixed-frame sizing.
    - `components/outline/outline-viewer.tsx` — the `hideCallouts` and `fixedFrame` props and the comment explaining why the drawing must never take its height from its own contents on this sheet, and why the viewer's `<svg>` is absolutely positioned inside a `relative` box.
    - `app/design/summary/order-form.css` — the `order-form-rocker` class and the sheet's own scale variables.
    - `components/summary/use-print-fit.ts` — how the two sheets are pinned for print; the rocker box must not change either sheet's height.
    - `.planning/sketches/MANIFEST.md` — the drafting grammar the drawn curve must follow.
  </read_first>
  <action>
    Replace the order form's placeholder rocker chrome with the board's real curve.

    Give `RockerViewer` a compact mode: when `hideCallouts` is set it draws the two curves and nothing
    else — no output rail, no chips, no station lines, no drag targets — sized by the same fixed frame so
    an extreme board (the highest nose lift on the shortest board, and again on the longest) still fits its
    box without clipping. The `<svg>` is absolutely positioned to fill its container so the box never takes
    its height from the drawing, the rule the outline viewer's own comment records as what broke the print
    sheet once already.

    In `components/summary/order-form.tsx`, read `rocker`, `foil` and `outline` from the store, render
    `<RockerViewer hideCallouts />` inside the existing `caption="Rocker"` panel in place of the drawn
    space between the two `RockerTicks` calls, and replace `RockerTicks` itself with a component that
    prints the board's real lift values instead of the three fixed placeholder words: nose lift on the nose
    side of the box and tail lift on the tail side, each through `formatInchesFraction`, in the same
    `OrderFormTick` treatment and the same two flanking columns the muse draws. Keep the panel's caption,
    class name, size and position exactly as they are — this replaces the box's contents, not the box.
    Update the file header's note from "stays a placeholder until the rocker screen exists" to a record of
    what it now draws.

    Add no side profile to any other panel or screen. D-04 is explicit: the prototype's Rails-page side
    profile is not restored.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
    <automated>! grep -n 'High.*Medium.*Low' components/summary/order-form.tsx</automated>
    <human-check>Open /design/summary. The rocker box shows the board's own side profile with its real nose and tail lift values either side. Print-preview both sheets: still exactly two pages, nothing clipped, the rocker box unchanged in size and position. Repeat with the shortest board at maximum nose lift and with a 9-foot longboard. Check Daylight and Slate, and print preview from a dark theme.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npm run lint` and `npm run build` all exit 0.
    - `components/summary/order-form.tsx` contains `RockerViewer` and `formatInchesFraction` applied to the rocker lift values.
    - `grep -n 'High.*Medium.*Low' components/summary/order-form.tsx` returns nothing — the placeholder ticks are gone.
    - `components/summary/order-form.tsx` still contains `caption="Rocker"` and the `order-form-rocker` class, unchanged.
    - No other screen gains a side profile: `grep -rln 'RockerViewer' components/ app/` lists only `components/rocker/rocker-editor.tsx`, `components/rocker/rocker-viewer.tsx` and `components/summary/order-form.tsx`.
    - `components/rocker/rocker-viewer.tsx` accepts `hideCallouts` and, when set, renders no output rail and no drag targets.
    - `grep -rEn '#[0-9a-fA-F]{6}' components/summary/order-form.tsx` returns nothing.
    - Behavior: the Summary shows the board's real rocker curve with real lift values, the sheet still prints as exactly two pages, and nothing clips at the board-length or rocker-lift extremes.
  </acceptance_criteria>
  <done>The order form's reserved rocker box holds the board's own curve and its own numbers. The placeholder is gone, the sheet still prints on two pages, and no other screen grew a side profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| development-only code → production bundle | The capture affordance reads live design values and must not ship. |
| design state → printed order form | The rocker curve crosses onto a sheet a shaper takes to the shop floor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Information Disclosure | `components/rocker/rocker-editor.tsx` | low | mitigate | The capture affordance is gated on the development environment check so the bundler removes it, and `next.config.ts` already disables Turbopack server source maps — the Phase 1 fix for exactly this leak, where the dev-only label text survived in a `.js.map` even though the compiled output had dropped it. Task 1's grep gate confirms the string lives only inside the gated block. |
| T-04-15 | Tampering | `components/summary/order-form.tsx` | low | accept | The order form renders only the shaper's own design values as React-escaped text and SVG attributes computed from numbers; there is no string-built markup and no new external input. |
| T-04-16 | Denial of Service | `components/rocker/rocker-viewer.tsx` compact mode | low | accept | The compact drawing samples a fixed number of stations regardless of input and cannot iterate unboundedly; an extreme spec changes the drawn shape, not the work done. |
| T-04-SC | Tampering | package-manager installs | low | accept | No packages installed by this plan. |

ASVS level 1. No applicable category changes: this plan adds no authentication, session, access-control or cryptography surface, and its only input is design state already validated at the snapshot boundary.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols:
- `buildRockerPresetSource` (`components/rocker/rocker-editor.tsx`, development-only helper)
- A real lift-value tick component in `components/summary/order-form.tsx`, replacing `RockerTicks`

New fields on existing symbols:
- `BoardPreset.rocker`, `BoardPreset.foil`, populated on all four entries of `BOARD_PRESETS`
- `RockerViewer` gains its compact `hideCallouts` mode

Removed:
- `RockerTicks` and its fixed High/Medium/Low placeholder chrome in `components/summary/order-form.tsx`
</artifacts_this_phase_produces>

<verification>
- `npm test && npm run lint && npm run build` all green.
- Each preset loads a visibly distinct side profile, with the differences pinned by invariant tests.
- The development-only capture button emits pasteable preset source and is absent from a production build.
- The Summary's rocker box draws the board's real curve and real lift values; the sheet still prints as two pages with nothing clipped at the extremes.
- Verified in Daylight and Slate themes, plus a print preview from a dark theme.
</verification>

<success_criteria>
- All four presets carry their own rocker and foil, and a Fish reads as a fish from the side.
- The tune-live-and-capture-back loop exists for rocker and foil, gated out of production.
- The Summary order form's reserved rocker box holds the real curve, and no other screen gained one.
</success_criteria>

<output>
Create `.planning/phases/04-rocker-foil-editors/04-05-SUMMARY.md` when done.
</output>
