---
phase: 10-the-whole-app-on-a-phone
task: 2
total_tasks: 3
status: blocked
last_updated: 2026-09-12T01:15:00Z
---

<current_state>
Plan 10-08 ("the second real-device sweep, re-issued") is at its blocking checkpoint again. The
sweep sheet (`10-SWEEP-2.md`) has just been re-issued a second time, against a build that has been
fixed twice since the shaper last held a phone for it. Task 2 — the shaper walking the whole trip
on a real iPhone and a real Android phone — has not happened against this build yet. Task 3
(transcribe the answers, write `10-08-SUMMARY.md`) cannot start until those answers come back.

**This supersedes everything this file said before.** The previous park here described the FIRST
walk of `10-SWEEP-2.md` (2026-09-11, one phone, three findings, deliberately stopped). That walk is
done and its findings are fixed. This park describes the SECOND walk — a full restart from step 1,
on both phones, against the build those fixes produced (and one round of code review then
corrected).
</current_state>

<completed_work>
- The sheet was rewritten in full for this second walk: `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md`.
  - Green-build header now points at the **live production site**
    (`https://www.shaperassistant.com`), commit `7b5ece3` — no dev server, no Wi-Fi requirement.
  - `npx tsc --noEmit` and `npx vitest run` were re-run directly in this worktree and are green
    (2463 passed, 2 skipped, 0 failed) — matching the orchestrator's cited numbers exactly.
    `npm run build` and the full Playwright suite (225 passed, 213 skipped, 0 failed) were run by
    the orchestrator on `main` at this commit, since Turbopack can't resolve `next` inside this
    worktree; those numbers are cited, not re-run here.
  - Every answer cell in both the ten-step table (12 rows) and the "Eight things worth watching for
    by name" table is empty again, on both the iPhone and Android columns — verified with a `grep`
    count matching the row count exactly.
  - The two predictions the sheet got wrong last time (tabs stay at the bottom sideways; the Hide
    Toolbar tip is about orientation) are replaced with what's actually true now (desktop look
    sideways — tabs move to the top, controls beside the board; the tip is about which phone, not
    which way it's held), stated plainly as reversals, side by side with what the shaper actually
    said on their first walk.
  - New rows added: the RAILS-controls-sideways question (they now sit beside the INSTRUCTIONS
    reading sideways, on purpose, per D-10 — nobody has judged this in the hand); the "is 220 dots
    of board enough" question; and the Hide Toolbar tip is now two separate rows, one per phone,
    each with its own right answer, so which phone was walked never blocks scoring it again.
  - The three items the first walk never reached — step 8 (save two boards, check rack order),
    the avatar's five-tap count, and the entire second phone — are flagged and carried forward,
    impossible to skip.
  - The dated "First walk — what came back (2026-09-11)" section at the bottom is preserved
    byte-for-byte, with one new paragraph added directly under its own heading noting it was
    superseded by this re-issue and that none of its answers were carried into the tables above.
- `.continue-here.md` (this file) rewritten for this park, replacing the description of the first
  walk's stop with a description of this one.
</completed_work>

<remaining_work>
- Task 2: the shaper opens **https://www.shaperassistant.com** directly on a real iPhone and a real
  Android phone (no dev server, no same-Wi-Fi step) and walks all ten steps, from step 1, on BOTH
  phones — including the rows they already answered on their last walk, because the fixes since
  then change what a phone does sideways on every screen. Every one of the twelve ten-step rows and
  all eight named questions need PASS, FAIL, or N/A with a reason, on both phones. No blank cells.
- Task 3: once the shaper's answers come back, transcribe them verbatim into `10-SWEEP-2.md` (a
  FAIL is quoted, never paraphrased; a blank row is recorded as "still unwalked," never inferred),
  add a dated "Results — sweep run [date]" section beneath the tables, and write
  `.planning/phases/10-the-whole-app-on-a-phone/10-08-SUMMARY.md` mapping PHON-07, PHON-08,
  PHON-09 and PHON-10 to the specific rows that evidence each — marking none of them satisfied,
  since that is the orchestrator's job after the verifier reads the SUMMARY.
</remaining_work>

<decisions_made>
- No new decisions were taken in this park — this is a re-issue of the sheet under the orchestrator's
  own rulings, not a place where the executor made a judgment call. The two ruling clarifications
  worth remembering for whoever transcribes the answers:
  - "220 dots enough?" and "should the RAILS controls get out of the way sideways too?" are both
    explicitly framed as cheap-to-change, real questions for the shaper — not settled facts the
    walk merely confirms.
  - The Hide Toolbar tip is scored as two independent rows (iPhone / Android), each with its own
    right answer, specifically so which phone the walk happened on never again blocks filing the
    sheet, the way it did after the first walk.
</decisions_made>

<blockers>
- **This can only be unblocked by a human with two real phones — a real iPhone and a real Android
  phone.** No CLI, API, or browser automation stands in for a real thumb, a real signed-in Clerk
  session, or a real phone turned sideways. Nothing here can be auto-approved. Unlike the first
  walk, there is no Wi-Fi or dev-server dependency this time — the app is live at
  `https://www.shaperassistant.com`.
</blockers>

<context>
This is the second walk of the second sweep sheet. The very first sweep (`10-SWEEP.md`, plan
10-04) found three real defects and never finished its ten-step table. Plans 10-05/06/07 fixed
those three defects. This sheet (`10-SWEEP-2.md`, plan 10-08) was written to re-walk everything
from step 1 against that fixed build; its first walk (one phone, 2026-09-11) found three MORE real
things — the board picture collapsing to zero height sideways, the RAILS controls sitting under all
three tabs, and the Hide Toolbar tip still not appearing — and was deliberately stopped rather than
re-finding the same three things on a second phone.

Plans 10-09, 10-10 and 10-11 fixed those three things. A code review of that work
(`10-REVIEW-3.md`) then found one more real gap — 10-11's RAILS fix never reached a phone turned
sideways, because D-10 (landing one plan earlier in the same round) had already moved that
orientation into the desktop shell where the fix's gate can't fire — and that gap has also now been
fixed (commit `12e2fe8`), on purpose: sideways now deliberately keeps the RAILS controls beside the
reading, matching what a desktop has always shown there, with the question of whether that is
actually right in the hand left for this very sweep to answer.

Nothing in this phase changes application code from here. If this second walk finds anything new,
it becomes its own follow-up plan — it does not get patched inside this one.
</context>

<hand_over_evidence>
The exact numbers to give the shaper alongside the sheet, matching what's recorded at the top of
`10-SWEEP-2.md`:

| Check | Result |
|---|---|
| Where to open it | `https://www.shaperassistant.com` — live production, no dev server |
| Commit | `7b5ece3` on `main` — deployed and confirmed Ready |
| `npx tsc --noEmit` | Green — no errors (re-run in this worktree) |
| `npx vitest run` | Green — 2463 passed, 2 skipped, 0 failed (re-run in this worktree) |
| `npm run build` | Green, clean, exit 0 (run by the orchestrator on `main` at this commit) |
| `npm run test:e2e` (full suite, all three projects) | Green — 225 passed, 213 skipped, 0 failed (run by the orchestrator on `main` at this commit) |
| Desktop screenshot baselines | Unchanged — nothing moved for a mouse |

Anything the phones find from here is a real, new finding — not something the tests would have
caught first.
</hand_over_evidence>

<what_to_ask_the_shaper>
Hand the shaper `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` directly and ask them
to walk it exactly as written, from step 1, on both phones. In your own words to them:

- **Devices needed:** a real iPhone and a real Android phone. Nothing else — no Wi-Fi requirement
  this time.
- **Where to open it:** `https://www.shaperassistant.com`, directly, on each phone.
- **This restarts from step 1 on both phones, including what you already answered last time** —
  the fixes since your last walk change what a phone does sideways on every screen, so none of your
  earlier answers still stand, passes and fails alike.
- **What's new since your last walk**, so you know what you're re-testing:
  - A phone on its side is a normal browser again — your own call, now built: sideways gets the
    same layout a desktop gets (controls beside the board, tabs across the top, no bottom bar).
    This is the OPPOSITE of what the sheet told you to expect last time.
  - A board's picture can no longer disappear — it now has a 220-dot floor under it. On a very
    short sideways screen that can mean a small scroll to see one whole card.
  - The RAILS instructions page drops its sliders when you're upright (fixing what you found last
    time), but — new, and worth a real opinion — keeps them when you're sideways, because sideways
    now counts as the desktop layout.
  - The Hide Toolbar tip no longer cares which way you're holding the phone, only which phone it
    is: it should appear on an iPhone both ways, and never on an Android either way.
- **The avatar question is still a count, not an impression, and still never answered:** tap just
  outside the rim of the round account picture, five times, and write down how many of the five
  opened the account menu.
- **Two new judgment calls this round, both cheap to change:** is the RAILS-sideways behaviour
  (controls beside the reading) fine, or should it get out of the way like upright does? And is
  220 dots of board picture actually enough to tell a fish from a longboard?
- **Never write down anything typed into a sign-in form** — no email, no password, no verification
  code, no screenshot of a filled-in form. Only whether it worked.
- **No blank cells.** Every one of the twelve ten-step rows and all eight named questions need
  PASS, FAIL, or N/A with a reason, on both phones.
</what_to_ask_the_shaper>

<next_action>
Start with: wait for the shaper's filled-in answers (pasted inline, or "sweep complete" with the
file handed back). Do not attempt any step yourself, and do not fill a single cell on the shaper's
behalf.

Once the answers arrive, the continuation agent's job is exactly this:

1. Open `.planning/phases/10-the-whole-app-on-a-phone/10-SWEEP-2.md` and transcribe the shaper's
   answers into the cells they belong in — the ten-step table (all twelve rows, including 5a/5b/5c)
   and the eight-question table at the bottom. Use the shaper's own words verbatim for every FAIL;
   never paraphrase, never round up, never infer a blank cell from a neighboring answer or from
   what the automated tests say. A blank row is recorded as "still unwalked."
2. Add a dated "Results — sweep run [date]" section beneath the two tables (mirroring
   `10-SWEEP.md`'s own "Results — sweep run 2026-09-11" section as the house style), stating
   plainly whether the sheet came back complete, and listing each finding as its own item in the
   shaper's own words so it can be planned as its own fix.
3. Write `.planning/phases/10-the-whole-app-on-a-phone/10-08-SUMMARY.md` from the filled sheet.
   Name each of PHON-07, PHON-08, PHON-09 and PHON-10 and say exactly which rows evidence it and
   whether that evidence is complete. Mark **no** requirement as satisfied — that is the
   orchestrator's job after the verifier reads the SUMMARY — and do not claim PHON-10 is met by
   any automated test.
4. In the **same commit** that adds the filled-in `10-SWEEP-2.md` and creates `10-08-SUMMARY.md`,
   run `git rm .planning/phases/10-the-whole-app-on-a-phone/.continue-here.md` — this file is not
   permanent storage and gets deleted once the checkpoint resolves.
5. Do not touch `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, or `.planning/ROADMAP.md` —
   all three are the orchestrator's own writes.
</next_action>
