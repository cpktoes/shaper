---
phase: quick-260826-j97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/setup/preset-card.tsx
  - components/setup/continue-board-card.tsx
autonomous: false
requirements: [QUICK-260826-j97]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every card on the landing page has a visible drawn edge around it when nothing is hovered or focused — the same kind of structural line the tabbed panel's outer edge carries on every design screen."
    - "The preset cards and the Continue Current Board card get that edge together and look identical in weight, as their own docstrings promise."
    - "Hovering a card and tabbing to a card still paint the accent edge exactly as they do today — the change is only to the resting state."
    - "Nothing moves: card size, the grid, spacing, radius, type and the thumbnail well are all unchanged."
    - "The page stays white, the card stays sand, the well stays white — this task adds a line and changes no fill anywhere."
  artifacts:
    - components/setup/preset-card.tsx
    - components/setup/continue-board-card.tsx
  key_links:
    - "components/viewer/tabbed-panel.tsx line 97 is the source of truth: the panel's OUTER edge is a plain `border border-surf-line` with no ring. The landing card's resting edge must be the same token, applied the same way."
    - "The `border` slot on the card button already exists (currently transparent) and hover/focus already override it, so swapping the resting value is the whole change — no new utility, no new token, no restructure."
    - "`--surf-line`, not `--surf-line-faint`: this is a structural boundary saying where the card is. `line-faint` is correct only for the thumbnail well inside it (done in 260826-ist). Getting this backwards, in either direction, is the known trap in this codebase."
    - "Tailwind ring width is set by a ring-width utility, so removing the resting `ring-1` leaves the ring unpainted at rest while the hover/focus `ring-2` rules still set their own width. Task 2 measures `boxShadow` in the browser to prove that rather than trusting it."
---

<objective>
Give the landing page's cards a resting structural line, so each card reads as a drawn
object the way the tabbed panel does on every design screen.

Purpose: the founder sent a cropped screenshot of a design screen's panel corner — sand
behind, two nested rounded corners each carrying a 1px khaki line — and said "like this".
Asked whether the landing card should copy the edges or also the fills, they chose:
**just add the outer line.** The sand card gets its own khaki line, so the landing page
shows the same two nested lines as the crop. Since 260826-ist the inner line already
exists (the thumbnail well); only the outer one is missing.

Output: a real resting edge on both landing-page card components, plus the comment updates
that keep a later editor from "cleaning it up".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@components/setup/preset-card.tsx
@components/setup/continue-board-card.tsx
@components/viewer/tabbed-panel.tsx
</context>

<boundaries>
Locked by the founder. Do not negotiate with these:

- Do NOT invert the page and card fills. The page stays `--surf-ground`, the card stays
  `--surf-canvas`, the thumbnail well stays `--surf-panel`.
- Do NOT revert or amend quick task 260826-icz or 260826-ist.
- Do NOT touch the card radius (`rounded-xl` stays — radius parity with the panel was not
  asked for), spacing, padding, type, or anything on the four `/design` screens.
- Do NOT add a token to `app/globals.css`, and do not edit that file at all. `--surf-line`
  already exists and is already bridged into Tailwind.
- Do NOT refactor the duplicated className string into a shared constant. Two files, two
  identical edits, on purpose.
- Do NOT reinstate `lib/design/palette.test.ts` or `lib/design/contrast.ts`. They were
  deleted by decision in 260824-i05. Contrast is computed by hand in Task 2.
</boundaries>

<tasks>

<task type="tracer">
  <name>Task 1: Swap the resting edge on both landing-page cards</name>
  <files>components/setup/preset-card.tsx, components/setup/continue-board-card.tsx</files>
  <read_first>
    `components/viewer/tabbed-panel.tsx` — the outer panel div (line 97) and the docstring
    paragraph explaining why that edge is `--surf-line` and not `--surf-line-faint`. That
    paragraph is the justification you are extending to the landing card; do not restate it
    at length, cite it.
  </read_first>
  <action><!-- planner-discipline-allow: border-transparent, ring-1, ring-foreground/10 -->
    Both card components carry a byte-identical className string on their whole-card
    `<button>`. Make the same edit to both, in both files, in this one task — their
    docstrings state that `ContinueBoardCard` deliberately carries the same visual weight as
    `PresetCard` and they sit in the same grid, so changing one alone is a defect.

    Two changes inside that one string, nothing else:

    1. The resting border value moves from the transparent placeholder to `border-surf-line`.
       Keep the bare `border` keyword and keep the token utility adjacent to it, so the
       string reads `border border-surf-line`. The border slot already exists at 1px, so
       `box-sizing: border-box` means the card's outer size does not change and nothing on
       the page moves.
    2. Delete the resting ring pair — the 1px ring tinted at 10% of the foreground. Its two
       measured values are roughly 1.1–1.3:1 against the card fill in every theme, i.e. a
       ring that is present in the DOM and invisible on screen; with a real line now painted
       one pixel inside it, keeping it would put two edges within a pixel of each other and
       read muddy. The reference treatment settles it: the tabbed panel's outer edge is a
       plain border with no ring at all.

    Leave every hover and focus-visible utility exactly as it is. Those rules set their own
    ring width and their own border colour, so both interactive states are unaffected by
    either change. Do not add `ring-0` or any replacement — absence is the correct resting
    state.

    Then bring the commentary back into line. Match each file's existing density and voice —
    `preset-card.tsx` has a docstring plus short inline notes; `continue-board-card.tsx` has
    a single short docstring and no inline comments, so give it one added sentence and no
    more.

    - `preset-card.tsx` docstring: the sentence describing the card's own "border/ring"
      living on the button is now inaccurate. Reword it to talk about the card's own edge,
      keeping the point it was making (the edge lives on the button, so hover and focus
      apply directly with no parent/child style coupling).
    - `preset-card.tsx` inline note above the `return`: it currently explains only the
      thumbnail well. Extend it to describe both nested lines and which token each one uses
      and why — the card's own outer edge is structural (`--surf-line`, says where the card
      is, matching the tabbed panel's outer edge) and the well's inner edge recedes
      (`--surf-line-faint`, a grouping hint, matching the panel's inner content card). Keep
      the existing citation of `components/viewer/tabbed-panel.tsx`. Add one clause recording
      that the resting ring was removed deliberately so a later editor does not restore it.
    - `continue-board-card.tsx` docstring: extend the existing "same visual weight as
      PresetCard" sentence to say that the card's resting edge is part of that parity and has
      to be kept in step across both files.

    Write both comments for a shaper-facing codebase: plain, concrete, no jargon about
    re-renders. Do not name specific measured contrast numbers in the comments — Task 2
    produces those and they belong in the SUMMARY, not baked into source where they go stale.
  </action>
  <verify>
    <automated>
cd /Users/kontoes/Code/shaper
CARDS="components/setup/preset-card.tsx components/setup/continue-board-card.tsx"
# Gates are scoped to the card className line only, so comment prose cannot flip them.
CLS=$(grep -h 'flex w-full flex-col gap-2 rounded-xl' $CARDS)
echo "$CLS" | grep -c 'border border-surf-line' | grep -qx 2 && echo "PASS structural line on both cards" || echo "FAIL structural line"
echo "$CLS" | grep -cE 'border-transparent|ring-1|ring-foreground' | grep -qx 0 && echo "PASS resting ring gone" || echo "FAIL resting ring still present"
echo "$CLS" | grep -c 'hover:ring-2 hover:ring-surf-accent-ink' | grep -qx 2 && echo "PASS hover ring intact" || echo "FAIL hover ring"
echo "$CLS" | grep -c 'focus-visible:ring-2 focus-visible:ring-surf-accent-ink' | grep -qx 2 && echo "PASS focus ring intact" || echo "FAIL focus ring"
echo "$CLS" | grep -c 'bg-surf-canvas' | grep -qx 2 && echo "PASS card fill untouched" || echo "FAIL card fill changed"
echo "$CLS" | grep -c 'rounded-xl' | grep -qx 2 && echo "PASS card radius untouched" || echo "FAIL radius changed"
# The two className strings must still be identical to each other.
[ "$(echo "$CLS" | sed 's/^[[:space:]]*//' | sort -u | wc -l | tr -d ' ')" = "1" ] && echo "PASS both cards identical" || echo "FAIL cards drifted apart"
# line-faint stays on the thumbnail well and nowhere else in these two files.
grep -c 'border-surf-line-faint' components/setup/preset-card.tsx | grep -qx 1 && echo "PASS well edge untouched" || echo "FAIL well edge changed"
# Blast radius: exactly these two files, and globals.css untouched.
[ "$(git diff --name-only | sort | tr '\n' ' ')" = "components/setup/continue-board-card.tsx components/setup/preset-card.tsx " ] && echo "PASS blast radius" || echo "FAIL extra files touched: $(git diff --name-only | tr '\n' ' ')"
# Commentary is part of the deliverable.
grep -q 'tabbed-panel' components/setup/preset-card.tsx && echo "PASS citation kept" || echo "FAIL citation lost"
npm test 2>&1 | tail -5
npm run lint 2>&1 | tail -5
    </automated>
  </verify>
  <done>All eleven gates print PASS, `npm test` is green with no suite skipped, and `npm run lint` reports no errors. Both card files carry a 1px structural line at rest and no resting ring; hover and focus-visible utilities are byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the new resting edge in the browser, in all four themes</name>
  <files>(no source changes — measurement only)</files>
  <precondition>`npm run dev` serves the app at http://localhost:3000 from the main checkout (not a worktree — Turbopack will not resolve `next` in one). Start it if it is not already running.</precondition>
  <action>
    Reading the class name back is not verification. Two past bugs in this project were caught
    only by measuring what the browser actually painted, so measure.

    Open the landing page and, for each of the four themes, set the root classes directly
    rather than clicking through the settings menu, so the theme is unambiguous:

      document.documentElement.className = 'theme-daylight light'
      document.documentElement.className = 'theme-chalk light'
      document.documentElement.className = 'theme-slate dark'
      document.documentElement.className = 'theme-phosphor dark'

    (That mapping is `themeClassesFor` in `lib/theme.ts`: `theme-<id>` plus the bare mode
    class. Restore the page to its normal state when finished.)

    In each theme read, from a preset card button and from the Continue card button if it is
    on screen:

      const s = getComputedStyle(card);
      ({ border: s.borderTopColor, width: s.borderTopWidth, fill: s.backgroundColor,
         page: getComputedStyle(document.body).backgroundColor, ring: s.boxShadow })

    Record for each theme:
    - the painted border colour and that it is 1px on all four sides;
    - the card fill and the page fill behind it, confirming neither changed;
    - `boxShadow` at rest. It must report no shadow. That is the empirical proof that
      dropping the resting ring leaves ring width at zero — do not take the Tailwind
      behaviour on trust, this is the measurement that settles it.

    Then compute contrast for the new line against the surface on BOTH sides of it — the card
    fill it sits on and the page behind it — for all four themes. The line-on-canvas pairing
    is not one this app has used anywhere before, so it has never been checked. There is no
    contrast test in this repo any more (`lib/design/contrast.ts` and
    `lib/design/palette.test.ts` were deleted by decision in 260824-i05 — do not reinstate
    them and do not tell the founder to run them). Compute the ratios yourself from the
    measured rgb values with a throwaway node one-liner, using the standard WCAG relative
    luminance formula. The bar for a non-text boundary is 3:1.

    Expected, to check your measurement against rather than to copy into the SUMMARY:
    Daylight 3.01, Chalk 3.01, Slate 3.39, Phosphor 3.07 against the card fill; 4.13, 4.13,
    3.70, 3.80 against the page. If a measured ratio disagrees with the expectation by more
    than a rounding step, stop and report it rather than reconciling it — it means the
    painted colour is not the token you think it is.

    Check the boundary in the softest theme, not the loudest: Daylight and Chalk are the
    weakest at 3.01:1, so judge the result there.

    Also note, for the SUMMARY, that Slate is the one theme where the two nested lines differ
    in weight — its `--surf-line` and `--surf-line-faint` are different values, so the card's
    new outer line reads distinctly stronger than the thumbnail's inner line, while in the
    other three themes the two lines are the same painted colour. That is a property of the
    ramps, not a defect introduced here.

    Finally confirm both interactive states still paint their accent edge, in Daylight:
    - focus-visible: press Tab until a card has keyboard focus, then re-read `borderTopColor`
      and `boxShadow`. Both must be the accent-ink colour, the ring at 2px.
    - hover: move the real pointer over a card and re-read the same two values. If the tooling
      cannot produce a genuine `:hover`, say so plainly in the SUMMARY and fall back to
      confirming the hover rule is present in the served stylesheet — do not claim a
      measurement you did not take.
  </action>
  <verify>
    <human-check>
      The measurements exist and are written down: a four-theme table of the new line's
      contrast against the card fill and against the page; `boxShadow` reported as absent at
      rest in every theme; card fill and page fill confirmed unchanged from before the edit;
      and hover plus focus-visible each confirmed to paint the accent border and a 2px accent
      ring. Every ratio against the card fill is at or above 3:1.
    </human-check>
  </verify>
  <done>A four-theme measurement table is recorded, the resting state is proven ring-free by measurement rather than by reasoning, both interactive states are confirmed, and the softest theme (Daylight/Chalk at ~3.01:1) is named as the one the result was judged in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Look at the home page in all four themes</name>
  <what-built>
    The cards on the home page now have their own thin line drawn around them, so each card
    reads as a distinct object sitting on the page rather than a soft patch of colour. That
    line is the same one the working panel on every design screen already uses. Together with
    the line added around the board picture last time, you now get the two nested lines from
    the screenshot you sent. Nothing was moved, resized or recoloured — the page is still
    white, the card is still sand, the board picture still sits in its white well. The faint
    grey ring the cards used to have at rest was taken away, because a real line one pixel
    inside it would have made the edge look smudged; hovering a card and tabbing to a card
    still light up in the accent colour exactly as before.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, then open http://localhost:3000.
    2. Look at the preset cards with the mouse away from them. Each should have a clean thin
       khaki line around it, and the board picture inside should have its own thinner line —
       two nested lines, like the crop you sent.
    3. Hover a card. The edge should turn the accent colour and thicken, as it always has.
    4. Press Tab until a card is selected by keyboard. Same accent edge.
    5. If you have a board in progress, check the Continue Current Board card looks exactly
       as heavy as the preset cards beside it.
    6. Switch themes with the gear in the nav and look at all four. Daylight and Chalk are the
       softest — the line is real but light there by design. Slate is the one where the card's
       outer line will look noticeably stronger than the board picture's inner line.
  </how-to-verify>
  <resume-signal>Type "approved", or say what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none introduced)* | Two CSS utility classes on presentational components. No new input, no network call, no storage, no dependency, no server code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j97-01 | Information Disclosure | components/setup/*.tsx | low | accept | Static styling on an unauthenticated public landing page; no data is rendered that was not already rendered. |
| T-j97-02 | Tampering | dependencies | low | accept | No package-manager install in this plan, so the package legitimacy gate does not apply. Task 1 asserts the blast radius is exactly two `.tsx` files. |
</threat_model>

<verification>
- All eleven Task 1 gates print PASS.
- `npm test` green, `npm run lint` clean.
- `git diff --name-only` lists exactly `components/setup/preset-card.tsx` and
  `components/setup/continue-board-card.tsx` — `app/globals.css` untouched, no token added.
- The two card className strings are still byte-identical to each other.
- A four-theme measured contrast table for the new resting line is in the SUMMARY, against
  the card fill and against the page, with Daylight/Chalk named as the softest at ~3.01:1
  and every value at or above the 3:1 non-text bar.
- Measured proof, not asserted, that no ring is painted at rest and that hover and
  focus-visible each still paint the accent border and a 2px accent ring.
- Slate named in the SUMMARY as the one theme where the two nested lines differ in weight,
  framed as a property of the ramps rather than something this task introduced.
- The blocking human-verify checkpoint is approved.
</verification>

<success_criteria>
- Both landing-page cards carry a resting `--surf-line` edge, applied the same way the
  tabbed panel's outer edge applies it — a plain border, no ring.
- `--surf-line` was used and not `--surf-line-faint`, the well's inner edge still uses
  `--surf-line-faint`, and the reason each one is where it is now lives in a comment that
  cites `components/viewer/tabbed-panel.tsx`.
- The resting ring is gone and its removal is explained in the code, so it is not restored
  by the next editor as a missing style.
- Zero fills changed, zero tokens added, zero radius or spacing or type changes, nothing on
  the `/design` screens touched.
- Both card files changed together, and a gate proves their className strings did not drift.
</success_criteria>

<output>
Create `.planning/quick/260826-j97-landing-page-cards-gain-the-structural-o/260826-j97-SUMMARY.md` when done.

Write it for a shaper, not a developer: say what changed on the screen (the cards on the home
page now have their own drawn edge, matching the panels on the design screens, giving the two
nested lines from the founder's screenshot) and what did not (no fills changed, nothing moved,
hover and keyboard focus behave exactly as before). Same for the commit message.
</output>
