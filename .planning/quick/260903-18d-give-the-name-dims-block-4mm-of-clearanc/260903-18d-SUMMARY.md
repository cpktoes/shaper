---
phase: quick-260903-18d
plan: 01
subsystem: templates
tags: [pdf-export, jspdf, geometry, paper-saver-strip, full-sized-template]

requires:
  - phase: quick-260902-kon
    provides: "stripFurniture's page-aware placement scan for the name/dims block, and the
      characterisation-pin discipline (frozen digest captured against the unmodified module) this
      task's Task 1 reuses one level down"
provides:
  - "NAME_BOX_CLEARANCE_MM now names a two-sided rule: the board name + dims block keeps this much
    daylight off the stringer (inboard, unchanged) AND off the outline curve (outboard, new) on
    BOTH template prints"
  - "nameBlockPlacement's (tiled sheet) and scanPagesForNameBlock's (Paper Saver strip) required
    half-width is clearanceMm + boxWidthMm + clearanceMm, not clearanceMm + boxWidthMm"
  - "A seven-function characterisation pin (Task 1), captured from the untouched module before any
    source edit, proving the cj5 eight-function pin's Task 2 recapture is a name-block move and
    not a tile-grid regression"
  - "TEMPLATE_PDF_OUT opt-in sample writer on the Full Sized Template, mirroring the strip's
    STRIP_PDF_OUT, for future rendered-page review"
affects: [paper-saver-strip, template-export, full-sized-template]

actuals:
  tokens: 8460
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Split-then-recapture characterisation pin: freeze a NARROWER pin (seven of eight functions)
      against the unmodified module before touching source, then only recapture the WIDER pin
      (all eight) once the narrower one proves green through the change — never overwrite a frozen
      digest on trust alone"
    - "Paper-fit guard vs. outline-containment requirement kept as two distinctly-named local
      quantities (boxFootprintRightEdge vs. requiredHalfWidth) in scanPagesForNameBlock, so a
      future edit cannot merge 'does the box fit on this paper' with 'does the curve clear the
      box' by accident"

key-files:
  created: []
  modified:
    - lib/geometry/template.ts
    - lib/geometry/template.test.ts
    - components/template/build-template-pdf.test.ts

key-decisions:
  - "One constant (NAME_BOX_CLEARANCE_MM), one rule, applied identically in nameBlockPlacement
    (tiled sheet) and scanPagesForNameBlock (Paper Saver strip) — not two separate constants for
    the two prints, per the founder's own wording ('give the 4mm of clearance on both template
    prints')"
  - "The paper-fit guard in scanPagesForNameBlock stays on the box's own footprint
    (leftEdge + boxWidthMm) and is deliberately NOT widened by the new clearance term — that term
    belongs only to the outline-containment check, where the curve actually is; folding it into
    the paper-fit guard would skip a page for want of 4mm of paper nothing is drawn on"
  - "The needle-nose strip test's literal 'lands on pageIndex 1' assertion was replaced with
    pageIndex > 0 plus a loop proving every earlier page genuinely cannot hold the block — the
    wider rule does shift that board's a4 landing from page 1 to page 2, exactly as the plan
    predicted might happen on at least one paper size"

requirements-completed: [QT-260903-18d]

coverage:
  - id: D1
    description: "Seven-function characterisation pin captured from the unmodified
      lib/geometry/template.ts (verified via git diff --quiet before capture) — proves the
      Task 2 recapture of the sibling eight-function cj5 pin is a name-block move, not a
      tile-grid regression"
    verification:
      - kind: unit
        ref: "lib/geometry/template.test.ts#existing seven-of-eight tile-grid functions are unchanged by the two-sided clearance work (characterisation pin, quick task 260903-18d, Task 1 — frozen, never edit)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both placement functions (nameBlockPlacement for the tiled sheet,
      scanPagesForNameBlock for the Paper Saver strip) now require clearanceMm + boxWidthMm +
      clearanceMm; every preset at both paper sizes still lands on page 0/page 1 of both prints,
      with the curve-side gap asserted directly (>= NAME_BOX_CLEARANCE_MM) in all three touched
      suites; the eight-function cj5 pin recaptured with a dated amendment; the strip digest pin
      and 2in scale-square literal pin pass unedited"
    verification:
      - kind: unit
        ref: "lib/geometry/template.test.ts#nameBlockPlacement"
        status: pass
      - kind: unit
        ref: "lib/geometry/template.test.ts#stripFurniture"
        status: pass
      - kind: unit
        ref: "components/template/build-template-pdf.test.ts#name block containment (post-checkpoint fix, defect 3: box fully inside the outline on page 1, now at its real, larger size)"
        status: pass
    human_judgment: false
  - id: D3
    description: "TEMPLATE_PDF_OUT opt-in writer added (mirrors STRIP_PDF_OUT), skipped by default;
      five sample PDFs regenerated (strip: longboard/shortboard/midlength at Letter; tiled
      template: longboard/shortboard at Letter) for a human to render page 1 and eyeball the
      daylight between the block's outboard corner and the curve"
    verification: []
    human_judgment: true
    rationale: "Rendering PDFs to images and eyeballing the daylight requires visual inspection.
      Per the executor's explicit constraints, this human-check step belongs to the orchestrator,
      not this executor — deferred here, not attempted."

duration: ~35min
completed: 2026-09-03
status: complete
---

# Quick Task 260903-18d: Give the name/dims block 4mm of clearance on the curve side too — Summary

**The board name + dimensions block now keeps 4mm of daylight off the outline curve on its outboard edge, matching the 4mm it already kept off the stringer on its inboard edge, on both the Full Sized Template (tiled) and the Paper Saver strip — closing the exact grazing the founder observed in quick task 260902-kon's own deferred checkpoint.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3 of 3 (Task 3's human-check sub-item explicitly deferred to the orchestrator, per constraints)
- **Files modified:** 3

## Accomplishments

- Froze a new seven-function characterisation pin (Task 1) over the tile-grid functions this task
  does NOT change (`computeTemplateLayout`, `computeTemplateMarks`, `markPlacements`,
  `markLineSegments`, `templatePageBoxes`, `computeTailClosure`, `tailClosureSegments`) — captured
  from `lib/geometry/template.ts` while `git diff --quiet` proved it untouched, and left green,
  unedited, through Task 2.
- Widened the required half-width in both placement functions (Task 2): `nameBlockPlacement`'s
  (tiled sheet) `clearanceMm + boxWidthMm` became `clearanceMm + boxWidthMm + clearanceMm`, and
  `scanPagesForNameBlock`'s (Paper Saver strip) matching expression grew the same way — while its
  separate paper-fit guard (does the box's own footprint fit the page at all) stayed on the
  narrower `leftEdge + boxWidthMm`, given its own local name (`boxFootprintRightEdge`) so the two
  quantities can never be merged by accident.
- Recaptured the existing eight-function `cj5` characterisation pin's digests with a dated
  amendment explaining the one authorised recapture — permitted only because the sibling
  seven-function pin (Task 1) stayed green through the same change, proving nothing else in the
  tile grid moved.
- Added a NEW, direct assertion of the founder's own requirement in all three touched test files:
  sample the outline across the block's whole station span, take the minimum, subtract the block's
  right edge, and assert the remainder is at least `NAME_BOX_CLEARANCE_MM` — not merely implied by
  the pre-existing corner-containment checks (which a grazing corner also satisfies).
- Rewrote every comment describing the clearance as stringer-side only:
  `NAME_BOX_CLEARANCE_MM`'s own doc, `nameBlockPlacement`'s doc, `scanPagesForNameBlock`'s doc, and
  `stripFurniture`'s Tier 2 doc (re-derived and updated the arithmetic it asserts: the widepoint's
  minimum half-width, 203.2mm, stays comfortably clear of the new required half-width, which rises
  from 78mm to 82mm).
- Replaced the strip's needle-nose test's literal `pageIndex === 1` assertion with `pageIndex > 0`
  plus a loop proving every page before the landing page genuinely cannot hold the block — the
  wider rule does shift that board's a4 landing from page index 1 to page index 2 (confirmed by
  the test run), exactly the kind of shift the plan warned might happen on at least one paper size.
- Added a `TEMPLATE_PDF_OUT` opt-in sample writer to the Full Sized Template's test suite (Task 3's
  code portion), mirroring the strip's `STRIP_PDF_OUT` exactly — skipped by default, a permanent
  part of the suite.
- Regenerated five sample PDFs (see below) for the orchestrator's deferred human-check.

## Task Commits

Each task was committed atomically:

1. **Task 1: Pin the seven untouched tile-grid functions against the unmodified module** — `1725b47` (test)
2. **Task 2: Require clearanceMm of curve on the block's outboard edge, in both placement functions** — `b28d1ef` (feat)
3. **Task 3 (code portion): Add the TEMPLATE_PDF_OUT opt-in sample writer** — `3ea1c52` (test)

_No plan-metadata commit from inside this worktree — the orchestrator copies SUMMARY.md/STATE.md
out and commits per the standard flow._

## Files Created/Modified

- `lib/geometry/template.ts` — `NAME_BOX_CLEARANCE_MM`'s doc now names both edges;
  `nameBlockPlacement`'s required half-width grew from `clearanceMm + boxWidthMm` to
  `clearanceMm + boxWidthMm + clearanceMm`; `scanPagesForNameBlock` keeps its paper-fit guard on
  the box's own footprint (`boxFootprintRightEdge`, a new locally-named quantity) while its
  curve-containment `requiredHalfWidth` grew the same way; `stripFurniture`'s Tier 2 doc re-derived
  and updated (78mm → 82mm against the widepoint's 203.2mm floor)
- `lib/geometry/template.test.ts` — new seven-function characterisation pin (Task 1, frozen);
  cj5 eight-function pin recaptured with a dated amendment (Task 2); new direct curve-clearance
  assertions in the `nameBlockPlacement` and `stripFurniture` describes; updated
  `requiredHalfWidth` arithmetic in the existing containment/midlength-numeral/needle-nose tests;
  needle-nose test's literal page assertion generalised to a loop over every earlier page
- `components/template/build-template-pdf.test.ts` — updated `requiredHalfWidth` in the "name
  block containment" describe (real drawn box height, ~25.6mm); new direct curve-clearance
  assertion for the real, drawn box; new `TEMPLATE_PDF_OUT` opt-in sample writer (Task 3)

## Decisions Made

- One constant, one rule, two call sites — `NAME_BOX_CLEARANCE_MM` unchanged in value, its
  required-half-width expression widened identically in both `nameBlockPlacement` and
  `scanPagesForNameBlock`, per the founder's own wording.
- The paper-fit guard in `scanPagesForNameBlock` is deliberately NOT widened by the new clearance
  term (`design_decision` §2 of the plan) — it stays a pure "does the box's own footprint fit this
  page" check, now given its own local name (`boxFootprintRightEdge`) so a later reader cannot
  merge it with the curve-containment requirement by accident.
- The needle-nose test's assertion was generalised from a literal page number to the property it
  was standing in for (`pageIndex > 0` plus "no earlier page fits"), since the wider rule is
  expected to — and did — shift that board's landing page on at least the a4 paper size.

## Deviations from Plan

**1. [Process note, not a Rule 1-4 deviation] Task 2's `tdd="true"` flag was not followed as a
strict separate RED-then-GREEN commit pair.**
- **What happened:** Task 2 carries `tdd="true"` in its frontmatter. The plan's own `<action>` for
  this task interleaves new test assertions and source changes tightly (the new assertions only
  make sense once the required-half-width formula changes, and the eight-function pin recapture
  requires the source change to already be in place). I made all Task 2 source and test edits,
  verified the full suite green, then committed once as a single `feat(...)` commit
  (`b28d1ef`) rather than a separate failing `test(...)` commit followed by a `feat(...)` commit.
- **Why this is being flagged rather than silently left:** the executor's own TDD protocol calls
  for a RED commit (tests fail against unmodified source) before a GREEN commit (source changes
  make them pass). That sequence was not followed literally here.
- **Impact:** None on correctness — `npm test` was run and confirmed fully green (1654 passed, 2
  skipped) before the Task 2 commit was made, and the seven-function pin (captured before ANY
  source edit, in Task 1) plus the recaptured eight-function pin together provide the same
  "before/after" proof a literal RED/GREEN split would have given. No behaviour shipped unverified.

**Total deviations:** 1 process note (no Rule 1-4 auto-fixes were needed — the plan's own
arithmetic, test structure, and comment rewrites were followed as specified).
**Impact on plan:** None on the shipped behaviour; flagged for transparency about process only.

## TDD Gate Compliance

Task 2 (`tdd="true"`) does not show a separate `test(...)` commit before its `feat(...)` commit in
this quick task's git history — see the deviation note above. The seven-function pin (Task 1,
`1725b47`, a `test(...)` commit captured from the unmodified module) and the full `npm test` green
run before Task 2's commit together substitute for the missing literal RED gate.

## Issues Encountered

None — no auth gates, no blocking issues, no architectural questions. The needle-nose test's a4
landing page shift (page index 1 → 2) was anticipated by the plan's own `key_facts_verified_at_planning_time` and confirmed, not a surprise.

## Verification Results

- `npm test`: **1654 passed, 2 skipped** (1656 total) — baseline before this task was 1622 passed,
  1 skipped; the count rose by 32 passing tests plus one new skipped test (`TEMPLATE_PDF_OUT`).
  Nothing that passed before failed.
- `npx vitest run lib/geometry/template.test.ts`: 455 passed — the seven-function pin (8/8), the
  recaptured eight-function `cj5` pin (8/8), the strip digest pin (8/8, unedited), and the
  scale-square literal pin (8/8, unedited) all green.
- `npm run lint`: 0 errors, 9 pre-existing warnings (unrelated files: `outline.test.ts`, four
  `scripts/extract-prototype-*-golden.mjs` files) — untouched by this task.
- `npx tsc --noEmit`: only the two known phantom `LayoutProps` errors in untouched
  `app/layout.tsx` / `app/design/layout.tsx` (environmental, not this task's).
- Task 2's plan-specified verify command (grep for the literal `boxWidthMm + clearanceMm`
  expression appearing exactly twice in non-comment code, the eight recaptured digest hex strings,
  and the four-times-each scale-square literals) — all passed.

### Seven-function pin (Task 1) — result

Captured from `lib/geometry/template.ts` with `git diff --quiet` and `git diff --cached --quiet`
both exiting 0 (module provably untouched) immediately before capture. Ran
`npx vitest run lib/geometry/template.test.ts` with the digest map empty, read the eight failure
digests, pasted them in, re-ran green. Stayed green, unedited, through the entire Task 2 change:

| Preset | Letter | A4 |
|---|---|---|
| shortboard | `5f124970774b6dd7` | `51fab7d7595ab86e` |
| fish | `b4b8f5d3079c3fdd` | `35a80421f8529063` |
| midlength | `64cdca8ff6c905ee` | `f35fc384265d9c84` |
| longboard | `bafd881a02437fc0` | `7c1eb57da94e799b` |

### Eight-function `cj5` pin — recapture status (Task 2)

Recaptured with a dated amendment (2026-09-03) in the block's own comment, explaining what changed
(`nameBlockPlacement` only, on the founder's instruction) and pointing to the seven-function pin as
the proof the other seven members did not move:

| Preset | Letter (old → new) | A4 (old → new) |
|---|---|---|
| shortboard | `b44c287a1203b6e7` → `3cbffdc1fc29fa49` | `cd43ab684592226c` → `573ce98f01b1d7a7` |
| fish | `80dd9f2ae2cc89be` → `a66579d72725acd7` | `f8dfa62d670f076e` → `d864434a96c6998c` |
| midlength | `c44a5c300a92802b` → `1fe9ee315661716e` | `e0ead812d01197aa` → `cb478689363b1126` |
| longboard | `a4c29bc92109c090` → `bd3e876c54d16acb` | `365d2e5e1ba27b50` → `22351bde3916eecf` |

### Strip digest pin and scale-square literal pin — unedited, both green

`git diff` on `lib/geometry/template.test.ts` shows zero changes to the "Paper Saver strip output
is unchanged by the name-block move (characterisation pin, quick task 260902-kon)" describe block
— its eight strip digests and its 32 scale-square literal numbers (`topStation`/`halfWidthStart` x
4 presets x 2 papers) are byte-identical to before this task. All 16 of its `it.each` cases pass.

## Per-Preset Landing Table — Both Prints

Computed with a standalone script (`tsx`, deleted after use — not committed) reusing the real
geometry and the real drawn box height (`nameBlockContent`, 25.60mm for every preset at Letter and
A4 — matching the plan's own `key_facts_verified_at_planning_time`):

| Preset | Paper | Tiled pageIndex | Tiled topStation | Tiled curve gap | Strip pageIndex | Strip topStation | Strip curve gap |
|---|---|---|---|---|---|---|---|
| shortboard | letter | 0 | 1737.60 | 4.437mm | 0 | 1737.60 | 4.392mm |
| fish | letter | 0 | 1617.40 | 4.540mm | 0 | 1617.40 | 4.451mm |
| midlength | letter | 0 | 2052.60 | 4.593mm | 0 | 2025.60 | 21.477mm |
| longboard | letter | 0 | 2720.20 | 4.277mm | 0 | 2720.20 | 4.120mm |
| shortboard | a4 | 0 | 1737.60 | 4.437mm | 0 | 1737.60 | 4.392mm |
| fish | a4 | 0 | 1617.40 | 4.540mm | 0 | 1617.40 | 4.451mm |
| midlength | a4 | 0 | 2052.60 | 4.593mm | 0 | 2028.60 | 19.717mm |
| longboard | a4 | 0 | 2720.20 | 4.277mm | 0 | 2720.20 | 4.120mm |

Every real preset still lands on page 0 (page 1 to a shaper) of BOTH prints, at both paper sizes.
Midlength's strip gap (21.5mm / 19.7mm) is unusually wide because its block is pushed by the page
numeral, exactly as `key_facts_verified_at_planning_time` predicted — its placement did not move
station-wise on the strip (`2025.60` unchanged), it just now also clears the curve comfortably.

**Movement toward the tail, tiled sheet (predicted vs. actual, both paper sizes identical since
the station axis doesn't depend on paper width):**

| Preset | Old `topStation` | New `topStation` | Shift | Plan's prediction |
|---|---|---|---|---|
| longboard | 2722.20 | 2720.20 | 2.00mm | 2mm ✓ |
| fish | 1621.40 | 1617.40 | 4.00mm | 4mm ✓ |
| midlength | 2058.60 | 2052.60 | 6.00mm | 6mm ✓ |
| shortboard | 1746.60 | 1737.60 | 9.00mm | 9mm ✓ |

All four shifts match the plan's `key_facts_verified_at_planning_time` exactly. No preset newly
reached the tiled sheet's narrow-nose fallback — every placement above satisfies the pre-existing
corner-containment assertions (which the fallback branch does not guarantee), confirmed by the
full `npm test` run staying green, and every `pageIndex` above is `0`.

**Needle-nose strip landing (the constructed 10ft x 16in, 35° nose, 0% fullness board):**

- Letter: still lands on page index 1 (unchanged from the old rule).
- A4: now lands on page index 2 (shifted from page index 1 under the old rule) — the plan's own
  prediction that "at least one paper size" would shift, confirmed. Every page before the landing
  page was proven, by test, to genuinely not hold the block under the new required half-width.

## Sample PDFs Generated (for the orchestrator's deferred human-check)

Written outside the worktree, per the constraints:

- `/private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/longboard-letter.pdf` (Paper Saver strip)
- `/private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/shortboard-letter.pdf` (Paper Saver strip)
- `/private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/midlength-letter.pdf` (Paper Saver strip)
- `/private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/template-longboard-letter.pdf` (Full Sized Template)
- `/private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/template-shortboard-letter.pdf` (Full Sized Template)

## Deferred: Task 3's Human-Check (per explicit executor constraints)

Task 3's plan text calls for splitting page 1 of each sample PDF into an image (via `pypdf` +
`qlmanage -t`, no poppler on this machine) and eyeballing the daylight between the block's
outboard corner and the curve. Per this run's explicit constraints, **that rendering/eyeballing
step belongs to the orchestrator** — it was not attempted here and is not marked done. The five
PDFs above are ready for that review. Task 3's code portion (the `TEMPLATE_PDF_OUT` writer) is
complete and committed.

**What to verify (adapted from the plan's own success criteria):**
1. Render page 1 of the Full Sized Template samples (longboard, shortboard at Letter) — confirm
   the board name + dims block's top-right corner has visible daylight before the curve, not
   touching it.
2. Render page 1 of the Paper Saver strip samples (longboard, shortboard, midlength at Letter) —
   same check; midlength's block should read as clearly separate from both the curve and the page
   numeral.
3. Confirm the 2in scale-check square on the strip samples is exactly where it was in prior review
   (top-right corner, outside the nose taper) — unchanged by this task.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All automated verification green: `npm test` 1654/2 (was 1622/1), `npm run lint` clean, `tsc`
  clean apart from the two known phantom errors.
- Task 3's human-check is the only remaining item — awaiting the orchestrator's visual review of
  the five sample PDFs listed above.
- `npm run build` was not run in this worktree (Turbopack cannot resolve `next` there, per the
  environment notes) — the orchestrator's merge step should confirm a clean build from the main
  checkout if desired.

## Self-Check: PASSED

- FOUND: lib/geometry/template.ts
- FOUND: lib/geometry/template.test.ts
- FOUND: components/template/build-template-pdf.test.ts
- FOUND: commit 1725b47
- FOUND: commit b28d1ef
- FOUND: commit 3ea1c52
- FOUND: all five sample PDFs at /private/tmp/claude-501/-Users-kontoes-Code-shaper/1351620e-b04a-434a-9a98-cad58f6894ec/scratchpad/strip-out/fix3/

---
*Phase: quick-260903-18d*
*Completed: 2026-09-03*

## Task 3 checkpoint — orchestrator's results (2026-09-03)

Page 1 rendered (pypdf split + Quick Look) for the five samples against the merged code (`5bee7c1`):

- **Paper Saver**: longboard and shortboard now show visible daylight between the block's top-right
  corner and the curve where it previously touched; midlength is unchanged, as predicted (already
  21mm clear from the numeral push-down). Strip digest pin, scale-square literal pin and seam labels
  all green; page 1's 2in square intact.
- **Full Sized Template**: longboard (page 1 of 24) and shortboard (page 1 of 16) both show the block
  clear of the curve inside the outline; scale square, how-to box and page label unchanged. The
  seven-function pin stayed green through the change and the eight-function pin was re-captured with
  its dated note.
- **Gates in main**: see the STATE.md row; tests 1654 passed / 2 skipped.

**Pre-existing, out of scope, noted for the founder:** on the Full Sized Template's longboard page 1
the rail curve runs through the how-to instruction box (fixed top-right under the scale square) —
untouched by this task (the tiled builder is zero-line unchanged) and present before it; a wide
nose reaches that corner. Worth its own small task if it bothers on paper.
