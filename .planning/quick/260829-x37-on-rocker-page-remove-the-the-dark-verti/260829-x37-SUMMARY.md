---
phase: 260829-x37
plan: 01
subsystem: ui
tags: [rocker, svg, construction-overlay, rocker-viewer]

# Dependency graph
requires:
  - phase: 260829-snm
    provides: the construction overlay's steering lines, knot dots and drag targets
provides:
  - Construction overlay on the ROCKER screen with the five full-height vertical station lines removed
affects: [rocker-editor, order-form]

# Actuals (#2632)
actuals:
  tokens: 537
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - components/rocker/rocker-viewer.tsx

key-decisions:
  - "Removed only the first child of the showConstruction branch (the five full-height vertical lines) and its explanatory comment, leaving the four steering lines, three knot dots, four drag targets and their hit circles untouched"
  - "Dropped the one closing sentence in the deck-envelope comment that pointed at the deleted lines, since it described code that no longer exists; the rest of that comment (worst-case reserve, compact branch, non-finite fallback) is still accurate and was left alone"

patterns-established: []

requirements-completed: [QUICK-260829-x37]

coverage:
  - id: D1
    description: "On the ROCKER screen, switching construction lines on no longer draws the five dark full-height vertical lines at the measured stations"
    requirement: "QUICK-260829-x37"
    verification:
      - kind: unit
        ref: "grep -Fq 'pxY(maxDeckIn)' components/rocker/rocker-viewer.tsx (must NOT match)"
        status: pass
      - kind: unit
        ref: "grep -c 'construction-line-' / 'construction-dot-' components/rocker/rocker-viewer.tsx == 1 each"
        status: pass
    human_judgment: false
  - id: D2
    description: "The remaining construction overlay (four steering lines, three knot dots, four drag targets) still draws and drags exactly as before, in both orientations, in wide view, and the order form's ROCKER window is unchanged"
    verification: []
    human_judgment: true
    rationale: "Requires visually confirming the drawing and dragging behaviour in a browser at /design/rocker and /design/summary; deferred to the orchestrator/user per this task's constraints (no dev server run inside the worktree)"

# Metrics
duration: ~10min
completed: 2026-08-30
status: complete
---

# Quick Task 260829-x37: Remove the dark vertical station lines from the rocker construction overlay Summary

**Deleted the five full-height vertical lines the ROCKER screen's construction-lines toggle used to draw at each measured station — the curve's own steering lines, knot dots and drag targets are untouched.**

## Performance

- **Duration:** ~10 min
- **Tasks:** 2 completed
- **Files modified:** 1

## Accomplishments
- Removed the JSX that mapped over the five measured stations to draw a full-height vertical line from the baseline to the deck envelope, plus its four-line justifying comment
- Repaired the one sentence in the deck-envelope comment above the layout call that referenced those now-deleted lines
- Confirmed the ROCKER editor (`rocker-editor.tsx`) is still the only place that switches the overlay on, and that the SUMMARY order form's `RockerViewer` call never passes `showConstruction` at all
- Full test suite (1236 tests) and linter both green with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove the full-height vertical station lines from the construction overlay** - `dd0bfab` (fix)
2. **Task 2: Prove the change is surgical and confirm it on screen** - no code changes (verification only); see notes below

_Plan metadata commit handled by the orchestrator after merge, per this quick task's constraints._

## Files Created/Modified
- `components/rocker/rocker-viewer.tsx` - The construction overlay's first child (five full-height vertical station lines + its comment) deleted; the deck-envelope comment's closing sentence about those lines removed. Everything else in the `showConstruction` branch (four steering lines, three knot dots, four drag targets, transparent hit circles) is byte-identical to before.

## Decisions Made
- Removed only the first child of the `showConstruction` branch (the vertical lines map) and its comment — nothing else in the branch was touched
- Dropped only the one stale closing sentence in the deck-envelope comment, leaving the worst-case-reserve/compact-branch/non-finite-fallback prose intact since it is still correct

## Deviations from Plan

**Task 2's automated verify check as literally written is overbroad.** The plan's second automated check for Task 2 is:

```
test "$(grep -rl 'showConstruction={' components/ app/ | wc -l | tr -d ' ')" = "1"
```

Run literally, this returns 5 files, not 1 — because `showConstruction={` is also the name of an unrelated prop on `OutlineViewer` (used by `board-rack-card.tsx`, `preset-card.tsx`, `outline-editor.tsx`, and twice inside `order-form.tsx`'s deck/bottom template panels), not just `RockerViewer`'s prop of the same name. This is a pattern-matching artifact of the check, not a real second consumer of the rocker construction overlay.

I verified the actual intent by hand instead: grepping for `<RockerViewer` call sites shows exactly one that passes `showConstruction` — `rocker-editor.tsx:276` (`showConstruction={showConstruction}`) — and the SUMMARY order form's own `<RockerViewer>` call (`order-form.tsx:335`) never passes `showConstruction` at all (it only passes `rocker`, `foil`, `length`, `callouts="compact"`, `fitToBoard`, `boardFill={false}`), so it stays permanently off there. This confirms the plan's underlying claim — `rocker-editor.tsx` is the only place that switches the overlay on — even though the literal grep pattern in the plan's verify step is too broad to prove it on its own. No code change was needed to satisfy this; it is purely a verify-script imprecision, noted here per Rule 3 (blocking-check investigated and resolved, not a real blocker).

**No other deviations** — Task 1 was executed exactly as specified: the five-station vertical-line map and its comment deleted, the one stale prose sentence dropped, and nothing else in the branch touched.

## Issues Encountered
None beyond the verify-script note above.

## Human Verification Deferred

Per this task's constraints, the plan's Task 2 `<human-check>` (browser confirmation at `/design/rocker` and `/design/summary`) was not run inside the worktree — no dev server or browser was started. That check still needs to happen:

- On `/design/rocker`: switch construction lines ON and confirm the five dark vertical lines are gone while the four steering lines, three plain dots and four round grab targets remain; drag each of the four grab points in both nose-left and nose-up orientations; open wide view and confirm the same clean drawing.
- On `/design/summary`: confirm the ROCKER window on the order form looks exactly as it did before (it was never able to show construction lines, so this is a no-op check).

## Next Phase Readiness
No blockers. The change is contained to one file, fully covered by automated checks and the existing test suite; only the visual/drag confirmation in the browser remains, for the orchestrator/user to perform.

---
*Phase: 260829-x37*
*Completed: 2026-08-30*
