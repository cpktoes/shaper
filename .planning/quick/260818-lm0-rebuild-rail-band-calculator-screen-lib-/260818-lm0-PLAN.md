---
phase: quick-260818-lm0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/extract-prototype-rails-golden.mjs
  - lib/geometry/__fixtures__/prototype-rails-golden.json
  - lib/geometry/units.ts
  - lib/geometry/rail-bands.ts
  - lib/geometry/rail-bands.test.ts
  - app/design/rails/page.tsx
  - app/design/layout.tsx
  - components/site-nav.tsx
  - components/rails/rail-band-editor.tsx
  - components/rails/rail-controls.tsx
  - components/rails/rail-section-plot.tsx
  - components/rails/rail-data-table.tsx
autonomous: true
requirements: [RAIL-01, VIZ-01, UNIT-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A shaper opens /design/rails and sees the prototype's default rail setup: Nose 1 5/16\" @12\", Center 2 1/2\" board thickness, Tail 1 9/16\" @12\", all Flat deck profile, all Family 'med', all Ratio 60/40, tail Hard Edge on"
    - "Moving any sidebar control redraws all three cross-section plots and updates every number in the DATA table immediately"
    - "The three cross-section plots are drawn on a 1-inch grid at one shared scale, so nose, center and tail rails can be compared by eye without mental rescaling"
    - "Turning on a section's Hard Edge, Symmetrical, Use Single Tuck or Remove (corner cut) visibly adds or removes the matching segment in the plot and the matching row in the DATA table"
    - "Every number on screen reads in inches as a fraction (e.g. 1 9/16\"), while every value in the rail-band design state is metric millimetres"
    - "`npm test` proves the ported rail-band math reproduces the prototype's own numbers — section results, cross-section profile points, plot segments and data rows — for 11 fixture states, to within 1e-9 inch"
    - "A shaper can move between the outline editor and the rail band calculator from a top nav on both screens without editing the URL"
  artifacts:
    - lib/geometry/rail-bands.ts
    - lib/geometry/rail-bands.test.ts
    - lib/geometry/__fixtures__/prototype-rails-golden.json
    - scripts/extract-prototype-rails-golden.mjs
    - app/design/rails/page.tsx
    - app/design/layout.tsx
    - components/site-nav.tsx
    - components/rails/rail-band-editor.tsx
    - components/rails/rail-controls.tsx
    - components/rails/rail-section-plot.tsx
    - components/rails/rail-data-table.tsx
  key_links:
    - "Sidebar control onChange -> inchesToMm() at the boundary -> RailBandSpec state (the single place inches enter the model)"
    - "RailBandSpec -> computeRailBands() -> per-section {result, profile, segments, bounds, rows} -> plots + DATA table"
    - "prototype-rails-golden.json <- scripts/extract-prototype-rails-golden.mjs <- reference/project/Rails.dc.html (generated, never hand-edited)"
    - "buildRailProfile() output is the cross-section path Volume will integrate later — built and pinned now, unused by this screen"
    - "app/design/layout.tsx -> SiteNav -> /design/outline and /design/rails"
---

<objective>
Rebuild the Claude Design prototype's **Rail Band Calculator** as the second screen of the Next.js app, and give the user a way to walk between the two screens in the browser.

Purpose: the rail-band calculator is half of the product's Core Value — the numbers a shaper cuts foam to. They come off the founder's spreadsheet, so the port must be provably identical to the prototype, not merely similar. Pinning them with extract-and-execute golden tests (the technique already proven on the outline port) is what makes "provably" real.

Output: `lib/geometry/rail-bands.ts` (pure, metric-internal, golden-tested), the `/design/rails` screen with VIEWER and DATA pages, and a top nav shared by both design screens.

**Not a tracer-first plan.** The architecture this phase needs — `lib/geometry/*` pure module + extract-and-execute golden fixture + Tailwind/shadcn screen under `app/design/*` — was proven end-to-end yesterday by quick task 260818-kvp (outline editor). A thin vertical slice would add no information, so this decomposes as math-then-screen-then-nav.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/design/GEOMETRY-MODULE.md
@AGENTS.md
@lib/geometry/units.ts
@lib/geometry/outline.ts
@lib/geometry/outline.test.ts
@scripts/extract-prototype-golden.mjs
@components/outline/outline-controls.tsx
@components/outline/outline-editor.tsx
@components/outline/outline-viewer.tsx
@app/design/outline/page.tsx
@app/globals.css
</context>

<source_audit>

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Implement `lib/geometry/rail-bands.ts` — metric-internal, pure TS, porting spreadsheet-derived rail-band formulas | Task 1 |
| GOAL | Port the cross-section profile builder (`buildProfilePoints` equivalent), kept for Volume | Task 1 |
| GOAL | Vitest golden tests via extract-and-execute against `Rails.dc.html` | Task 1 |
| GOAL | `/design/rails` screen: dark sidebar with Nose/Center/Tail collapsible sections | Task 2 |
| GOAL | Sidebar controls: thickness, deck profile, family, ratio + Sym, tail Hard Edge | Task 2 |
| GOAL | Sidebar Advanced: corner-cut offset + Remove, Bottom Tuck 3 + Single Tuck, reset | Task 2 |
| GOAL | VIEWER page: three cross-section plots, 1-inch grid, uniform scale, colored segments, dots, shared legend | Task 2 |
| GOAL | DATA page: merged table, Rail Side / Deck Side / Bottom groups x Nose/Center/Tail columns | Task 2 |
| GOAL | Navigation between the two screens (TEMPLATE / RAILS top nav) | Task 3 |
| REQ | RAIL-01 (rail band calculator) | Tasks 1, 2 |
| REQ | VIZ-01 (visualize the design) | Task 2 |
| REQ | UNIT-01 (inches/litres at the surface, metric internally) | Tasks 1, 2 |
| RESEARCH | n/a — quick mode, no research phase | — |
| CONTEXT | n/a — no CONTEXT.md; scope decisions arrive inline as this task's constraints and are restated in `<scope_boundaries>` | — |

No unplanned items.
</source_audit>

<scope_boundaries>
**In scope:** the rail-band math module, the VIEWER page, the DATA page, and the top nav.

**Explicitly out of scope — do not build, do not stub, do not leave a placeholder button for:**
- The INSTRUCTIONS page (static traced-PPT artwork; a later task)
- "View Full Sized" actual-size modal, `plotSvgHtml`, and all printing
- Copy-to-clipboard / "Print Rails & Data" sidebar buttons
- The cm units toggle and the Settings disclosure (inches only)
- The board-outline plan view (`buildBoardOutlinePlot`, `planRefPaths`, `sideRefPaths`) and its legend checkboxes
- The "How to read this" callout plot (`howToRead`)
- Cross-screen state sharing with the outline editor (`applySeed` / `syncSnapshot` props plumbing) — the rails screen owns its own state
- Volume integration

`buildRailProfile` **is** in scope even though nothing on this screen renders it: Volume will consume it, and pinning it now while the prototype is in front of us is the whole point.
</scope_boundaries>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the rail-band math with extract-and-execute golden fixtures</name>
  <files>scripts/extract-prototype-rails-golden.mjs, lib/geometry/__fixtures__/prototype-rails-golden.json, lib/geometry/units.ts, lib/geometry/rail-bands.ts, lib/geometry/rail-bands.test.ts, package.json</files>

  <read_first>
    - `scripts/extract-prototype-golden.mjs` — the extraction harness to imitate (header warning, brace-matching, fixture map, JSON write). Copy its shape.
    - `lib/geometry/outline.ts` lines 1-40 — the port-header convention: a numbered list of every deliberate deviation from the prototype, and nothing else.
    - `lib/geometry/outline.test.ts` — the golden-test convention: typed golden state, spec builder, `TOLERANCE_IN`, comparison in inches.
    - `reference/project/Rails.dc.html` lines 507-530 (state defaults), 652-664 (`round16`, `toFrac`), 676-698 (`disp`, `familyLabel`, the six band helpers), 700-723 (`computeSection`), 543-566 (`syncSnapshot`), 929-960 (`cardFromResult`), 962-988 (`buildProfilePoints`), 990-1025 (`buildSegmentDefs`), 1027-1042 (`buildPlot` axis math only), 1314-1336 (`railDataHeadings` / `railDataLabelOrder` / merge).
  </read_first>

  <behavior>
    - For all 11 fixtures x 3 sections, every numeric field of our `RailSectionResult` equals the prototype's `computeSection` output to within 1e-9 inch.
    - `null` fields line up exactly: with Remove corner cut on, `cornerCutRail` / `cornerCutOffset` / `cornerCutDeck` are all null; with it off, all three are numbers.
    - Our `buildRailProfile` returns the same number of points, in the same order, at the same coordinates as the prototype's `buildProfilePoints` — including the hard-edge case (7 points ending at the apex bottom), the single-tuck case (7 points), and the default two-tuck case (8 points).
    - Our `buildRailSegments` returns the same segment `key` sequence with the same endpoints as `buildSegmentDefs`, including the conditional `cornerCut` / `hardEdge` / `tuck1` / `tuck2` members.
    - Our `buildRailDataGroups`, once formatted through `formatInchesFraction(v, 16)` with `null` rendering as an em dash and the hard-edge sentinel rendering as `Hard Edge`, reproduces the prototype's `cardFromResult` groups label-for-label and value-for-value.
    - A value whose magnitude rounds below 1/16 inch renders as an em dash, matching the prototype's `d()` helper.
    - `roundToSixteenthInch` snaps 2.5in x 80% to exactly 2in, and 1.31in x 72% to the nearest sixteenth.
    - `mergeRailDataTable` emits rows in the fixed canonical order regardless of which sections are open, filling absent cells with an em dash.
  </behavior>

  <action>
Build the golden harness first, then the module, then the test — in that order, so the prototype's own numbers exist on disk before any porting judgement is made.

**A. `scripts/extract-prototype-rails-golden.mjs`.** Model it on `scripts/extract-prototype-golden.mjs`: same generated-file header warning (extract and EXECUTE the prototype's own methods; never hand-copy or retype the prototype's math; the JSON is generated and must never be hand-edited), same brace-matching helper walking forward from a `name(args) {` marker to the matching close brace, same `mkdirSync` + `writeFileSync` tail.

Read `reference/project/Rails.dc.html` and extract these method bodies by marker: `round16(x) {`, `toFrac(x) {`, `disp(inches) {`, `familyLabel(x) {`, `apexLenRangeFor(x) {`, `cornerCutRailOffsetFor(x) {`, `deckMark1For(x) {`, `deckMark3For(x) {`, `computeSection({ thickness`, `buildProfilePoints(r, thickness, domed, opts) {`, `buildSegmentDefs(r, thickness, domed, opts) {`, `cardFromResult(title, subtitle, r, domed, boardThickness) {`, `syncSnapshot() {`. The `disp` marker sits on the same source line as the close of `sideRefPaths` — marker search still finds it; do not reformat the prototype file.

Assemble the extracted bodies as own properties of one plain host object built with `new Function(argNames, body)`, so that calling `host.computeSection(...)` binds `this` to the host and its `this.apexLenRangeFor(...)` calls resolve. Before each fixture set `host.state = fixtureState` and `host.props = {}` (`syncSnapshot` and `disp` read `this.state`; nothing reads `this.props`).

Fixture states are full prototype state objects: start from a `defaults` constant transcribed from the state block at lines 510-527 (`centerDeckPercent` 100, `centerBoardThickness` 2.5, `centerFamily` 3, `centerRatioTop` 60, `centerSymmetrical` false, the matching nose/tail trio, `noseThickness` 1.31, `tailThickness` 1.56, all three `*CornerCutOffsetOverride` / `*BottomTuck3Override` null, all three `*RemoveCornerCut` / `*SingleTuck` false, `tailHardEdge` true, `units` 'in'), then spread it into 11 named fixtures: `default`; `domedCenter` (`centerDeckPercent` 80); `domedAll` (nose 72, center 80, tail 85); `familyBoxy` (all three families 1); `familyKnifey` (all three families 5); `symmetrical` (all three `*Symmetrical` true, `tailHardEdge` false); `hardEdgeOff` (`tailHardEdge` false); `singleTuck` (all three `*SingleTuck` true, `tailHardEdge` false); `removeCornerCut` (all three `*RemoveCornerCut` true); `overrides` (`centerCornerCutOffsetOverride` 0.09375, `noseBottomTuck3Override` 0.5, `tailBottomTuck3Override` 0.75, `tailHardEdge` false); `ratioExtremes` (nose 30, center 70, tail 30).

Per fixture, call `host.syncSnapshot()` once — it is the prototype's own taper-clamp and profile derivation, so the harness never re-implements `round16(thickness * percent / 100)`. Take `noseRailThickness` / `centerRailThickness` / `tailRailThickness` and `noseProfile` / `centerProfile` / `tailProfile` straight off that snapshot. Then per section derive only the two facts the snapshot does not name: `domed = fixture[sectionDeckPercentKey] < 100` and `thicknessEff = domed ? snapshotRailThickness : boardThickness` (board thickness is `noseThickness` / `centerBoardThickness` / `tailThickness`). Call `host.computeSection(...)` and `host.buildSegmentDefs(...)` reusing the argument wiring verbatim from `syncSnapshot` lines 552-554 (center `scale` 1 / `domedBandBase` 6; nose and tail `scale` 0.75 / `domedBandBase` 4.5; `hardEdge` passed only on the tail call). Copying that call-site argument wiring is allowed; retyping any formula from `computeSection`'s body into this script is not. Build each section's card with `host.cardFromResult(title, subtitle, result, domed, boardThickness)`, composing the subtitle exactly as lines 1214-1216 do (`Domed`/`Flat`, then `Family` plus `host.familyLabel(family)`, then `Ratio top/bottom`, then ` · Symmetrical` for nose and center when symmetrical, and for the tail ` · Hard Edge` when hard edge else ` · Symmetrical` when symmetrical).

Write `lib/geometry/__fixtures__/prototype-rails-golden.json` keyed by fixture name, each holding `state` and a `sections` object keyed nose/center/tail, each section holding `domed`, `boardThicknessIn`, `railThicknessClampedIn`, `thicknessEffIn`, `result`, `profile`, `segments` (key, label, p1, p2 only) and `card`. Add `"golden:rails": "node scripts/extract-prototype-rails-golden.mjs"` to `package.json` and make the existing `"golden"` script run the outline extractor and then this one, so one command regenerates everything.

**B. `lib/geometry/units.ts`.** Add `roundToSixteenthInch(value: Mm): Mm` — convert to inches, `Math.round(inches * 16) / 16`, convert back. Document it as the port of the prototype's `round16` (Rails.dc.html line 652), and that this sixteenth-inch snap is observable product behaviour (the Tapered Rail Thickness a shaper reads off the Deck Profile slider), not a display nicety — so it belongs in the model, applied in the inch domain, exactly where the spreadsheet applies it.

**C. `lib/geometry/rail-bands.ts`.** Open with the same numbered port-header `outline.ts` uses, listing every deliberate deviation and nothing else. The deviations to record are:

1. INCH-DOMAIN CORE. Unlike `outline.ts`, where only four constants carried a unit, nearly every constant in these formulas is an inch measurement. Converting each one individually would multiply the chance of a silent transcription error in the exact code whose correctness is the product's Core Value. So this module keeps private inch-domain functions that are statement-for-statement ports of the prototype (`computeSectionInches`, `buildProfilePointsInches`, `buildSegmentDefsInches`, and the four band helpers), each carrying the prototype's own spreadsheet-cell comments, and the exported functions convert Mm in and Mm out at the boundary. The public surface is millimetres only; the inch core is never exported.
2. DEAD CODE. The prototype declares `railTuck1For` (line 695) and `cornerCutDeckFor` (line 698) but `computeSection` never calls them — it derives Rail Tuck 1 from `apexCenter - apexLenRange / 2` and Corner Cut (Deck) from `railMark1 - cornerCutRail`. Neither helper is ported; record why.
3. HARD EDGE DEFAULTING. The prototype omits `hardEdge` on its nose and center `computeSection` calls, leaving it `undefined` and therefore falsy at every read. This port passes an explicit `false`, which is behaviourally identical.
4. PRESENTATION SPLIT. `buildSegmentDefs` carries `color` and `dash` on each segment. Colours are a UI concern and every prototype dash is `'none'`, so the exported segment type carries `key`, `label`, `p1`, `p2` only; the plot component owns a colour map keyed by `key`.

Export: `RailFamily` (1|2|3|4|5); `RailSectionKey`; `RailSectionSpec` (`boardThickness: Mm`, `deckPercent: number`, `family: RailFamily`, `ratioTopPercent: number`, `symmetrical: boolean`, `cornerCutOffsetOverride: Mm | null`, `removeCornerCut: boolean`, `singleTuck: boolean`, `bottomTuck3Override: Mm | null`); `RailBandSpec` (nose, center, tail, `tailHardEdge: boolean`); `RAIL_SECTION_CONSTANTS` (center `{ scale: 1, domedBandBase: 6in }`, nose and tail `{ scale: 0.75, domedBandBase: 4.5in }`); `DEFAULT_RAIL_BAND_SPEC` built from the prototype's state defaults; `RailSectionResult` carrying every field `computeSection` returns, with `cornerCutRail` / `cornerCutOffset` / `cornerCutDeck` typed `Mm | null`; `RailSegment`; `RailDataGroup`; `computeRailSection`; `buildRailProfile`; `buildRailSegments`; `railPlotBounds`; `buildRailDataGroups`; `computeRailBands`; `railFamilyLabel`; `RAIL_DATA_HEADINGS`; `RAIL_DATA_LABEL_ORDER`; `mergeRailDataTable`.

`railPlotBounds(result, opts)` returns `{ xAxisMin: Mm, yAxisMax: Mm }` from `buildPlot` lines 1031-1035 only — `blankThickness`, the same `bandWidth` expression, `xAxisMin = min(-(bandWidth + 2in), xAxisMinShared ?? 0)`, `yAxisMax = blankThickness + 0.25in`. The optional `xAxisMinShared` is what lets the three plots share one axis. No pixel math in this module.

`buildRailDataGroups(result, { domed, boardThickness })` returns the `cardFromResult` group structure with values as data, not strings: `Mm` for a real measurement, `null` where the prototype prints an em dash (magnitude snaps below 1/16 inch), and a `"hard-edge"` sentinel for the single Bottom Tuck 3 row when hard edge is on. Row inclusion follows `cardFromResult` exactly — Board Thickness first only when domed, Rail Tuck 1 omitted when hard edge, both Corner Cut rows omitted when removed, Bottom Tuck 1 and 2 omitted when single tuck.

`computeRailBands(spec)` is the `syncSnapshot` orchestration: per section, `domed = deckPercent < 100`, `railThicknessClamped = roundToSixteenthInch(boardThickness * deckPercent / 100)`, `thicknessEff = domed ? railThicknessClamped : boardThickness`; then compute result, profile, segments, bounds and data groups, returning them keyed nose/center/tail. Hard edge applies to the tail only.

Port `bandWidth`, `domedY` and `band1Y` literally, including branches the current call sites can never reach (the `|| 1` and `Math.max(r.deckMark3, 1)` fallbacks) — a literal port diffs against the prototype in one pass; a "simplified" one does not.

**D. `lib/geometry/rail-bands.test.ts`.** Follow `outline.test.ts`: import the golden JSON, type the golden state, and build a `RailBandSpec` from it with `inchesToMm` at the boundary. Set `TOLERANCE_IN` to 1e-9 and compare everything in inches via `mmToInches`. Iterate all fixtures x all three sections and assert: every `result` field (numbers within tolerance, nulls exactly aligned); `profile` length and each point; the `segments` key sequence, label sequence and endpoints; and the data groups after formatting each value through `formatInchesFraction(value, 16)` with `null` rendering as an em dash and the hard-edge sentinel as `Hard Edge`, compared to the golden card's groups. Add plain unit tests, not golden-driven, for `roundToSixteenthInch` snapping, `railFamilyLabel`'s five names, and `mergeRailDataTable` emitting the canonical row order with em dashes for cells a section does not carry.
  </action>

  <verify>
    <automated>npm run golden:rails && git diff --quiet lib/geometry/__fixtures__/prototype-rails-golden.json && npx vitest run lib/geometry/rail-bands.test.ts lib/geometry/units.test.ts</automated>
  </verify>

  <done>The golden JSON regenerates byte-identically from `Rails.dc.html`, and every rail-band assertion passes for 11 fixtures x 3 sections. `lib/geometry/rail-bands.ts` imports nothing from React, Next or the DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /design/rails screen — sidebar, VIEWER page, DATA page</name>
  <files>components/rails/rail-band-editor.tsx, components/rails/rail-controls.tsx, components/rails/rail-section-plot.tsx, components/rails/rail-data-table.tsx, app/design/rails/page.tsx</files>

  <read_first>
    - `components/outline/outline-editor.tsx`, `outline-controls.tsx`, `outline-viewer.tsx` — the established screen shape: editor owns one spec object in state, controls convert at the boundary, viewer draws SVG. Reuse the `SectionHeading` and `SliderRow` styling wholesale.
    - `app/globals.css` — the `outline-*` palette tokens already defined (`outline-accent`, `outline-sidebar-bg`, `outline-sidebar-text`, `outline-sidebar-text-muted`, `outline-sidebar-divider`, `outline-page-bg`, `outline-ink`). Use them; add no new CSS tokens.
    - `app/design/outline/page.tsx` — the route + metadata pattern.
    - `reference/project/Rails.dc.html` lines 25-281 (sidebar markup), 283-356 (tabs + viewer markup), 447-474 (data table markup), 444 (the italic footnote text), 1027-1147 (`buildPlot` pixel math, grid, ticks, dots, legend), 1149-1234 (control bounds, `deckProfileFor`, card assembly), 1218-1235 (shared x-axis pass and shared legend), 1314-1336 (data table merge).
  </read_first>

  <action>
Build four client components under `components/rails/` plus the route. Inches appear only inside these components; state is millimetres.

**`rail-band-editor.tsx`** (client). Holds `useState<RailBandSpec>(DEFAULT_RAIL_BAND_SPEC)` plus UI-only state that is not part of the design: `noseOpen` / `centerOpen` / `tailOpen` (all true), `noseAdvancedOpen` / `centerAdvancedOpen` / `tailAdvancedOpen` (all false), and `activePage` ('viewer'). Derive `useMemo(() => computeRailBands(spec), [spec])`. Layout mirrors `outline-editor.tsx`: a `max-w-[400px]` dark sidebar (`bg-outline-sidebar-bg`) beside a `flex-1` main column on `bg-outline-page-bg`.

Above the main card, render two page tabs, VIEWER and DATA, styled as the prototype's tabs (rounded top corners, active tab white with an `#e4ddc9` border and no bottom border, inactive transparent with muted text). Do not render an INSTRUCTIONS tab.

Which sections appear in the main area is driven by the open flags, exactly as the prototype's `outputCards` is: a collapsed section drops out of both the VIEWER and the DATA columns. Order is always Nose, Center, Tail.

**`rail-controls.tsx`** (client). The sidebar. Title "Rail Band Calculator" with the subtitle "Rail band calculator for shaping consistent rails". Then three collapsible sections — NOSE RAIL, CENTER RAIL, TAIL RAIL — each an uppercase accent header button with a caret and a bottom divider, matching the outline sidebar's `SectionHeading`.

Per section, in order:
- Thickness slider. Nose and tail read `Thickness @12" — {fraction}`, range 1 to 2.5 inches, step 1/16. Center reads `Board Thickness — {fraction}`, range 1.75 to 3.5 inches, step 1/16.
- Deck Profile slider, label `Deck Profile — {fraction} (Tapered Thickness)` where the fraction is `roundToSixteenthInch(boardThickness * deckPercent / 100)`. This slider is inverted, per the prototype's `deckProfileFor` (lines 1182-1194): the slider's own range is 66 to 100, its displayed value is `166 - deckPercent`, and a change sets `deckPercent = 166 - sliderValue`, which puts 100% (Flat) at the left end. Its step is computed the prototype's way so both endpoints stay reachable: `rawStep = Math.max(0.01, (0.0625 / thicknessInInches) * 100)`, `steps = Math.max(1, Math.round(34 / rawStep))`, `step = 34 / steps`. Hint labels below: Flat on the left, Heavily Domed on the right.
- A two-column row holding Family and Ratio. Family is a 1-to-5 step-1 slider labelled `Family — {name}` from `railFamilyLabel`, with Boxy / Medium / Knifey hints beneath. Ratio is a 30-to-70 step-1 slider labelled `Ratio — {top}/{100 - top}`, with a `Sym` checkbox in its header row and four tick captions positioned beneath at 0%, 50%, 75% and 100%: 30/70, 50/50, 60/40, 70/30.
- Tail only: a `Hard Edge` checkbox directly beneath the ratio column, checked by default.
- An `Advanced` disclosure (collapsed by default, indented, its own smaller accent header) containing: a `Corner Cut Offset — {fraction}` slider over 0 to 1.5 inches step 1/16 whose value is the computed `result.cornerCutOffset` (falling back to zero when removed) so it shows the family default until the shaper moves it, with a `Remove` checkbox in its header row that disables the slider and drops it to 40% opacity, and the caption "0 = falls on Rail Mark 1"; a `Bottom Tuck 3 — {fraction}` slider over the same range whose value is the computed `result.bottomTuck3`, with a `Use Single Tuck` checkbox in its header row; and a `↺ Reset Advanced Settings` text button clearing that section's two overrides to null and its Remove and Use Single Tuck flags to false. On the tail, hide the whole Bottom Tuck 3 block while Hard Edge is on, matching the prototype.

Every slider change converts through `inchesToMm` before it reaches `RailBandSpec`; every label formats through `formatInchesFraction`.

**`rail-section-plot.tsx`** (client). Draws one section's cross-section. Props: the section output from `computeRailBands`, plus the shared x-axis minimum. Reproduce `buildPlot`'s pixel mapping (lines 1029-1042) with the prototype's constants — `SCALE` 56 px per inch, `LEFT_PAD` 22, `AXIS_LABEL_PAD` 20, `minX = xAxisMin - 0.15`, `maxX = 0.15`, `minY = -0.15`, `maxY = yAxisMax + 0.15` — and no callout or crop handling. Draw, in order: the 1-inch grid (`#ece5d4`, verticals from 0 down to `floor(xAxisMin)` and horizontals from 0 up to `floor(yAxisMax)`); the three reference lines (`#cbbf9e`, dashed, at y=0, at blank thickness, and the vertical apex line); the segments at stroke-width 2 with `vector-effect="non-scaling-stroke"`; the dots at r 2.75; then x and y tick marks with their labels.

Colours live here, in one constant map keyed by segment key: `domedBand` `#6b8e4e`, `band1` `var(--outline-accent)`, `band2` `#b5563a`, `cornerCut` `#4d8a86`, `hardEdge` `#1c1b19`, `tuck1` `#7d5ba6`, `tuck2` `#3a6ea5`, and `boardConn` / `bottomConn` / `railConn` all `#1c1b19`. Dots follow the prototype's rule: one dot at each end of every segment except the three connectors (and except the hard-edge segment when hard edge is on), plus an Apex Center dot at `#a8425f`, plus a Tapered Rail Thickness dot at `#6b8e4e` on the domed band's apex end when the section is domed.

Render the `<svg>` with `viewBox="0 0 {width} {height}"` and `width="100%"`, so the parent's width sets the on-screen size. Ticks are SVG `<text>` at 10px in `#6b6355`, not absolutely-positioned HTML spans — the prototype only used spans because of its own layout constraints.

Uniform scale across the three plots comes from two facts together, and both are required: every plot is built with the same `xAxisMinShared` (so `minX` and `maxX`, and therefore `width`, are identical), and all three are rendered into the same parent width. Compute the shared minimum in the editor as the smallest `bounds.xAxisMin` over the open sections and pass it down, exactly as the prototype's second pass at lines 1218-1233 does.

Stack the plots vertically inside one white card (`border-[#e4ddc9]`, rounded-xl, padded), each under a 16px extra-bold title reading Nose, Center or Tail, with the card heading "Rail Viewer" above them. Beneath all three, render one shared legend row — built from the first open section — as small colour dots with labels: Apex Center first, then each segment's label except the two hidden connector keys, then Tapered Rail Thickness when that section is domed.

**`rail-data-table.tsx`** (client). Renders `mergeRailDataTable` output: a header row with an empty label cell (flex 1.4) and one right-aligned bold column title per open section, then the three groups in order Rail Side, Deck Side, Bottom, each with an uppercase accent heading, each row a muted label plus right-aligned bold nowrap values with a `#f3efe3` bottom rule. Format `Mm` values with `formatInchesFraction`, render null cells as an em dash, and render the hard-edge sentinel as `Hard Edge`. Below the table, the prototype's italic muted footnote in its own card: "This rail band calculator is intended to provide a quantitative aspect to shaping consistent surfboard rails. It's recommended to understand how rail shapes affects surfboard performance, and how these marks can result in producing your desired outcome."

**`app/design/rails/page.tsx`.** Match `app/design/outline/page.tsx`: a server component exporting `metadata` (title "Rail Band Calculator — Shaper", a plain-English description) and rendering `<RailBandEditor />`.
  </action>

  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <done>`/design/rails` builds and type-checks. The sidebar carries all three collapsible sections with thickness, deck profile, family, ratio + Sym, tail Hard Edge and the Advanced group; the VIEWER page stacks three uniformly-scaled cross-section plots on a 1-inch grid with a shared legend; the DATA page shows the merged three-column table.</done>
</task>

<task type="auto">
  <name>Task 3: Add the TEMPLATE / RAILS top nav across both design screens</name>
  <files>components/site-nav.tsx, app/design/layout.tsx</files>

  <read_first>
    - `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md` — nested layout signature and the typed props helper this Next version uses. Read it before writing the file; `app/layout.tsx` already uses `LayoutProps<"/">`, so a `/design` layout takes the matching typed props for its own segment.
    - `node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md` — the current linking API and the hook for reading the active path.
    - `app/layout.tsx` — the root layout's `min-h-full flex flex-col` body, which the new layout has to sit inside without breaking the editors' full-height flex chain.
  </read_first>

  <action>
Add one nested layout at `app/design/layout.tsx` wrapping both `/design/outline` and `/design/rails`, so neither page has to know the nav exists. It renders `<SiteNav />` then `{children}` inside a `flex min-h-0 flex-1 flex-col` column — both editors size themselves with `flex-1` against a full-height parent, so the column must pass that height through rather than collapsing it.

`components/site-nav.tsx` is a client component (it reads the active path to mark the current link). Style it as a slim bar in the sidebar's own dark tone (`bg-outline-sidebar-bg`) with a bottom border in `border-outline-sidebar-divider`: the wordmark SHAPER on the left in `text-outline-sidebar-text`, then two links — TEMPLATE to `/design/outline` and RAILS to `/design/rails` — set in the prototype's section-header style, uppercase, bold, tracked-out, small. The active link is `text-outline-accent` with a 2px accent underline; the inactive link is `text-outline-sidebar-text-muted` and turns accent on hover. Use the framework's link component, not a bare anchor, so navigation stays client-side.

Leave `app/page.tsx`'s redirect to `/design/outline` as it is.
  </action>

  <verify>
    <automated>npm run build && npx tsc --noEmit && grep -q '/design/rails' components/site-nav.tsx && grep -q '/design/outline' components/site-nav.tsx</automated>
  </verify>

  <done>Both `/design/outline` and `/design/rails` render the top nav, each highlighting its own link, and neither editor's full-height layout is broken by the added bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> React state | Slider and checkbox values are the only inputs; they never leave the browser |
| Build script -> repo | `scripts/extract-prototype-rails-golden.mjs` executes code extracted from a repo-local reference file at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lm0-01 | Tampering | `scripts/extract-prototype-rails-golden.mjs` `new Function` over `Rails.dc.html` | low | accept | Dev-only script over a committed, reviewed reference file; never runs in the app, in CI deploy, or on user input. Same posture already accepted for the outline extractor. |
| T-lm0-02 | Denial of Service | `computeRailBands` on slider input | low | mitigate | Slider ranges are bounded (thickness 1-3.5in, family 1-5, ratio 30-70, tucks 0-1.5in) and the math is a fixed number of arithmetic steps with no loops, so no input reachable from the UI can make it hang. |
| T-lm0-03 | Information Disclosure | rails screen | low | accept | No network calls, no auth, no persistence in this task — nothing to disclose. |
| T-lm0-SC | Tampering | npm installs | high | mitigate | No new packages are added by this plan. If one becomes necessary, stop and run the package-legitimacy gate before installing. |
</threat_model>

<verification>
1. `npm run golden` regenerates both fixture files with no git diff — proving the goldens still come from the prototype and were not hand-edited.
2. `npm test` passes, including the 11 rail-band fixtures across all three sections.
3. `npm run build` succeeds and `npm run lint` is clean.
4. `npm run dev`, then in the browser: `/design/rails` shows the default rail setup; moving Deck Profile off Flat makes the Domed Deck Band segment and the Board Thickness / Domed Deck Band rows appear; unchecking tail Hard Edge makes Rail Tuck 1, Bottom Tuck 1 and Bottom Tuck 2 appear on the tail; the DATA tab shows the merged three-column table; the TEMPLATE / RAILS nav moves between the two screens.
5. Stop here for the user's browser review. Do not deploy.
</verification>

<success_criteria>
- `lib/geometry/rail-bands.ts` reproduces the prototype's `computeSection`, `buildProfilePoints`, `buildSegmentDefs` and `cardFromResult` outputs to within 1e-9 inch for 11 fixture states x 3 sections
- The module's public surface is millimetres only, imports nothing from React/Next/DOM, and its inch-domain core is private
- `buildRailProfile` is implemented and pinned, ready for Volume, though nothing on this screen renders it
- `/design/rails` reproduces the prototype's Rail Band Calculator sidebar, VIEWER page and DATA page
- The three cross-section plots share one x-axis minimum and one render width, so they are genuinely comparable at a glance
- Every on-screen number is an inch fraction; every stored value is `Mm`
- A top nav links `/design/outline` and `/design/rails` on both screens
- Nothing from `<scope_boundaries>` "out of scope" was built, stubbed, or left as a dead button
</success_criteria>

<output>
Create `.planning/quick/260818-lm0-rebuild-rail-band-calculator-screen-lib-/260818-lm0-SUMMARY.md` when done
</output>
