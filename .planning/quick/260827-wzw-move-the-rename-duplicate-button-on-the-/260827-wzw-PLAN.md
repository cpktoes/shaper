---
phase: 260827-wzw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/setup/board-rack-card.tsx
autonomous: true
requirements: [QUICK-260827-wzw]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a saved board card in the rack, the three-dot Rename/Duplicate/Delete trigger sits in the bottom-right corner instead of the top-right."
    - "The trigger anchors to the card itself, so it stays in the card's own corner even when a duplicate-failed message is showing beneath the card."
    - "Clicking the trigger still opens the menu and does not open the board — the trigger is still a DOM sibling of the whole-card button, never nested inside it."
    - "Rename, Duplicate and Delete still do exactly what they did before; the menu's contents and its Base UI structure are untouched."
    - "The card's thumbnail, name, dimensions line, last-touched line and 'Open This Board' line are all unchanged."
  artifacts:
    - components/setup/board-rack-card.tsx
  key_links:
    - "The absolutely-positioned trigger and its `relative` ancestor must be the same box. If the `relative` class stays on the outer wrapper (which also holds the duplicate-error message), a bottom offset measures from the bottom of the error text, not the bottom of the card, and the trigger drifts below the corner exactly when something has gone wrong."
    - "The trigger must remain a sibling of the whole-card `<button>`, never a child. Nesting it would be invalid HTML and a click on the trigger would bubble into the card's own navigation — the reason the wrapper exists at all (documented in this file's header comment)."
---

<objective>
Move the three-dot Rename/Duplicate/Delete button on each saved board card from the top-right
corner to the bottom-right corner.

Purpose: the shaper asked for the control to sit at the foot of the card rather than over the
top of the board drawing.

Output: one repositioned control in `components/setup/board-rack-card.tsx`, with the card's
contents, the menu's contents and every action's behaviour left exactly as they are.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@components/setup/board-rack-card.tsx
@components/setup/rack-card-menu.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Anchor the board-actions button to the card's bottom-right corner</name>
  <files>components/setup/board-rack-card.tsx</files>
  <read_first>
    `components/setup/board-rack-card.tsx` — the saved-variant `return` block (roughly lines
    145-171) and the file's header comment (lines 22-27), which explains why the trigger is a
    sibling of the whole-card button rather than a child of it.

    `components/setup/rack-card-menu.tsx` — read only to confirm the trigger accepts a
    `className` and that its positioning comes entirely from the caller. Do not edit this file.
  </read_first>
  <action>
    Two edits, both inside the saved-variant `return` of `board-rack-card.tsx`.

    First, give the trigger a positioning box that covers the card and nothing else. Today the
    outer wrapper is the positioning ancestor, and that wrapper also holds the `duplicateError`
    paragraph — so a bottom-anchored offset would measure from the bottom of the error text
    instead of the bottom of the card. Introduce one plain `<div>` that wraps only the whole-card
    `<button>` and the `<RackCardMenu>` together, and move the positioning-context class onto that
    new div. The outer wrapper keeps its flex-column classes and the incoming `className`; the
    `duplicateError` paragraph stays where it is in the outer wrapper, below the new div, so it
    still renders beneath the card. The button and the menu must remain siblings inside the new div
    — do not nest the trigger inside the button.

    Second, on the `<RackCardMenu>` call, replace the corner-offset utilities in its `className`
    with the bottom-right pair, so the whole value reads exactly
    `"absolute right-2 bottom-2 z-10"`. Keep `z-10` — the trigger still paints over the card
    button. Change nothing else about that call: `boardName`, `onRename`, `onDuplicate` and
    `onDelete` stay as they are, and `rack-card-menu.tsx` is not touched at all.

    Then bring the file's header comment in line with what the code now says. The passage
    describing the wrapper currently states that the button and the menu are siblings inside a
    relatively positioned wrapper with the trigger over the card's corner; it must now describe the
    dedicated card-sized box that holds those two siblings, and say which corner the trigger sits
    in, so a later reader is not told the offset lives somewhere it does not. Leave the reasoning
    about why the trigger cannot be nested inside the button intact — it is still the load-bearing
    constraint. Do not add any explanation of the previous corner or of this move; the comment
    describes the code as it stands, not its history.
  </action>
  <verify>
    <automated>
      # the trigger carries the bottom-right offsets
      grep -c 'absolute right-2 bottom-2 z-10' components/setup/board-rack-card.tsx   # expect 1
      # no top-corner offset survives outside comment lines
      grep -vE '^\s*(\*|//|/\*)' components/setup/board-rack-card.tsx | grep -c 'top-2'  # expect 0
      # the menu is still rendered exactly once, still outside the card button
      grep -c '<RackCardMenu' components/setup/board-rack-card.tsx                    # expect 1
      npx tsc --noEmit
      npm run lint
    </automated>
  </verify>
  <done>
    `components/setup/board-rack-card.tsx` renders the saved card as an outer flex column
    containing (a) a card-sized positioning box holding the whole-card button and the menu trigger
    as siblings, and (b) the duplicate-error paragraph; the trigger's className is
    `absolute right-2 bottom-2 z-10`; the header comment describes that structure; typecheck and
    lint both pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm the moved button in the running app</name>
  <files>components/setup/board-rack-card.tsx</files>
  <precondition>
    `npm run dev` is reachable at http://localhost:3000 and the browser is signed in to an account
    that already has at least one saved board on the development Neon branch — saved cards, and
    therefore this menu, only render for a signed-in shaper with saved rows.
  </precondition>
  <action>
    Start the dev server and look at the home screen's rack of saved boards. Confirm the change
    reads correctly and that nothing else on the card moved. If the shaper reports a problem,
    adjust the offsets or the wrapper in Task 1's file only — do not change the menu's contents,
    its actions, or any other card element to accommodate it.
  </action>
  <verify>
    <automated>npm test</automated>
    <human-check>
      On http://localhost:3000, signed in with at least one saved board:
      1. The three-dot button sits in the bottom-right corner of each saved board card, clear of
         the board drawing it used to cover.
      2. It does not collide with or obscure the board name, the dimensions line, the
         "Last touched" date or the "Open This Board" line.
      3. Clicking it opens the Rename / Duplicate / Delete menu and does NOT open the board.
         Note whether the menu opening downward from the lower corner reads acceptably over the
         cards below it — if it does not, that is a follow-up task, not a change here.
      4. Rename opens the rename dialog, Delete opens the delete confirmation, Duplicate copies the
         board — all exactly as before.
      5. Clicking anywhere else on the card still opens that board, and keyboard focus on the card
         still shows the accent ring.
      6. The in-progress (not-saved) card is unchanged and still has no three-dot button.
    </human-check>
  </verify>
  <done>
    The shaper confirms the button sits in the bottom-right corner of the saved cards, the menu and
    its three actions behave exactly as before, and the rest of the card is untouched. Geometry
    suites stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none crossed)* | This change moves one presentational control within an already-rendered card. It adds no input, no network call, no query and no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260827-wzw-01 | Elevation of Privilege | `board-rack-card.tsx` trigger placement | low | mitigate | The destructive Delete action must stay behind the same deliberate menu open, not become a one-click target. Task 1 moves only the trigger's offsets; the menu's items are untouched, and Task 2's human check confirms Delete still routes through its confirmation dialog. |
| T-260827-wzw-02 | Tampering | card click routing | low | accept | The trigger stays a sibling of the card button, so a click on it cannot bubble into board navigation. No new event handling is introduced; the existing structure is preserved by construction. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- `npm test` — all geometry suites green (this change touches no `lib/` code, so any failure here
  is unrelated and must be reported, not worked around)
- Human confirmation per Task 2
</verification>

<success_criteria>
The three-dot Rename/Duplicate/Delete button on every saved board card appears in the card's
bottom-right corner and stays there whether or not a duplicate-failed message is showing; the
menu, its three actions and the rest of the card are byte-for-byte unchanged in behaviour.
</success_criteria>

<output>
Create `.planning/quick/260827-wzw-move-the-rename-duplicate-button-on-the-/260827-wzw-SUMMARY.md` when done
</output>
