---
phase: quick-260830-43t
plan: 01
subsystem: ui
tags: [tailwind, flexbox, print, order-form]

requires: []
provides:
  - RIDER INFO band's three ruled lines (Name, Ph #, Height/Weight) sit on one even pitch derived from the column's own height, not a tuned pixel value
affects: [summary, order-form]

actuals:
  tokens: 570
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns:
    - "`justify-evenly` (no `gap`, no `py`) to distribute space into four equal slots when the visual mark (a ruled line) is a row's bottom edge rather than its own box — spacing boxes evenly is not the same as spacing the ink evenly"

key-files:
  created: []
  modified:
    - components/summary/order-form.tsx

key-decisions:
  - "Rider column class changed from `flex min-w-0 flex-1 flex-col justify-between gap-1 py-0.5` to `flex min-w-0 flex-1 flex-col justify-evenly` — three deletions and one word, no new pixel constant, per the plan's derivation"
  - "Comment above the column rewritten from an argument FOR the old justify-between choice to an explanation of why justify-evenly is correct (rule-is-the-box's-bottom-edge, four-equal-slots, no-gap/no-py, band-untouched)"

patterns-established:
  - "When a field's underline is baseline-anchored (the value span's own `border-b`), treat the visible mark as the row's bottom edge for spacing purposes, not the row box itself — `justify-evenly` (not `justify-between`) is what actually spaces the marks evenly"

requirements-completed: [QT-260830-43t]

coverage:
  - id: D1
    description: "Rider column's three ruled lines (Name, Ph #, Height/Weight) sit on one even pitch, derived from the band's own height with no pixel constant"
    requirement: "QT-260830-43t"
    verification:
      - kind: unit
        ref: "npm test — 1270/1270 passing (no unit coverage for this layout-only area; suite is a regression guard)"
        status: pass
      - kind: manual_procedural
        ref: "Task 1 browser <verify> snippet (EVEN_PITCH, HEADROOM_EQ_PITCH, TAIL_EQ_PITCH_LESS_ROW, HEIGHT_WEIGHT_SHARE_A_RULE, BAND_UNCHANGED) — not run from this worktree, no browser available; orchestrator to run post-merge"
        status: unknown
    human_judgment: true
    rationale: "Task 2 of the plan is a blocking human-verify checkpoint (visual + print-preview judgment on the founder's own eye, plus a judgment call on writing-room adequacy) that this executor cannot perform — no browser/dev server in this worktree. Deferred to the orchestrator per task constraints."

duration: 12min
completed: 2026-08-30
status: complete
---

# Phase quick-260830-43t Plan 01: Re-space the RIDER INFO rider column Summary

**Rider column's three write-in lines (Name, Ph #, Height/Weight) switched from `justify-between` (spacing the row BOXES evenly) to `justify-evenly` (spacing the four blank SLOTS evenly), giving Name real writing room without touching the header band's 12% share.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-30T03:05:00Z
- **Completed:** 2026-08-30T03:17:00Z
- **Tasks:** 1 of 2 (Task 2 is a blocking human-verify checkpoint, deferred — see below)
- **Files modified:** 1

## Accomplishments
- Rider column's class string changed from `flex min-w-0 flex-1 flex-col justify-between gap-1 py-0.5` to `flex min-w-0 flex-1 flex-col justify-evenly` — exactly the diff specified in the plan's `<design_decision>` section 2, no more and no less
- Stale comment (which argued FOR the old `justify-between` choice) rewritten to explain the new layout: the rule-is-the-box's-bottom-edge fact, why four equal slots fix the top-crowding, why `gap`/`py` are deliberately absent, and why the header band's 12% share cannot be affected
- `npm run lint` clean (0 errors; 9 pre-existing warnings in unrelated files, out of scope) and `npm test` 1270/1270 passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Re-space the rider column so the three ruled lines share one pitch** - `c4efdca` (fix)

**Plan metadata:** not yet committed — orchestrator handles final docs commit per its own protocol.

## Files Created/Modified
- `components/summary/order-form.tsx` - Rider column (RIDER INFO band, lines ~222-238) reflowed from `justify-between`+`gap-1`+`py-0.5` to bare `justify-evenly`; comment above it rewritten

## Decisions Made
- Followed the plan's diff literally: no pixel constant was introduced anywhere, matching the `must_haves.truths` requirement that "the rhythm is derived, not tuned"
- Did not touch `.order-form-band-header`'s `flex: 0 0 12%`, `order-form.css`, or the Height/Weight wrapper's horizontal `gap-3` — all explicitly out of scope per the plan

## Deviations from Plan

None — plan executed exactly as written for Task 1. Task 2 (the checkpoint) is a designed stop this executor cannot perform, not a deviation.

## Predicted Post-Change Measurements

Cannot be verified from this worktree (no browser/dev server per the environment notes) — these are the arithmetic predictions for the orchestrator to check against a live re-measurement, using the orchestrator's own supplied before-state:

Given: `sheet { l 24, w 516, h 1391.8 }`, `band { t 164.5, l 31.5, w 501, h 137 }`, rider column `{ t 164.5, b 301.5, h 137 }`, row height 19px, before-figures `head 21 · p1 57 · p2 57 · tail 2` (rules at 185.5 / 242.5 / 299.5).

With `justify-evenly` on a 137px-tall column holding three 19px rows:

```
slot s = (137 − 3×19) / 4 = 20
pitch  = s + row = 20 + 19 = 39
```

| | before | **predicted after** | moves |
|---|---|---|---|
| band top → NAME rule (head) | 21 | **39** | +18 |
| NAME rule → PH # rule (p1) | 57 | **39** | −18 |
| PH # rule → HEIGHT rule (p2) | 57 | **39** | −18 |
| HEIGHT rule → band bottom (tail) | 2 | **20** | +18 |
| NAME rule, absolute | 185.5 | **203.5** | down 18 |
| PH # rule, absolute | 242.5 | **242.5** | unchanged |
| HEIGHT/WEIGHT rule, absolute | 299.5 | **281.5** | up 18 |

This is arithmetically identical to the plan's own `<design_decision>` section 3 acceptance table — confirms the code change is the one the plan derived, nothing more.

Non-regression: `.order-form-rocker` box and the sheet rectangle are predicted **unchanged** (identical `t`/`l`/`w`/`h` to the before-capture) — the rider column is a `flex-1` child inside `.order-form-band-header`'s fixed `12%` share, and this change only redistributes space already inside that column. If the orchestrator's post-merge re-measurement shows either rectangle moved, that is a genuine regression this plan's `<design_decision>` section 6 explicitly says must not happen — stop and report rather than compensate.

## Known Stubs

None.

## Threat Flags

None — layout-only change to three CSS utility classes on one existing client component, no new trust boundary.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Task 1 (the implementation) is complete, committed, lint-clean and test-green. **Task 2 — the blocking human-verify checkpoint — is explicitly deferred**: it requires a running dev server and a browser to look at http://localhost:3000/design/summary, check the print preview, and answer the plan's "is 39px/7.8mm enough writing room, or would you rather the lines were further apart" judgment call. This executor has neither a dev server nor a browser available in its worktree. The orchestrator should:
1. Re-run the plan's Task 1 `<verify>` browser snippet at container width 880px and confirm the five checks (`EVEN_PITCH`, `HEADROOM_EQ_PITCH`, `TAIL_EQ_PITCH_LESS_ROW`, `HEIGHT_WEIGHT_SHARE_A_RULE`, `BAND_UNCHANGED`) all print true, and that the absolute rule positions match the Predicted table above.
2. Confirm the `.order-form-rocker` and sheet rectangles are byte-identical to the orchestrator's own before-capture.
3. Surface Task 2's checkpoint (visual look, print preview, and the writing-room judgment call) to the founder for approval.

---
*Phase: quick-260830-43t*
*Completed: 2026-08-30*
