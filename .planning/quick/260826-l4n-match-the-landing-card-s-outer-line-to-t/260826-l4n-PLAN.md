---
phase: quick-260826-l4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/setup/preset-card.tsx
autonomous: true
requirements: [QUICK-260826-l4n]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The line where the landing card's sand frame meets the white window inside it is now the SAME line the design screens use around their working panel. That is the whole request: the founder said 'match them', so they now match."
    - "In Daylight, Chalk and Phosphor nothing changes on screen at all — the two line colours are identical in those three themes. Slate is the only theme where the founder will see a difference, and there the line goes from almost invisible to clearly visible."
    - "The line around the board's own surface — the inner one — stays soft. Two lines of different weight, one inside the other, is the point: the outer one says where the window starts, the inner one just groups the drawing. This is exactly the pairing the design screens use."
    - "The card still paints no line around its outside at rest, and hovering it or tabbing to it still lights up the accent edge and the accent ring."
    - "No new colour and no new token. This reuses --surf-line, which already exists, is already wired to all four themes, and is already what components/viewer/tabbed-panel.tsx paints its panel edge with."
    - "The Continue Current Board card is untouched — it has no window box — and the two cards' outer frames still match each other exactly."
    - "The file's own comments now describe the card that exists. The previous task wrote a paragraph arguing for the softer line, including its measured cost; that argument is now reversed and must not survive, or the next editor will undo this change on the strength of it."
    - "npm test and npm run lint are green."
  artifacts:
    - components/setup/preset-card.tsx
    - .planning/quick/260826-l4n-match-the-landing-card-s-outer-line-to-t/260826-l4n-SUMMARY.md
  key_links:
    - "The ONLY markup that changes is one word in one className: the window box `<div>`'s `border-surf-line-faint` becomes `border-surf-line`. Nothing else in the file's markup is edited."
    - "SEQUENTIAL-RENAME TRAP: `border-surf-line-faint` contains `border-surf-line` as a prefix. Do NOT run a bare find-and-replace on `border-surf-line`, and do NOT use a regex without a trailing boundary — either will corrupt the well's class on the very next line. Anchor the edit on the full window-box class string."
    - "The well's `overflow-hidden rounded-lg border border-surf-line-faint bg-surf-panel` must survive BYTE-IDENTICAL. It is deliberately the softer token and always was."
    - "The card button's `border border-transparent` slot must survive untouched. `hover:border-surf-accent-ink` and `focus-visible:border-surf-accent-ink` set colour only, not width, so restoring a resting colour there silently kills both interactive states. Set transparent deliberately in 260826-k5o."
    - "MEASURED, already computed — do not re-derive: --surf-line and --surf-line-faint hold the SAME value in Daylight (#897c58), Chalk (#897c58) and Phosphor (#3e783e). Only Slate differs: #333842 -> #6a707c. Slate contrast goes 1.43:1 -> 3.39:1 against the sand frame and 1.56:1 -> 3.70:1 against the window, clearing the project's 3:1 non-text bar on both sides where neither did before."
    - "MEASURED: this task changes colour only, never width. Border width stays 1px, so every dimension on the card is unchanged. ANY geometry movement is a failure, not an expected side effect."
---

<objective>
Repoint one line on the landing page's board cards: the edge where the sand frame meets the
white window box moves from the soft line token to the structural one, so it matches the edge
`components/viewer/tabbed-panel.tsx` draws around every design screen's working panel.

Purpose: in 260826-kim that edge was given the soft token because the founder's word was
"faint". They were told plainly that the design screens use the slightly stronger line there,
that three of four themes are identical either way, and that Slate is the only theme where the
card reads softer — and offered the flip. The founder's answer was "match them". This is that
flip; it is locked, and there is nothing left to design.

Output: one token change in `components/setup/preset-card.tsx`, the two blocks of prose that
argued for the *previous* choice rewritten to describe the matched treatment, and a browser
measurement in Slate and Daylight.

The stack after this task, outermost first — unchanged except for the weight of one line:
page (`--surf-ground`) -> sand frame (`--surf-canvas`, 12px band) -> **structural line
(`--surf-line`)** -> window (`--surf-tab-active`, 12px band) -> receding line
(`--surf-line-faint`, on the well) -> panel (`--surf-panel`) -> board.

That is now the same two-weight pairing, in the same order, as `tabbed-panel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@components/setup/preset-card.tsx
@components/viewer/tabbed-panel.tsx
</context>

<decisions_already_made>
Do not re-open these. They were settled by the founder or by measurement before this plan was
written.

1. **The window box's token becomes `--surf-line`.** The founder was shown the tradeoff in full
   and answered "match them". There is no remaining judgement call here.

2. **The well keeps `--surf-line-faint`.** Do not "finish the job" by moving the inner line too.
   The two-weight pairing IS the treatment being matched: `tabbed-panel.tsx` puts `--surf-line`
   on the outer panel edge and `--surf-line-faint` on the inner content card, for the reason its
   own docstring gives — a panel boundary is structural, a content grouping should recede.
   Making both the same weight would break the match this task exists to create.

3. **The card button's resting edge stays transparent.** Do not restore a resting colour.

4. **No token is added and `app/globals.css` is not opened.** `--surf-line` already exists and is
   already bridged to `border-surf-line`. The three-layer palette is not touched, and
   `@theme static` is never switched to `inline`.

5. **`ContinueBoardCard` gets nothing from this task** and is not opened. It has no window box.

6. **No palette or contrast test is written.** `lib/design/palette.test.ts` was removed on
   purpose. Do not reinstate it and do not tell the founder to run it.
</decisions_already_made>

<tasks>

<task type="tracer">
  <name>Task 1: Flip the window box to the structural line, and reverse the comments that argue against it</name>
  <files>components/setup/preset-card.tsx</files>
  <reversibility rating="reversible">One token plus prose; `git revert` restores the previous card exactly.</reversibility>
  <action>
Make exactly one markup change. The middle `<div>` — the window box, currently
`className="rounded-lg border border-surf-line-faint bg-surf-tab-active p-3"` — becomes
`className="rounded-lg border border-surf-line bg-surf-tab-active p-3"`.

**Do this as a whole-string replacement, anchored on that complete class string.** Do not run a
find-and-replace on the token alone and do not use a regex that lacks a trailing boundary: the
well's class on the very next line begins with the same characters, and a loose pattern will
corrupt it. After editing, the window box `<div>` and the well `<div>` must differ in exactly
one place.

Touch nothing else in the markup. The card `<button>`'s className string is not edited. The
well's className string is not edited. `ContinueBoardCard` is not opened. `app/globals.css` is
not opened.

Then rewrite the file's two blocks of prose. Both were written in 260826-kim to justify the
choice this task reverses, so both now argue against the code they sit above — the specific
failure mode here is a future editor reading them and reverting this change on their strength.
A partial comment edit is worse than none.

**Head-of-file docstring.** Its closing sentence describes the sand band's inner edge as a soft
hairline. Correct it: that edge is now the same structural line the design screens use, and the
card as a whole now mirrors `components/viewer/tabbed-panel.tsx` — structural weight on the
outer box, receding weight on the inner one.

**Inline comment above `return`.** Rewrite it to carry these facts, in the file's existing voice
and at its existing density (this file uses long explanatory comments; match that, do not pad).
When naming tokens, write them as `--surf-line` and `--surf-line-faint` — the custom-property
form the file already uses — never as Tailwind class names, so the file's class strings stay
greppable:

  - the stack, outermost first, exactly as listed in this plan's `<objective>`, with the sand
    frame's inner edge labelled as the structural one and the well's as the receding one
  - that `--surf-ground`, `--surf-tab-active` and `--surf-panel` hold the same value in all four
    themes and `--surf-canvas` is the only distinct surface, so the two hairlines are doing all
    of the separating work. This is the fact most likely to be "fixed" by someone who has not
    measured it, and it is why neither line may be simplified away.
  - WHY the two lines are different weights, and that this is deliberate rather than an
    oversight: it is the pairing `components/viewer/tabbed-panel.tsx` uses, for the reason that
    file's own docstring gives — a panel boundary is structural, a content grouping should
    recede. Cite `tabbed-panel.tsx` by path so the source of the treatment is findable.
  - that the two tokens hold the same value in three of the four themes, so this pairing is only
    actually visible in Slate — and there the outer line measures 3.39:1 against the sand frame
    and 3.70:1 against the window, both clearing the project's 3:1 non-text bar.
  - the still-true warning that the card's resting outer edge must stay transparent because the
    hover and focus-visible states set colour only, not width.

Delete the paragraph that argued this edge should be softer than its design-screen counterpart
because a soft line was asked for, together with the two Slate figures it cited as the price of
that softer choice. Those numbers describe a card that no longer exists; leaving them in place
would tell a future editor that this change made Slate worse, when it made it better.
  </action>
  <verify>
    <automated>grep -c 'rounded-lg border border-surf-line bg-surf-tab-active p-3' components/setup/preset-card.tsx</automated>
    <automated>grep -c 'overflow-hidden rounded-lg border border-surf-line-faint bg-surf-panel' components/setup/preset-card.tsx</automated>
    <automated>grep -c 'border border-transparent bg-surf-canvas' components/setup/preset-card.tsx</automated>
    <automated>grep -oE 'border-surf-line(-faint)?' components/setup/preset-card.tsx | sort | uniq -c</automated>
    <automated>grep -c 'faint hairline' components/setup/preset-card.tsx</automated>
    <automated>grep -cE '1\.43|1\.56' components/setup/preset-card.tsx</automated>
    <automated>grep -cE '3\.39|3\.70' components/setup/preset-card.tsx</automated>
    <automated>grep -c 'tabbed-panel' components/setup/preset-card.tsx</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
  </verify>
  <done>
Greps 1-3 each return 1: the window box now carries the structural line, and the well's class
and the button's transparent edge slot both survived byte-identical.

Grep 4 — the rename-trap gate — prints exactly two rows, `1 border-surf-line` and
`1 border-surf-line-faint`. Two of either, or a zero, means the replacement was loose and the
well was hit, or a comment now contains a Tailwind class name.

Grep 5 returns 0 (the docstring no longer calls that edge soft). Grep 6 returns 0 (the stale
Slate figures for the previous choice are gone). Grep 7 returns 1 or more (the achieved figures
were carried into the rewritten comment, not merely deleted). Grep 8 returns 1 or more (the
comment cites the file whose treatment is being matched).

`npm run lint` and `npm test` are green (670 tests). `git diff --stat` shows exactly one file
changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove it in the browser — Slate changed, Daylight did not, nothing moved</name>
  <precondition>The dev server is running on localhost:3000 (`npm run dev`) and a browser can be driven against it.</precondition>
  <files>(no source edits — measurement only)</files>
  <action>
Open the landing page at `http://localhost:3000/` in a wide window, so the preset grid is at its
four-across breakpoint.

**Measure the rendered values, do not read class names back.** Reading a className proves the
string is in the DOM; it does not prove the browser painted the line. Use `getComputedStyle` and
report resolved rgb triples.

**Slate first — it is the only theme where this task does anything.** Switch to Slate via the
nav's settings gear. On the window box (the middle `<div>`) read `borderTopWidth`,
`borderTopStyle` and `borderTopColor`; on the well (the `<div>` holding the board) read
`borderTopColor`. Expected, already computed — you are confirming, not deriving:

  - window box border: `1px solid rgb(106, 112, 124)`  (was `rgb(51, 56, 66)`)
  - well border:       `rgb(51, 56, 66)`  (UNCHANGED — if this moved, the replacement was loose
    and the plan's rename trap caught you)

Then say plainly, in words, whether the outer line now reads as a line on screen and whether the
inner one is visibly softer than it. That two-weight difference is only perceptible in Slate;
this is the one chance to look at it.

**Daylight second — the no-op check.** Switch to Daylight. Both the window box and the well must
read `rgb(137, 124, 88)`, identical to each other and identical to before this task. Confirm the
card looks exactly as it did. If anything visibly changed in Daylight, something other than the
intended token was edited.

**Geometry — this task changes colour only, never width, so nothing may move.** Border width was
1px before and is 1px after. Confirm against the 260826-kim measurement:

  - card border-box width: 493px
  - window box border-box width: 467px
  - well border-box width: 441px
  - well aspect ratio: 0.5484
  - grid still four cards across, no wrap, no reflow, board not clipped

Any change to any of these is a failure — stop and report rather than adjusting padding to
compensate.

**Interactive states.** The cards carry `transition-colors` at 0.15s, which has already produced
one false alarm in this series. So trigger and read in SEPARATE calls: hover the card in one
call, then read the button's computed `borderTopColor` and `boxShadow` in the next. Repeat for
keyboard focus (Tab to the card). Both must resolve to the accent colour, and the 2px accent
ring must be present.

**Parity gate.** The two card components' outer `<button>` className strings were byte-identical
before this task and no test enforces it — only their docstrings do. Confirm they still match.
  </action>
  <verify>
    <automated>diff <(grep -o 'flex w-full flex-col gap-2 rounded-xl[^"]*' components/setup/preset-card.tsx) <(grep -o 'flex w-full flex-col gap-2 rounded-xl[^"]*' components/setup/continue-board-card.tsx) && echo BUTTON_CLASSNAMES_IDENTICAL</automated>
    <automated>git diff --name-only | tr '\n' ' '</automated>
    <automated>git diff -- components/setup/continue-board-card.tsx app/globals.css components/viewer/tabbed-panel.tsx | wc -l</automated>
    <human-check>In the browser: Slate's window-box border computes to rgb(106, 112, 124) while the well stays rgb(51, 56, 66); Daylight is unchanged with both at rgb(137, 124, 88); card 493px / window 467px / well 441px / aspect 0.5484 all unchanged; grid four across; board unclipped; hover and keyboard focus both still paint the accent edge and the 2px accent ring.</human-check>
  </verify>
  <done>
The parity check prints BUTTON_CLASSNAMES_IDENTICAL, `git diff --name-only` lists only
`components/setup/preset-card.tsx`, and the out-of-scope diff returns 0 lines.

Slate and Daylight both measured and recorded with their resolved line colours, with the Slate
verdict written in plain words. Geometry matches the 260826-kim numbers exactly. Hover and
focus-visible both confirmed painting the accent, each read in a call separate from the one that
triggered it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This task changes one CSS class token on one presentational `<div>`. It crosses no trust boundary, reads no user input, adds no dependency, and touches no data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l4n-01 | Tampering | package manager installs | low | accept | No packages are installed by this task; no legitimacy gate is required. |
| T-l4n-02 | Tampering | find-and-replace across `preset-card.tsx` | low | mitigate | `border-surf-line` is a prefix of `border-surf-line-faint`. Task 1 mandates a whole-string anchored replacement and gates on `grep -oE 'border-surf-line(-faint)?' \| sort \| uniq -c` printing exactly one of each. |
| T-l4n-03 | Denial of Service | landing page render | low | accept | Changing a border colour adds no layout or paint cost; Task 2's geometry check confirms no reflow. |
</threat_model>

<verification>
1. `npm test` green (670 passing) and `npm run lint` green (0 errors).
2. Task 1's greps: 1, 1, 1, exactly one row each for `border-surf-line` and
   `border-surf-line-faint`, then 0, 0, >=1, >=1.
3. Only `components/setup/preset-card.tsx` appears in `git diff --name-only`; the diff against
   `continue-board-card.tsx`, `app/globals.css` and `tabbed-panel.tsx` is empty.
4. Slate measured: window box `rgb(106, 112, 124)`, well `rgb(51, 56, 66)`, with a plain-words
   verdict on whether the outer line now reads and whether the inner one reads as softer.
5. Daylight measured and confirmed unchanged: both edges `rgb(137, 124, 88)`.
6. Geometry identical to 260826-kim — 493 / 467 / 441, aspect 0.5484, four across, unclipped.
7. Hover and focus-visible confirmed still painting the accent edge and ring, each read in a
   separate call from the one that triggered it.
8. The two cards' outer button className strings still byte-identical.

**No palette or contrast test exists.** `lib/design/palette.test.ts` was removed. Do not
reinstate it and do not tell the founder to run it — the contrast numbers in this plan were
computed by hand and are confirmed by looking at the screen.
</verification>

<success_criteria>
The line where the landing card's sand frame meets its white window is now the same line the
design screens draw around their working panel, so the two match. The softer line around the
board drawing itself is untouched, which is what makes the pairing match rather than merely the
colour. Daylight, Chalk and Phosphor look exactly as they did; Slate's outer line is now visible
where it was not. The board, the grid, hover and focus are all unchanged, and the file's
comments describe the card that now exists rather than arguing for the one that no longer does.
</success_criteria>

<output>
Create `.planning/quick/260826-l4n-match-the-landing-card-s-outer-line-to-t/260826-l4n-SUMMARY.md`
when done.

Write it in plain English for a shaper: what changed on the landing page, that three of the four
colour themes look identical and only Slate visibly changed, and that the line around the board
drawing itself was deliberately left alone.
</output>
